> A row is an attempt, not a sale.
> Failed attempts are kept for fraud review and are never deleted.

That comment is on `order_v2` and it is why the counts in the dashboard are 22% higher than finance's.

| status | rows |
| --- | --- |
