# Decisions (ADR-lite; one line each: what + why)

- 2026-06-12 · Tauri 2 over Electron/SwiftUI: small/fast/native feel; Rust core for streaming perf.
- 2026-06-12 · tokio-postgres over sqlx: need RowDescription `table_oid()`/`column_id()` for editable results, CancelToken, notices.
- 2026-06-12 · CodeMirror 6 over Monaco: ~1MB vs ~5MB, faster in WKWebView; intellisense quality comes from our completion engine either way.
- 2026-06-12 · Custom DOM grid (TanStack Virtual) over Glide Data Grid: full UX control; Glide is the fallback at P2 checkpoint.
- 2026-06-12 · SSH tunnels via system `ssh -L` subprocess: respects ~/.ssh/config; russh only if subprocess proves flaky.
- 2026-06-12 · Passwords in macOS Keychain (keyring crate); rusqlite app-db stores everything else.
- 2026-06-12 · JSON row values over Channel v1; binary/Arrow only if profiling demands.
- 2026-06-12 · zustand for state; `motion` for springs; lucide icons; cmdk palette.
- 2026-06-12 · `time` pinned to 0.3.47 in Cargo.lock: 0.3.48 breaks tauri-utils 2.9.2 with E0119 (conflicting From impls). Unpin when tauri-utils ships a fix; don't `cargo update` blindly.
- 2026-06-12 · keyring crate pinned to v3 (apple-native): v4 is a restructured meta-crate needing store init; v3 API is stable and sufficient.
- 2026-06-12 · Execution uses PG **simple protocol** (`simple_query`): every type arrives as wire text (psql-identical), multi-statement native. P5 editability metadata comes from a separate `prepare()` (RowDescription table_oid/attnum) without re-executing.
- 2026-06-12 · TLS = rustls with no-verify ServerCertVerifier ≈ psql `sslmode=require` semantics (RDS works out of the box); `prefer` tries TLS then falls back to plain.

### v0.1.5
- 2026-06-13 · Editability via `prepare()` only (no re-exec); ctid is the row-locator fallback when no PK is in the result (editable-with-warning); multi-cell edits to one row batch into a single UPDATE (`plan_edits` groups by table+row).
- 2026-06-13 · One dedicated PG connection per query tab (`ensureTabSession`) so transactions/temp state stay coherent; schema introspection runs on a separate per-profile *primary* session. Tx-open indicator is a SQL-sniff heuristic (tokio-postgres doesn't expose ReadyForQuery).
- 2026-06-13 · `window.confirm()` is a silent no-op in WKWebView → all destructive actions use a custom DangerModal or a two-click arm pattern, never `confirm()`.
- 2026-06-13 · WKWebView forces macOS autocorrect/capitalize on every field with no global switch → `app/noAutocorrect.ts` stamps it off per-field + a MutationObserver for new fields (opt-out `data-allow-autocorrect`).
- 2026-06-14 · SSH tunnel = system `ssh -N -L` subprocess, one shared per profile (`AppState.tunnels`), `BatchMode=yes`, honours `~/.ssh/config`; routes connect through the local port. `kill_on_drop` reaps it.

### v0.2
- 2026-06-14 · Theme engine (`design/theme.ts`): a palette is *seeds*, expanded to the full CSS-var token set as inline vars at startup (no flash). Two kinds: **hue** (curated: accent+hue+tint → tinted neutral ramp) and **anchors** (custom: bg/fg/primary/secondary → surfaces by sRGB mix). `--accent-fg` is auto-contrast. Custom themes are authored once; the opposite light/dark variant is synthesised so the mode toggle flips them too.
- 2026-06-14 · Floating-card shell on a vibrancy gutter: transparent window + `window-vibrancy` material shows through `--gutter` between `.card` panels (rail / sidebar / main / inspector). Replaced the old flush `.app-shell/.sidebar/.main-area`.
- 2026-06-14 · Per-tab results+edits keyed `byTab` with the active tab mirrored to top-level store fields → every consumer reads unchanged and a background tab's stream can't corrupt the visible tab. `committing`/`preview` stay global (one commit at a time).
- 2026-06-14 · Connection liveness: `connect` takes an `on_close` callback the driver task fires on socket death → `session-closed` event → frontend flips the dot + drops dead sessions; `ensureTabSession` auto-reconnects on next use.
- 2026-06-15 · Editing a saved connection invalidates its live state when connection-affecting fields change (`connSig`): close the profile's sessions + drop its cached tunnel so the next connect uses the new values. The tunnel also carries a `spec` (host+ssh params) and `ensure_tunnel` rebuilds on mismatch; a repointed host can't keep forwarding to the old one. Cosmetic edits leave the live connection alone.
- 2026-06-15 · Inspector commit/preview re-resolve a live session via `ensureTabSession` instead of the result's `executedSessionId` (which can be dead after a repoint/drop/dev-rebuild); edits are pk-based so they apply on a fresh connection.
- 2026-06-15 · Colorized JSON view/edit uses a CodeMirror `JsonField`, never a transparent-textarea-over-`<pre>` overlay; WKWebView's opaque native field background always covers the colored layer.
- 2026-06-15 · App icon built from the transparent SVG via `scripts/make_icon.py` → `icon-master.png` composited onto Apple's grid (824² rounded squircle in 1024, charcoal gradient, soft shadow); all sizes regenerated by `tauri icon`.

### v0.2.5
- 2026-06-17 · Tabs unified: `Tab` gains `kind: "query"|"table"` + `table` ref; opening a table creates a **session-only** table tab (persist only query tabs, stripped to the appdb fields) instead of a full-panel swap. App renders editor vs `TableBrowser` by the active tab's kind; tab strip shows a kind icon.
- 2026-06-17 · Browser state went **per-tab**: `useBrowser` keyed `byTab` mirrored from the active tab (results/edits pattern); the table ref lives on the `Tab`, so every table tab keeps its own filters/sort/scroll/draft. Opening an already-open table focuses it (no dupes).
- 2026-06-17 · Column-header data-type icons come from the **editability map's `type_name`** (already fetched eagerly per result); the simple-protocol `ColumnMeta` carries no type, so no Rust change. `grid/typeIcon.tsx` maps type families → lucide glyph + `--syn-*` color; reuses `isArrayType`.
- 2026-06-17 · Inline add-row replaces the `InsertPanel` overlay: a sticky draft band pinned under the grid header (outside the virtualizer; cells offset `+draftH`), per-tab `draftRow` in the browser store; `⌘↵` insert / `Esc` cancel / `∅` NULL. Add-row button moved into the bottom toolbar (far left; Filter+Sort right).
- 2026-06-17 · Close-with-unsaved-edits guard: `closeGuard` store routes every close (tab X / ⌘W / browser header X) → prompts when `useEdits.byTab[id].pending` is non-empty. `Esc` keep · `Enter` discard&close · `⌘↵` commit&close (selects the tab first so the commit targets it; keeps the tab open if commit fails).
- 2026-06-17 · Connect errors surfaced: TLS→plain fallback no longer masks **server-sent** errors (`e.as_db_error()`); a bad password / pg_hba now reports its real reason instead of a misleading "no encryption". Global `ConnToast` shows connect failures on any view (dashboard included) with an Edit shortcut.
- 2026-06-17 · Tab strip auto-scrolls the active tab into view via `scrollIntoView({ inline: "nearest" })`; minimal slide, no jitter when already visible; reveals the `+` for the last tab. Native horizontal scrollbar hidden.

### v0.3 → v0.5 (releasable push)
- 2026-06-26 · The v0.3→v0.5 plan lives in `docs/ROADMAP_v0.5.md` (active plan doc); main `ROADMAP.md` keeps the v0.1–v0.2.5 history + session log + a pointer banner. Drafted from a 16-workstream audit + competitor-research workflow (8 subsystem code-audits + TablePlus/Postico/Beekeeper/DataGrip/DBeaver/community inventories → gap matrix → completeness critic → roadmap).
- 2026-06-26 · v0.3 builds a **shared overlay primitive** FIRST (one z-index / Esc / outside-click / viewport-clamp layer for every popup), then a nested `ContextMenu`/`Submenu` on top: root-fix for the off-screen CellEditor + the no-Esc ThemePicker + the 3 hand-rolled flat menus, and the substrate for the right-click-everywhere epic. Don't hand-roll another popup.
- 2026-06-26 · Find = one mental model: ⌘F scoped to the focused surface (editor via `@codemirror/search`; results over the in-memory rows with an honest "N of M loaded / capped" indicator; JsonTree already has ⌘F). Esc closes, ⏎/⇧⏎ next/prev, match count shown; identical chrome everywhere.
- 2026-06-26 · Perf is gated by **measurement, not feel**: v0.4 records before/after numbers for cold-start / keystroke→completion / 50k scroll / drag-select against the stated budgets in the session note; a milestone Gate isn't green until its perf slice is measured.

### Mission upgrade (2026-07-02)
- 2026-07-02 · Goal changed from "releasable v0.5" (endpoint) to **surpass every mainstream Postgres client**: no finish line; v0.5 stays a waypoint/version cap for the current push. Two invariants outrank all feature work: (1) never feels slow at any data scale, (2) never lies about or corrupts data. Feature count only grows.
- 2026-07-02 · `docs/GAPS.md` is the ranked gap census (from a 13-workstream code+competitor audit; raw reports in `docs/audit-2026-07/`). Roadmap milestones draw from it: correctness ledger (GAPS §1) before features; the correctness keystone is **statement-at-a-time execution** in the driver (replaces whole-buffer `simple_query`); prerequisite for matched≠1 rollback, honest multi-statement results, tx-state tracking.
- 2026-07-02 · Promotions locked: run-statement-under-cursor and selection-stats-in-status-bar are P0 (v0.4); v0.35 = Correctness Milestone. Leapfrog features (GAPS §4: run-CTE-standalone, reverse-FK lookup, inverse-SQL undo, impact estimates…) are the beyond-v0.5 identity; build those before parity-polish.
- 2026-07-02 · **Multi-statement runs = psql autocommit semantics** (statement-at-a-time `simple_query`, stop on first error, earlier statements stay committed), NOT an atomic implicit transaction (Postico's model). Rationale: the whole-buffer form made the UI lie (reported-committed work silently rolled back by a later error) and broke VACUUM/CREATE INDEX CONCURRENTLY. Explicit BEGIN/COMMIT still span statements on the tab's session. A "run script atomically" toggle can layer on later. Error positions are rebased to whole-buffer CHAR offsets in the driver (`StmtSpan.char_offset`).
- 2026-07-02 · **Session timeouts: statement_timeout=5min + idle_in_transaction=10min at connect.** Deviates from locked decision #6 (30s): without a Settings escape hatch, 30s would kill legitimate analytics queries on big staging tables daily. 5min is the interim never-hangs-forever net (keepalives 30s/10s×3 handle the dead-tunnel case in ~1min); revisit the number when Settings (⌘,) lands.
- 2026-07-02 · **ROW_CAP auto-cancel is best-effort by design**: the driver cancels its own drain when the cap is crossed on the last statement, but a fast query legitimately wins the race and completes as a normal capped drain. Both paths return Ok + capped; a 57014 is only swallowed when WE fired the cancel (user ⌘. still errors through).
- **Prod safe-mode is server-side, per-session** (2026-07-03): `is_prod` → `default_transaction_read_only=on` in connect options (every session incl. spares), unlock = `SET … = off` on ONE tab's session behind a confirm. Chosen over client-side SQL sniffing: the server can't be fooled by CTEs/DO blocks/functions. Caveat: PG read-only still permits writes to existing TEMP tables; acceptable (no persistent data at risk).
- **Statement timeout is an app-level setting applied at connect** (2026-07-03): stored in useSettings (300s default, 0 = none), passed per `ipc.connect`; existing sessions keep their timeout until rebuilt. Per-profile override deferred until someone needs it.
- **⌘⇧F = format in the editor, filter-focus elsewhere** (2026-07-03): CodeMirror keymap preventDefaults first, window listener bails on defaultPrevented; one chord, two context-correct meanings.
- **Active tab restore via localStorage, not appdb** (2026-07-03): UI nicety, not data: losing it is harmless, so no migration/schema cost.
- **Per-connection workspaces** (2026-07-06): tabs carry `profile_id`; visibility = own ∪ pinned ∪ legacy-null. Pins are the cross-connection mechanism (origin-colored badge). Legacy tabs adopt-on-edit rather than bulk-migrate (no wrong guesses, no disappearing tabs). Unpin with a dead/foreign origin adopts the current connection. DB-switcher clones get fresh workspaces (different profile id); deliberate: different database, different tabs.
- **Commits are ONE batched simple-query message** (2026-07-06): `BEGIN;U₁;…;Uₙ` in one round trip, per-statement RETURNING counts verified from the single result stream, COMMIT/ROLLBACK as trip two; 2 RTTs for any N. Result-set accounting must be exact (n+1 sets) or the batch is refused+rolled back; any error mid-batch rolls back everything. Verify-then-commit semantics unchanged, only the transport shape.
- **Edit mappings are frontend-fed, trusted-but-verified** (2026-07-06): preview/apply/delete accept the EditabilityMap+snapshot-names the frontend already holds (zero catalog trips; preview = zero server contact), and `editability` accepts snapshot identity hints (1 RTT). Safety: incomplete hint → silent server derivation; snapshot table must match the map's server-derived dotted name (cross-DB oid collision guard); stale hints can only end in rollback (SQL error or matched≠1). SQLSTATE class-42 → refetch map; preview retries once silently, apply NEVER auto-retries; it regenerates the preview with a "schema changed" notice. Commit replays the exact stashed preview inputs (byte-for-byte guarantee).
- **Schema snapshots persist per profile (stale-while-revalidate)** (2026-07-06): appdb `schema_cache` keyed profile_id + `connSig`; hydrate at connect START for t=0 sidebar/completion, real introspect refreshes behind it. Ordering rule: hydrate never overwrites anything, server fetch always wins; no timestamps needed. Raw-JSON introspect passthrough rejected (escaping bloat + loses Rust-side validation).
- **SSH tunnels keyed and shared by SPEC, not profile** (2026-07-06): DB-switcher clones ride one ssh process. `profile_specs` tracks bindings; invalidate/delete unbinds the profile and drops the tunnel only when no other profile shares the spec (a shared tunnel drop would kill sibling sessions). A repointed profile computes a new spec and naturally gets a fresh tunnel.

### v0.7.0-bedrock
- 2026-07-18 · appdb schema versioned via `PRAGMA user_version` + numbered migrations, one transaction each, stamped on commit (replaces the `let _ = ALTER` swallow-all). v1 = pre-versioning baseline: CREATE IF NOT EXISTS + column-checked ALTERs, so a legacy unversioned DB is stamped in place with data intact and real errors propagate. List reads (profiles/tabs/saved/history) skip-and-log corrupt rows: one bad row costs one row, never the whole list. History rows carry status ok/error/cancelled (failed + ⌘.-cancelled runs are recorded from the frontend catch path, 57014 = cancelled), per-row SQL capped at 20k chars with a marker, table pruned to newest 20k rows on insert. `tauri-plugin-single-instance` registered before every other plugin; a second launch focuses the existing window instead of racing the first for the shared appdb.
- 2026-07-18 · **Tx state is driver-lexed** (tokio-postgres discards ReadyForQuery's status byte at every site; checked 0.7.17 source): splitter statement-heads (BEGIN/START, COMMIT/END, ROLLBACK/ABORT, ROLLBACK TO un-fails, AND CHAIN stays in-tx, PREPARE TRANSACTION → idle) + error outcomes fold into idle/in-tx/failed-tx per session, surfaced as a dedicated `tx-state` event (not a QueryEvent). Known limit: `CALL proc()` with internal COMMIT drifts the tracker, but only in the fail-safe direction (worst case a SAVEPOINT errors; nothing can be silently committed). Ride-along: ROW_CAP auto-cancel is disabled inside an open tx (our own 57014 would abort the user's transaction).
- 2026-07-18 · **Edits inside a user transaction = SAVEPOINT/RELEASE** (`ROLLBACK TO`+`RELEASE` on verify failure); the outer tx is NEVER committed or rolled back by an edit. `committed:true` there means "applied into the open tx"; durability is the user's COMMIT (tx chip stays lit). Batch wrapper contributes exactly one result set in both modes, keeping n+1 stream accounting.
- 2026-07-18 · **ctid guards use `= 'v'::cast` / `IS NULL`, not IS NOT DISTINCT FROM**: semantically identical for known old values, and stays indexable; json/xml/geometric types (no `=` operator) compare via `::text`. Guard columns: every untruncated same-table result column, deduped by attnum. Same guard grammar on edit and delete paths.
- 2026-07-18 · **Table identity is `{schema, name}` end-to-end** (`table_refs` in EditabilityMap/EditMapHint, schema+name on TableIdentityHint); the dotted `tables` string survives as display-only. Casts are captured pre-qualified at editability time (`pg_catalog` bare, else quoted+qualified; `"char"` force-quoted: bare `::char` silently means bpchar(1)).
- 2026-07-18 · **Cancel escalates out-of-band**: CancelToken (1.5s deadline) → busy-poll ≤1.2s → `pg_cancel_backend` over a fresh connection built from the stored Config (5s deadline); `pg_terminate_backend` is a separate user-confirmed command on the force-disconnect hatch, never automatic. Internal ROW_CAP cancel uses the raw token (escalation would self-deadlock on the busy poll).
- 2026-07-18 · **Overlay z-index derives from escStack position** (BASE 50 + depth via `pushOverlay`); stacking order can't lie about key routing. escStack ignores Esc while `e.isComposing`.
- 2026-07-18 · **Draft-row grammar**: untouched = DEFAULT, touched-empty = `''`, ∅ = NULL; touched-state per cell. Revised same day after audit: an EMPTY field in a multi-cell TSV spread-paste stays untouched (DEFAULT, the spreadsheet convention; `''` into numeric/date columns was hostile); a deliberate `''` remains reachable per cell (type + clear, or single-cell paste).
- 2026-07-18 · **Hint-fed plans carry attname identity guards** (audit fix): every hint-named column used in a hinted statement (SET + locator + guard) gets `(SELECT attname FROM pg_attribute WHERE attrelid=… AND attnum=…) = 'name'` ANDed into the WHERE; an external RENAME+ADD between snapshot and ⌘S now rolls back honestly (matched 0) instead of writing the wrong column. Derived (non-hint) plans carry none; still 2 RTTs.
- 2026-07-18 · **Savepoint hygiene** (audit fix): per-batch unique savepoint names (`qwry_edit_sp_{n}`) so our cleanup can never roll back a user's identically-named savepoint; batches REFUSE to start in a failed tx ("current transaction is aborted; ROLLBACK first"). Failed COMMIT/END (deferred constraints) folds Idle, matching the server.
- 2026-07-19 · **Sort is a chain** (spelunk): `setSortChain(SortKey[])` ({column, dir, nulls?}) is the browse sort API; per-key NULLS FIRST/LAST are keyset ladder VARIANTS (default placements emit byte-identical SQL to before, proven); tiebreaker inherits the FIRST key's direction, PK-in-chain absorbed. Supersedes "no NULLS clauses" phrasing in the 07-18 keyset entry. Editor results: stable client-side multi-sort in the existing view-map slot.
- 2026-07-19 · **Raw-WHERE mode owns the WHERE entirely** (spelunk): builder rows can never filter behind the user's back while raw mode is on; one `compiledWhere` feeds pages, count, and histograms. Jump-to-row = honest OFFSET page-1, then scrolling re-anchors keyset from the landed page (proven gapless).
- 2026-07-19 · Sidebar pins persist in localStorage per profile as {schema,name} pairs (UI nicety, active-tab-restore precedent). Introspect v2 requires PG 10+ (pg_sequence join); pre-10 errors the whole batch; ledgered, PG 9.6 is EOL.
- 2026-07-19 · **ctid keyset is triple-gated** (tempo audit): ordinary relkind only, `relhassubclass=false` (inheritance children have colliding ctids: TimescaleDB hypertables), AND reltuples estimate ≤1M (TID scans have no pathkeys; ORDER BY ctid is a seq scan + top-N sort PER PAGE); missing metadata fails safe to offset fallback. Page-1 keys are PINNED on the tab: key drift (snapshot refresh) or a committed edit to a key column forces a full re-run, never a spliced append. Inherent residual (ledgered): concurrent writers move ctids between pages.
- 2026-07-18 · **Browse pagination is true keyset** (tempo): PK/ctid tiebreaker inherits the sort direction (uniform ORDER BY, page 1 included; total order requires it); row-value `(a,b)>(v…)` fast path only when uniform-dir AND catalog NOT NULL; otherwise OR-ladder with explicit NULL partitions; sort keys always treated nullable (stale snapshots); seek values are wire text + `::format_type` casts. Offset fallback ONLY for non-keysettable relations (views, partitioned-no-PK, ctid on PG<14, truncated seek cells), never wrong-rows keyset. loadMore appends one page on the tab session and skips history.
- 2026-07-18 · **pg_catalog functions cached in appdb keyed by server_version** (tempo): introspect stays one round trip: a count(*) probe rides the batch; count drift (extension into pg_catalog) or corrupt JSON = miss → refetch+re-persist. User-schema functions always live.
- 2026-07-18 · **Editor statement spans are incremental** (tempo): prefix reuse + 64KB growing re-lex window + suffix re-adopt on exact shifted restart; the FINAL span is always re-lexed (a trailing `;` can sit inside an unterminated comment; differential testing caught this). Completion context is capped at a 200KB window around the cursor with byte-identical output under the cap; sql-sync deliberately keeps one synchronous toString() per keystroke (0.016ms JSC rope) because ⌘S/close/quit read the store synchronously.
- 2026-07-18 · `history_search` LIKE-scans only the newest 5k rows (constant cost; FTS is the lift if older-match search is ever wanted).
- 2026-07-18 · `ConnState` gains `"error"` (failed connect ≠ never-tried gray); every existing consumer compares `=== "connected"` so it behaves as not-connected everywhere else.

### v0.8.1-timeline
- 2026-07-19 · **Undo OLD-capture is in-statement, zero extra RTTs**: `UPDATE t AS __t SET … FROM (SELECT <changed> FROM t WHERE <full-where> FOR UPDATE) __old WHERE <full-where on __t> RETURNING __old.*, __t.*, __t.<locator>`; the outer WHERE repeats the predicates (indexable, matched-count byte-equivalent; FOR UPDATE locks before capture so __old is the exact pre-image). PG 14–17 grammar; PG 18 OLD/NEW RETURNING unused. DELETE captures via RETURNING * + one catalog probe riding the batch (DDL blocked by the DELETEs' own lock).
- 2026-07-19 · **The undo artifact is a structured revert PLAN, not SQL text**: SQL regenerated by the same generator at undo time (nothing parsed); an applied undo mints its own inverse, so redo emerges with zero special-case code. Reverts: PK locators (post-commit values), NEW ctid + server-normalized value guards on ctid tables, attname identity guards always; GENERATED/identity-ALWAYS excluded from re-INSERTs (no OVERRIDING SYSTEM VALUE: honesty over magic; an undone delete of an identity-ALWAYS row gets a fresh id, ledgered).
- 2026-07-19 · **Undo re-enters run_verified_batch**: stale undo = matched≠1 = full rollback + "undo no longer matches". Offer is session-stamped (server re-checked), single-shot (consumed whether it commits or not, NEVER auto-retried), TTL 15min, invalidated on DDL/newer-commit/session-death; prod read-only refuses at the server (tested).
- 2026-07-19 · **Buffer time-machine views are read-only throwaway EditorStates**: the live draft is parked and every store-sync path is hard-gated while viewing, so a snapshot can never leak into tab.sql; restore = one undoable transaction. Snapshots ride appdb (v5) keyed per tab, deduped, capped 50.
- 2026-07-19 · .sql file links are session-only Tab fields (path + saved-text); persisting them needs a tabs schema column; deferred. Drag-drop uses Tauri window events (WKWebView never delivers HTML5 file drops with dragDropEnabled).
- 2026-07-19 · **CSV import: validate = a full rehearsal that ALWAYS rolls back; commit = ONE all-or-nothing tx** (timeline): both run identical batched 500-row multi-row INSERTs with `'v'::type` casts (cast names from live pg_attribute/pg_type, edit.rs cast discipline). Validate re-runs a failed batch row-by-row under savepoints to name exact bad rows (cap 50 + honest "more"); commit failure attributes the row via PG's char-position→tuple-span map, else names the batch range. `flexible(false)`: short/long rows are per-row errors, never padded. Header sniff rule: row 1 is a header ONLY on a type mismatch vs the data rows (all-match ⇒ headerless; never eat a data row); UTF-8 only, per-row errors otherwise. Fatal codes (25006 read-only prod, 25P02, 57014, 08*) abort instead of repeating.
- 2026-07-19 · import.rs carries byte-identical private copies of edit.rs `ql`/`qi`/cast rules with parity unit tests; edit.rs was concurrently owned in this wave so the helpers couldn't be made pub(crate); fold them into one shared module next quiet pass.
- 2026-07-19 · History panel: grouping is CLIENT-side over the unchanged history_search API: consecutive same-normalized-SQL+same-profile runs collapse to ×N (expand →/←, badge tints when a collapsed group hides non-ok runs); day dividers Today/Yesterday/date; per-connection chip pushes profile_id into the query, status chips filter client-side; absolute timestamp on hover, relative stays primary.

### v0.9.0-flightcheck
- 2026-07-19 · **Destructive-confirm grammar is ⌘⌫, Enter = safe** (Postico convention): CloseGuard/DangerModal map plain Enter to Keep/Cancel and the destructive confirm to ⌘⌫ (rendered on the button); ⌘↵ stays commit-&-close on CloseGuard. EditPreview's Enter=Commit is DELIBERATE (primary action of an explicitly opened review, not a destructive surprise; documented in-file). DangerModal's old ⌘↵ confirm is gone.
- 2026-07-19 · **Overlay focus discipline lives in the primitive**: escStack captures the opener per layer (at render, before autoFocus steals focus) and restores on pop via microtask; skipped when a newer overlay opened (owns focus) or the close deliberately focused something (activeElement ≠ body); fallback = main editor → .main-card (tabIndex -1), never <body>. Modal traps Tab/⇧Tab topmost-only + role=dialog/aria-modal/aria-label from every consumer, and pulls focus off the background at open (typing can't leak into the editor behind a dialog). Menus/popovers stay focusless by design (roles only).
- 2026-07-19 · **UI zoom is chrome-level** (⌘= / ⌘− / ⌘0, 70–150%, persisted): the px --text-* tokens are re-emitted as inline overrides scaled by uiZoom (bases read once from the loaded stylesheet, so tokens.css stays authoritative) + root font-size % + a --zoom factor for opt-in CSS. Editor (--editor-fs) and grid (--grid-fs + row geometry) are EXCLUDED: they have their own size settings and the grid's virtualizer/hit-test math is px-anchored. ⌘0 moved from jump-to-tab-10 to zoom reset (tabs are ⌘1…9); menu accelerator is Cmd+= (muda has no "Plus" key).

### v0.9.1-firstlight
- 2026-07-20 · **Hidden-until-painted launch**: the window is created `visible: false` and revealed by the frontend after startup data (profiles+tabs) settles + double-rAF post-paint; never an empty glass pane. window-state plugin runs with `StateFlags::all() - VISIBLE` (restoring visibility would defeat the hidden start); a Rust 5s fallback thread shows the window if the webview never boots. Corollary: any future "show earlier" change must keep all three pieces in sync.
- 2026-07-20 · **Store wiring pins the entry chunk**: stores with module-level cross-store `subscribe()` (edits, browser) are side-effect-imported in main.tsx. Lazy-chunking them starts their wiring late and desyncs active-tab state; same failure class as the HMR stale-store gotcha. Shell reads editor registries via `editor/editorBus.ts`, never from SqlEditor (which would drag CodeMirror into the entry chunk).
- 2026-07-20 · **One-line inputs never hold newlines**: draft cells with multiline text render a read-only ⏎ preview and edit through ValuePop (WebKit silently strips \n from input.value → state desync). Draft paste inserts at the CARET; merged-valid-JSON compacts; everything else goes to the pop-out; a paste never replaces cell state wholesale. ValuePop `startDirty` exists because its no-change close would otherwise swallow paste-opened content.
- 2026-07-20 · **Draft JSON compaction is display-only and guarded**: parse validates, but compaction (json/jsonb columns ONLY, never text-like) is skipped when the text trips LOSSY_NUMS: >2^53 ints don't survive a JS parse/stringify round-trip; the exact typed bytes stage instead (⏎ preview absorbs the multiline). Audit caught both draft paths skipping the guard the inspector already enforces. Companion: `startup_ok` IPC. The refused-appdb path manages a fatal stub state and the frontend reveal checks it, so the hidden window never pops up empty beside the "qwry can't start" dialog.

### v0.9.2-lookingglass
- 2026-07-20 · **Translucent tokens never carry sticky-overlay duty alone**: `--accent-soft` (and any alpha token) must be layered over an opaque base (`linear-gradient(tint, tint), var(--bg-*)`) when the element occludes scrolling content (the draft-corner gutter bug). Layout probes (elementFromPoint/rects) cannot see this class; repro harnesses need pixel evidence.
- 2026-07-20 · **Programmatic focus in the grid always uses `preventScroll: true`** and scroll authority belongs to `scrollToIndex` alone; WKWebView's focus-reveal scrolls to the element's FLOW (unstuck) position and had yanked the scroller to origin via draft-cell `autoFocus` on remount. Focus-once refs replace autoFocus in virtualized bands; remount-refocus only when `document.activeElement === document.body`.
- 2026-07-20 · **Virtualizers under sticky chrome get `scrollPaddingEnd`** (= the chrome's px) or end-aligned scrolls park the target under/behind the chrome; TanStack v3 re-reads options per render so a variable band height is safe.
- 2026-07-20 · **Grid z ladder is documented order, not ad-hoc**: celledit 5 < rownums 6 < header 7 < draft band 8 < corner/dropline 9 < draft-error 10; overlays 20/30; toast 45; escStack 50–59; prod strip 60. New grid chrome slots into this ladder explicitly.
- 2026-07-20 · **Store actions capture their tab at entry, never after an await**: commitDraft/insertRow now thread the captured tabId and skip re-runs when the active tab changed (loadMore's rule, made universal). Post-await `get().active` is the recurring wrong-tab-write bug class.
- 2026-07-20 · **All clipboard writes route through `lib/copyCue.ts`** (toast cue, failure-visible). Bare writeText in UI code is a defect; the cue toast lives at z 45 (below overlays: feedback, never interaction).
- 2026-07-20 · **⌘⇧F = Format SQL everywhere** (editor keymap + global fallback via editorBus); schema-filter focus is ⌥⌘F matched by `e.code === "KeyF"` (⌥F produces "ƒ"; e.key comparisons break under ⌥).
- 2026-07-20 · **TSV paste parses the quoting convention back** (`parseTsv` in clipboard.ts, exact inverse of the writer, null on malformed → naive-split fallback): both paste surfaces (grid ⌘V, draft band) were naive-splitting, so a copied json cell landed as `"[""x""]"` and a quoted newline split one cell into two rows. Boundary: single-line pastes (no tab/newline) NEVER unquote: a pasted JSON string literal must stay verbatim; unquoting only where TSV structure is unambiguous.
- 2026-07-21 · **Draft-row value grammar: empty input = DEFAULT, always.** DraftCell {text, state?: "null"|"empty"}; the old touched-but-empty='' convention committed '' when a pasted PK was ⌫-cleared. Explicit NULL/'' are deliberate acts (per-cell menu / ⌥⌫-on-empty; with text present ⌥⌫ stays macOS word-delete), rendered as the grid's own chips; ∅ button retired. Placeholders self-document from the schema snapshot (default expr / "identity" / "required" / NULL); identity-ALWAYS + generated render "auto" and are never staged (dup-row skips them too). The required pre-check resolves columns from the LIVE snapshot first; refusing a legal write off a tab-frozen TableInfo is the "app lies" failure mode. "Set empty" is anchored to scalar text-family types ('' into text[] = malformed array literal).

### v0.9.5-truename
- 2026-07-21 · **Workspace identity rules** (from the cross-conn table-open bug): (1) any global tab lookup that ends in select() MUST scope to the visible workspace (own ∪ legacy-null ∪ pinned); unscoped matches select foreign tabs whose cached rows render under the wrong connection's branding; select() itself now refuses invisible ids (dev-warn). (2) **Never write to A and repaint from B**: post-write reloads (insert, import, delete, undo) re-run through the profile that executed the write via run()'s new {profileId} override; every other run keeps v0.6 rail semantics ("pinned tabs run against the ACTIVE conn"). (3) The chrome must speak for the ROWS, not just the rail: origin provenance banner when executedProfileId ≠ rail; prod strip + write-lock chip key on the origin (a prod-pinned tab under a staging rail was rendering prod rows + committing prod writes with zero prod ceremony); EditPreview names its commit target; tx surfaces scan ALL of a tab's sessions (skey suffix), rollback targets the session holding the tx.
- 2026-07-22 · **One row-view concept, one gesture**: RowPeek deleted; Space, ⇧Space, and `Open as Record` all open Record View (strict superset: in-place editing, read-only reasons, truncated→inspector, 2-row diff). Space also CLOSES it (Quick Look grammar). Rationale: two near-identical modals needed every guard fix twice (3 documented incidents). Inspector narrow mode (< 280px store width) hides shortcut hints via a store-derived class, NOT a container query; `container-type` would become the containing block for the fixed `.insp-copy-backdrop` and shrink click-away to the panel.
- 2026-07-22 · **docs/DESIGN.md is the third law file** (pixels; WRITING=words, LESSONS=bugs), born from the founding audit: ~80 hand-authored control rule-sets / 9 species, pressed state on 2, 163 off-grid literals, 8 icon sizes, 5 motion dialects. Enforcement is structural: `<Kbd>` makes wrong glyph forms untypeable (order ⌃⌥⇧⌘, ↩ not ↵, UI font not mono); base classes (.btnish variants, .iconbtn, .linkish) make drifted controls unwritable; `scripts/design-lint.ts` gates the rest (census 597 at birth; flips to --enforce when migration lands). Em dash banned from UI strings (WRITING.md rule 7 amended: split/comma/·; docs keep it). --dur-quick corrected 150→120ms to match the de-facto standard it was competing with.
- 2026-07-22 · **Cancel gets its own transport**: every SSH tunnel pre-spawns a second ssh child (control lane, `ControlMaster=no`): Postgres cancel NEEDS a new connection, and over one multiplexed ssh TCP that connection's handshake queues behind the very row-flood being cancelled (head-of-line blocking). Control paths (token cancel via cancel_query_raw, OOB, terminate) try the lane on sub-deadlines and fall back to the data lane; sessions can hard-abort their own connection (single-fire notifier) so disconnect/terminate/failed-cap-cancel never drain a dead result to completion. Cancel feedback is set synchronously in the UI and cleared only when the run settles; the wire's truth arrives late, the button's truth must not.
- 2026-07-23 · **Match Connection theming**: a Theme Picker card (radio-exclusive with palettes: choosing either clears the other, incl. saving a custom theme) derives the whole theme from the ACTIVE connection's color over a pure-neutral ramp (tint 0); the seed is hex-validated (the sanitize moat) and its hue is STEERED out of the danger band (±15° around red → nearest edge 335/25); Run must never wear Delete's color. connAccent is runtime-only, pushed one-way from the connections store; disconnected falls back to the chosen palette. Colorless profiles hash their id into the avatar rotation (stable for the profile's lifetime); index-derived slots let deleting an unrelated profile retint the app.
- 2026-07-23 · **Stable chrome, refined twice**: strips get fixed heights and reveals occupy layout (visibility, not display); WITHIN-mode swaps must never resize, BETWEEN-mode transitions may reflow; and when a control should hold constant across modes, EQUALIZE its faces by copy first (Cancelling/Connecting dropped the progress …; width-over-register, scoped) then reserve via a stacked grid slot with centered labels. The qb action slot walked stretch→right-align→conditional before landing here; the detour is the documentation.
- 2026-07-24 · **Per-connection themes**: sparse `connThemes: {profileId → {paletteId, match}}`; an absent entry FOLLOWS the app theme, so turning the mode on retints nothing until a pick lands. Picks route by scope (active connection when "One Theme Everywhere" is off, app theme otherwise) and the picker's radio derives from the same resolution, so display scope ≡ write scope by construction. Toggle ON promotes the on-screen choice to the app theme and leaves the map dormant (toggle OFF restores every assignment). Fixed label + switch, never state-swapped text (HIG: the label names the setting, the switch position IS the state; and stable-chrome). Owner dots (absolute overlay) are the pick-landed-here feedback. Stale palette ids degrade to the default palette at rehydrate; same outcome as deleting the theme in-app.
- 2026-07-24 · **Switch species**: `<Switch>`/`.switch` (tokens.css), a macOS-style toggle for feature/setting rows; a hidden native checkbox is the truth (role=switch, Space, label association) and every visual state derives from it via `:has()`; no class mirroring to desync. Spring knob on --ease-spring with the macOS press-stretch (width grows held, springs on release). Checkboxes remain for selection within content (browse filter enable, CM search panels) per HIG.
- 2026-07-24 · **Auto-update over GitHub Releases**: tauri-plugin-updater + minisign (private key and its password live outside the repo, on the release machine and in GitHub Actions secrets; only the pubkey is baked into tauri.conf.json); endpoint = releases/latest/download/latest.json (requires PUBLIC repo); release ritual = bump conf version → tag vX.Y.Z → push (workflow refuses a tag/conf mismatch; a forgotten bump publishes a feed advertising the old version, silently dead). A relaunch is a process death: the updater runs the Quit ceremony (prepareExit in stores/exitGuard.ts, shared verbatim with ⌘Q) before the download and again before the relaunch; staged edits/draft rows/open transactions are never silently lost. Stall watchdog (90s eventless → error) + generation counter so an orphaned download can never surprise-relaunch; installed-but-not-relaunched is its own honest state.
- 2026-08-20 · **Rebuilt-session consent is inline, tiered, single-surface**: the commit preview never stacks a second dialog. A rebuilt session shows an accented chip on the preview itself ("Connection was rebuilt · commits against current state", hover for the full story); it escalates to warn tier + a "Commit Anyway" button ONLY when the dead session held an open transaction (tombstoned by session id in `deadTxSessions` — ids are never reused, so FIFO cap instead of clearing logic). Enter still commits in both tiers: the preview IS the review surface and the row-verified pipeline is the actual safety; the old confirmDanger-on-top mapped Enter to Cancel and ate the commit mid-keystroke. Commit-time rebuilds (between ⌘S and Enter) restamp silently for the same reason.
- 2026-08-21 · **One inner scroller per value, and the pencil is a control**: a structured value in the record view gets exactly ONE scroll surface between the page and the content — the JSON box itself (`.rv-val:has(> .jsonfield)` un-caps the shell; a capped shell around a capped box made every long JSON a two-flick read). The edit pencil is a clickable `.iconbtn`, not a decoration: inline after plain values (negative vertical margins keep the 22px box from growing the row), `.bordered` chip anchored inside the box's bottom-right corner for JSON (on the non-scrolling shell, so it holds still). Editable cells carry NO native title — the header hint + pencil are the affordance; a tooltip trailing the cursor across a scrolling box is noise. Read-only reasons keep their titles.
- 2026-08-21 · **Refresh feedback is honest choreography, not decoration**: ⇧⌘R animates the sidebar db glyph as "the rebuild" — the disc stack splits apart when the heal starts, holds apart while it runs, claps home on the verdict, and the top→bottom accent shine plays ONLY on success (failure settles shine-less; the dot tells the red story). Background heals skip the split and shine only when a dead session was actually found and torn down for rebuild (`healProfile` returns `{ok, rebuilt}`; tab replacements ride the fresh spare lazily, the primary being green is the claim); a no-op probe stays invisible, so the shine stays trustworthy. Timing lives in one place (stores/refreshFx.ts: min-apart so fast verdicts still read, shine lags the clap, generation guard); springs via presets only. AUTO_HEAL_SHINE is a trial flag.

### Agent A1 · W0 (research, 2026-09-05; reports in ~/projects/qwry-agent-lab/docs/research/w0-*.md)
- 2026-09-05 · **AST gate = `pg_query` 6.2.0** (libpg_query 17 grammar), not `sqlparser`: parses the grammar the server runs (no false rejects) and exposes `into_clause` / `locking_clause` / CTE bodies. Measured cost: cold 53s release / 23s debug (bindgen + libclang, auto-found from the Command Line Tools and on GitHub's macos-latest = macOS 26 image), 0.1s no-op check, 0.54ms per 2KB parse. CI caches `target/` on `Cargo.lock`.
- 2026-09-05 · **Gate policy**: allow SELECT / WITH…SELECT / EXPLAIN [ANALYZE] of an allowed SELECT / VALUES / TABLE; refuse data-modifying CTEs, SELECT INTO / CTAS, FOR UPDATE/SHARE (refuse, never strip: the trace must show exactly what ran, §8.4), COPY, SHOW/SET (spec-literal; widening SHOW is an open question), multi-statement input (run_sql = exactly one statement) and unparseable input (parse error text feeds the repair loop). A function deny-list is walked over every FuncCall (pg_terminate/cancel_backend, pg_sleep*, set_config, lo_*, pg_read_file, pg_ls_*, dblink*): measured that `default_transaction_read_only` does NOT stop these. `locking_clause` / `into_clause` are NOT visited by pg_query's `.nodes()` walker: check them on every SelectStmt reached via CTE, set-op branch, FROM subquery, JOIN AND SubLink (`SELECT (SELECT … FOR UPDATE)` slipped the first draft, measured). Measured: read-only also blocks EXPLAIN ANALYZE of a write before any row is touched; bare EXPLAIN of a write returns a plan only, so it is refused as out of scope, not as a write.
- 2026-09-05 · **`claude -p` transport = streamable-HTTP MCP server inside the Rust process** (`rmcp` 3.2.0 `server` + `transport-streamable-http-server` on bare hyper/hyper-util through `TowerToHyperService`, no axum; spike compiled and served a live initialize), one server per app instance on 127.0.0.1:0, one bearer token per agent thread mapped to that thread's dedicated session; not a stdio sidecar (§11 q1 closed). The bearer header is delivered on every MCP request (measured). Never depend on `Mcp-Session-Id` (client 2.1.260 opens with the session-less 2026-07-28 revision and falls back to 2025-11-25); validate Origin; the token dies with the thread.
- 2026-09-05 · **`claude -p` flags are load-bearing**: `--tools ""` (else 26 built-ins incl. SendMessage/CronCreate are reachable) + `--allowedTools 'mcp__qwry__*'` (else every MCP call is permission-denied and the model gives up silently) + `--setting-sources ""` (else the user's own hooks/CLAUDE.md inject context; `--bare` is unusable: it needs an API key) + `--strict-mcp-config --mcp-config` + `--output-format stream-json --verbose [--include-partial-messages]` + `--system-prompt` (drop `--exclude-dynamic-system-prompt-sections`: documented no-op with it) + `--session-id <thread id>` on the first call and `--resume` after; prompt on stdin; direct exec via tokio::process, never a shell. `system/init.mcp_servers[].status != "connected"` is a hard failure before the first model turn (a dead MCP server yields exit 0 and a toolless model). `--max-turns` is per invocation: the TS loop owns the 12-turn thread cap.
- 2026-09-05 · **Provider HTTP goes through Rust** (`agent_http_stream`: the Keychain key is injected server-side, SSE bytes relayed over a Channel, abort by request id): satisfies §8.3 literally (keys never enter TS) and Tauri's http scope cannot allowlist arbitrary user-supplied local hosts/ports. Only Rust talks to the network, as only Rust talks to PG. Connect timeout 10s, idle-read 60s, no total; unreachable → error event carrying a "start the server" hint for local presets.
- 2026-09-05 · **Gemini ships as a preset of the OpenAI-compatible adapter** (`generativelanguage.googleapis.com/v1beta/openai/`, bearer), not the native adapter §7 listed third: streaming + tools are documented; the layer is beta and parallel tools undocumented, so promote to native only when a measured failure demands it.
- 2026-09-05 · **Preset quirks are data** (`presets.ts`): Mistral tool_call ids must match `^[A-Za-z0-9]{9}$` (also when routed through OpenRouter/Together: family detected from a model id containing `mistral`); xAI rejects `additionalProperties:false` and needs /chat/completions, not /responses; DeepSeek defaults to `deepseek-v4-flash` (its reasoning model is unreliable with tools); llama-server needs `--jinja`, vLLM `--enable-auto-tool-choice --tool-call-parser`; Ollama ≥ 0.8.0 streams tool calls; local `/v1/models` ids are file paths, derive labels. Measured: LFM2.5-2.6B on llama-server completes one forced tool call with canonical OpenAI chunking; this does not move §3 (multi-turn loops are the small-tier failure).
- 2026-09-05 · **Provider events gain `thinking` and `toolResult`**: OpenAI-compatible runtimes stream `reasoning_content` deltas (measured, ~40 chunks before the tool call) and Anthropic streams `thinking_delta`; both render in the thinking strip / trace, never as answer text. `toolResult` + `Provider.ownsLoop` cover claude -p, which executes tools itself: the loop records and never re-executes.
- 2026-09-05 · **Registry facts**: Sonnet 5, Opus 5 and Opus 4.6/4.7/4.8 reject temperature/top_p/top_k outright (400); Haiku 4.5 accepts; `samplingParams` is per model. Anthropic's minimum cacheable prefix is per model and non-monotonic (Opus 5 512, Sonnet 5 / Opus 4.8 1024, Opus 4.7 2048, Haiku 4.5 / Opus 4.6 4096): `cacheMinTokens` per model, and a miss is silent (check `cache_creation_input_tokens`). Hosted small-parameter models never earn `small` (tier = loop reliability, not RAM). Hand-typed model ids are `verified: false` until a live `/models` call confirms them (`claude-haiku-4.5` vs the real `claude-haiku-4-5` is the failure class); the picker prefers live model lists.
- 2026-09-05 · Claude Code keeps its own transcript under `~/.claude` for `--resume` threads: accepted as a second local copy (ideology 7 holds, nothing leaves the machine); `--no-session-persistence` would kill follow-ups.

### Agent A1 · W1 (build, 2026-09-05)
- 2026-09-05 · **A non-2xx provider response is DATA, not a Rust error**: `agent_http_stream` sends `Start` (status + headers) before any body, follows with an `Error` chunk carrying the provider's own text (capped at `ERROR_BODY_CAP`), and still resolves `Ok(HttpDone)`. `Platform.httpStream` is the single place that turns that into `HttpStatusError` / `HttpUnreachableError`, which it cannot do if the command rejects first — and rejecting first would throw away the provider's message, which is the only useful half of a 400. The adapter maps status plus body to a `ProviderErrorKind`; a 401 never quotes the body (it routinely echoes a key fragment, §8.3).
- 2026-09-05 · **Provider cancellation is a `CancellationToken` keyed on a CALLER-minted `request_id`** (module-level map in `agent_http.rs`, not `AppState`, so an abort works from any handler): TypeScript mints the id before the call, so `agent_http_abort` can land before the first chunk does. A server-minted id leaves that window open. An unknown id is a no-op, never an error.
- 2026-09-05 · **The MCP server is one listener per app instance and one `StreamableHttpService` + call log per THREAD TOKEN** (`agent_mcp.rs`, rmcp on bare hyper via `TowerToHyperService`, no axum): configured stateless (`legacy_session_mode: false`, `json_response: true`, allowed hosts loopback only) so no `Mcp-Session-Id` is ever issued for anything to route on — the CLI opens session-less and the bearer token IS the identity. The token maps to that thread's dedicated read-only session and dies with the thread, so a child calling back into a torn-down thread gets a clean error instead of another thread's database.
- 2026-09-05 · **The MCP result text mirrors `tools.tauri.ts` / `context.ts` byte for byte**, not merely in spirit: `Meta::build`, `describe_text`, `run_text`, `peek_text` and `probe_text` in `agent_mcp.rs` are line-for-line ports of `buildMeta`, `renderDescribe`, `formatRun`, `formatPeek` and `formatProbes`, and both sides' doc comments name the other. Otherwise `claude -p` reads a different app than the OpenAI adapter does and the EVAL numbers stop comparing. Fixed by the port: NULL renders `∅`, a capped result reads `(N rows, capped at 2000; showing M)`, probe blocks are `-- <sql>`.
- 2026-09-05 · **`describe_tables` prefers the comment it just read over the cached snapshot's**, column by column (`ColumnValues.comment` → `renderDescribe` / `describe_text`), so a `COMMENT ON COLUMN` added since the last introspect reaches the model; the CREATE TABLE header comment stays the snapshot's, because that is the one the candidate block (§4.2) quoted and the LEGACY rule was applied to. Both renderers changed together, as they must.
- 2026-09-05 · **`anthropic-version` belongs to the adapter, not the relay**: `outbound_headers` still drops any caller-supplied `authorization` / `x-api-key` (a TypeScript bug must never smuggle a header past the Keychain path) but leaves `anthropic-version` alone and fills in `ANTHROPIC_VERSION` only when the adapter sent none. The adapter is what knows which wire shape it was written against; the two constants agree today and this is what keeps a disagreement from becoming a duplicated header and a 400.
- 2026-09-05 · **`HttpStatusError` carries `retryAfterMs`**: the platform reads the wait the provider STATED (`Retry-After` delta-seconds or HTTP-date on the start chunk, or `HttpChunk.error.retry_after_ms`) and attaches it to the error, because response headers are not reachable from the adapter layer. `http.ts` prefers it and falls back to scraping the body; absent means the provider said nothing there, never that it said zero. A header-derived value travels as a number on the error and never as message text.
- 2026-09-05 · **Every appdb write the agent makes has a read**: `agent_answers_list(thread_id)` joins `agent_answers` to `agent_turns` in turn order, so a reopened thread rebuilds its assumption chips, sanity line and row count instead of showing a turn whose verdict is unrecoverable. `agent_answer_put` alone was a write nothing could read.
- 2026-09-05 · **Agent sessions are gated at the session, not only at the tool**: `PgSession.agent_gated` is set in `open_session(force_read_only)` before the id is registered, and `commands::execute` / `execute_stream` refuse such a session. Verifier finding (S1): `default_transaction_read_only` does not stop `pg_sleep` / `pg_terminate_backend` / `lo_import`, so a raw-SQL command reachable by session id was a side door around the AST gate.
- 2026-09-05 · **Cancel wins over an error body**: an `agent_http_abort` during a non-2xx drain emits `Cancelled`, never the partial provider text as Auth/Rate/Provider (a user cancel used to be recorded as a provider failure).
- 2026-09-05 · **`StopReason` gains `turnCap`; every truncation is `maxTokens`**: `claude -p` `error_max_turns` is a cut-off conversation (the loop reports the turn-cap verdict with Fix It, never "answered" on the empty text); Anthropic `model_context_window_exceeded`, OpenAI `content_filter` / `length` map to `maxTokens`. Trace and persisted tool records keep the provider's own tool name (`ToolName | string`): a stray built-in shows up by name instead of being laundered into the `run_sql` count.
- 2026-09-05 · **A superseded run never clears the live run's flags**: `runInto` clears `busy`/`phase` only when its controller is still the thread's authoritative one, and a cancel pressed while the first ask is still inside `agentConnect` is honoured the moment the session lands (`cancelRequested` set), so ⌘. works during a bastion connect too.
- 2026-09-05 · **NUMERIC compares as decimal text**: `eval/compare.ts` rounds NUMERIC half-up at four places on the digit string (BigInt), never through `Number()`, which collapsed two distinct 18-digit values into one string (a false PASS). FLOAT4/FLOAT8 keep the float rule; the cross-type agreement tests still hold.
- 2026-09-05 · **Unmeasured is not green** (EVAL.md §4): the CI gate fails when no baseline row exists for the run's bench + provider + model + prompt version; every recorded row is `claude-code`, so configuring a hosted `EVAL_API_KEY` begins with one deliberate local run and a baseline commit.
- 2026-09-05 · **W1 fixers were applied by the maintainer session, not by the workflow**: five parallel Opus fixers each made two tool calls and then hung until the harness's 3-minute stall detector killed them (six attempts, ~2.7 h); the findings carried exact fixes, so they were applied directly. Long-running commands in workflow prompts run in the background and are polled.

### Agent A1 · W2 (design lock, 2026-09-05; plan in ~/projects/qwry-agent-lab/docs/research/w2-ui-plan.md)
- 2026-09-05 · **The Ask card's UI state lives in `src/stores/ask.ts`**, persisted like `useInspector` (open + width only), not in `src/stores/agent.ts`: the agent store is loop-facing, written around LESSONS 3, and unpersisted; a persisted width inside it would force a `partialize` around the whole thread map. Thread identity (`activeThread` per profile) stays in agent.ts, where W1 put it. The card defaults CLOSED, unlike the inspector: a panel appearing uninvited after an update is the wrong first impression.
- 2026-09-05 · **Width budget = 480px for the main card, both directions**, enforced as a transition effect in App.tsx on `askOpen`/`inspectorOpen` (measured on the first frame, before the opening card has taken its width) so every open path (⌘J, ⌘I, menu, palette, titlebar) obeys without each caller knowing the rule.
- 2026-09-05 · **One grid species (AGENT-UX §2.4, item 3 of §2 at the time): `Grid` grows a `readOnly` prop, never a second grid.** In readOnly the grid renders the `statement` it is handed and touches none of `useEdits`/`useBrowser`/`useResults`/`useGridFilter`/`useFind`/`useGridStats`/`useInspector`: those are singletons indexed by the ACTIVE RESULTS TAB, so a standalone grid publishing to or reading from them paints one dataset's coordinates over another (LESSONS 4). Inspector targeting, ⌘F find, the status-bar quick-filter and selection stats are therefore OFF for Ask grids in W2; `colTypes` and `filterText` props are the scoped replacements. Editing, insert, delete, undo, paste, FK navigation are off; selection, copy/export, sort, hide, record view, keyboard nav stay.
- 2026-09-05 · **Esc inside the card is a ladder, never a close**: trace → picker → blur the composer. ⌘J mirrors ⌘I's focus discipline (capture on open, restore on close, focus pulled out of a collapsed card from any path). Focus is state-owned (LESSONS 7): `useAsk.focusSeq`, consumed by the lazy AskPanel with rAF so escStack's focus-restore lands first and loses. ⌘. cancels the Ask turn only when focus is inside `.ask-fixed` and a turn is busy; the menu accelerator path in App.tsx applies the same test, loading the agent store lazily so the editor's cancel stays instant.
- 2026-09-05 · **The READ-ONLY badge is the titlebar chip's LOCKED (ok) tint, always**; the sketch's danger toggle for prod is not reproduced: danger on that chip means writes on, which Ask can never be (AGENT-SPEC §8.1). Wording follows the chip: `READ-ONLY`, `PROD · READ-ONLY`.
- 2026-09-05 · **Species assignments**: assumption chips = Chip/pill toggle (`aria-pressed`); starter and follow-up chips = the dashed-ghost species in pill shape (a question not yet asked; hover turns the dash solid accent), rendered by ONE component (`FollowUps`) in both places so the morph motion is shared; thinking-strip chips are status pills until their call finishes, then buttons that open the trace; the send/stop control is the icon-button species at the 28px tier with the one accent fill (the sketch's 26px is off both the grid and the tier).
- 2026-09-05 · **Follow-ups render only when the loop supplies them**: `FollowUps` receives `questions={[]}` until `AskAnswer` carries `followUps` (requested of the agent owner); the UI never invents questions client-side. Starters in the empty state come from `starters.ts` over the schema's real nouns (LEGACY tables and partitions excluded, the generic row count never).
- 2026-09-05 · **Fix It and Retry are store actions in agent.ts** (`fixIt(exchangeId, sql)`, `retry(exchangeId)`), the same re-run shape as `toggleAssumption`: capture before await, `rearm`, `runInto`. Fix It hands the model the question, the failure text and the user's edited SQL as a starting point; it never rewrites someone else's query silently. Retry persists the user turn only when the failed run never reached `persist()`.
- 2026-09-05 · **Per-block provenance rides the footer** (AGENT-UX §9): the sketch shows the connection once in the header; the footer's first fragment carries the 14px avatar and name so every block speaks for its data even when scrolled away from the header.
- 2026-09-05 · **Thinking-strip chips group by label** (`probe ×2`, `run ×3`): consecutive calls with the same verb and identifier collapse into one chip that runs while any member runs and errs when any member erred; clicking it opens the trace at the FIRST call's step (the group's id is that call's id, which is also the trace step id). Chips are status pills while the exchange streams and become trace buttons once it lands.
- 2026-09-05 · **Three `readOnly` gates in Grid beyond the W2 plan**: the infinite-scroll `nearEndHook` never fires from a standalone grid (it would page the ACTIVE results tab), a TSV copy above `COPY_ASYNC_CELLS` runs synchronously instead of through the results-pane copy job, and an export failure reports through the copy cue toast (`export failed`) because there is no results pane to carry `globalError`; LESSONS 4 and 9. `RecordView` gained the same `readOnly` prop for the same reason: its staged-edit lookup is keyed by statement index, which a standalone grid's `index: 0` collides with, so a readOnly record view reads no pending edits and offers no edit hint.
- 2026-09-05 · **One error event per verdict, kind decided by the verdict**: `finish()` in loop.ts is the only emitter; a `failed` verdict WITH SQL is `sql` (editable SQL + Fix It), one WITHOUT is `provider` (Retry), and the stated wait (`retryAfterMs`) rides the event to `Exchange.error`. The loop used to emit `sql` and then `provider` for the same failure, and the store keeps the last one, so every SQL failure reached the UI as a provider error. The loop test asserts exactly one error event and its kind.
- 2026-09-05 · **Follow-ups are a separate post-verdict call, not a loop turn** (`src/agent/followups.ts`, `AskAnswer.followUps`): the store fires it after an `answered` verdict lands, on the run's own signal, and patches the exchange only while that same answer object is still on screen, so the answer never waits for the suggestions and the eval loop is unchanged. It is recorded as a `followups` trace step (prompt sent, raw reply, questions parsed) because nothing sent to a provider is hidden (AGENT-SPEC 8.4); it never runs for `ownsLoop` providers (a `claude -p` side call would `--resume` the thread and write itself into the conversation); v1 uses the thread's own model, the registry having no cost axis for "cheapest". Not persisted (appdb has no column): a reopened thread recomputes them for its newest answered exchange only.
- 2026-09-05 · **Base URL overrides are settings** (`useSettings.agentBaseUrls`, sparse, the preset default never stored): `modelChoice()` carries `baseUrl` so the loop's provider reaches the URL Settings › Models probed; `baseUrlFor()` reads the same map for the probes and group headings; the Settings field commits on blur or ↩ and the setup card commits the URL that answered.
- 2026-09-05 · **A reloaded failed exchange shows its failure block**: `openThread` maps `agent_answers.status` (`failed` → `sql` when SQL was recorded, else `provider`; `turn_cap` → `turncap`; `cancelled`) and prefers the recorded SQL over re-extracting it from the text. appdb keeps neither the error text nor the turn count, so the messages say only what is known (`the query failed. The error text is not kept in the history`, `stopped at the turn cap`), LESSONS 9 over invented detail.
- 2026-09-05 · **Re-runs start from a clean text** and **a busy thread refuses a new run**: `rearm()` clears `text`/`thinking` (deltas append, so the previous prose was becoming the new answer's prefix) and keeps the previous `answer` until the new one replaces it; `ask()`, `toggleAssumption()` and `locateExchange()` return early while `busy[threadId]`, so no caller can abort the live run through the controller swap. A provider failure on a re-run leaves the previous answer's SQL in the exchange; AnswerBlock hands FailureBlock `sql: null` for that kind so the old statement is never offered as the one that failed. A cancelled run keeps its SqlRow (the failure block shows only `cancelled` for it, AGENT-UX 7).
- 2026-09-05 · **`spring.layout` is the shared-layout preset** AGENT-UX 10 names (PANEL-class: the suggestion → echo morph travels and settles rather than snapping); **14 joins the avatar exceptions** in DESIGN rule 5 as the inline provenance mark (Ask header and footer); **`.badge-ok`** in tokens.css is the LOCKED tint (`prod-chip.locked`, 12%), worn by the READ-ONLY badge and the `large` tier (the `mid` tier wears `.badge-accent`), replacing two local color-mix tints.
- 2026-09-05 · **`Manage in Settings › Models…` lands on the section**: `setSettingsOpen(open, section?)` carries a runtime-only `settingsSection`; ModelsSettings scrolls its heading into view on mount with the default (instant) `scrollIntoView`, never animated; the section clears when the modal closes.
- 2026-09-05 · **Finished chips open in the drawer before the answer lands**: `toolEnd` carries `args` and `result`, `ToolChip` keeps them, and the drawer renders a finished chip through the same tool-row renderer as a trace step; a running chip stays a static row. Context, turn and verdict rows still arrive with the answer.
- 2026-09-05 · **Path-shaped model ids match the registry on their file name** (`baseModelId` in registry.ts, used by `modelInfo`/`tierOf`): llama-server reports `/…/LFM2.5-2.6B-Q4_K_M.gguf`, and without this the loop gated it as `mid` and ran the tool loop on a small model; `listModels` therefore labels and tiers such a path from its registry row.
- 2026-09-05 · **The width budget binds the drag as much as the open**: both side-card resize handlers cap the dragged width at `main + current - 480` from a live `.main-card` measurement (`sideCardDragCap`, App.tsx), never under the card's current width, so a drag can shrink a card in an already-tight window but never widen one into the main card. The transition effect alone left the main card squeezable to nothing once both cards were open (review finding, AGENT-UX §1).
- 2026-09-05 · **The composer swallows only unmodified typing keys** (LESSONS 10): Esc and every ⌘/⌃/⌥ chord bubble; ↩ sends, ⇧↩ newlines, `isComposing` respected. A click inside the card focuses the panel root unless the target is an input, textarea, select, contenteditable or the grid (which focuses itself), so WKWebView's no-focus-on-click buttons still leave the card as the chord target. Composer drafts are per connection (`useAsk.drafts`, keyed by the on-screen `draftFor`): a draft typed toward one connection never surfaces in another's composer and comes back when its connection does (LESSONS 4).
- 2026-09-05 · **AskPanel pushes `setActiveProfile` + `loadThreads` on connection change** (provenance structural, LESSONS 4); the thread list is a ContextMenu (menu-row species) with `Delete Current Thread…` behind `confirmDanger`; a trace target whose exchange left the active thread closes itself.
- 2026-09-05 · **Scroll and reveal**: a new exchange pins the thread scroller to the bottom instantly in a layout effect; streaming growth never auto-scrolls; nothing animates scroll or typing. Answer sections fade in with `--dur-slow` opacity via `.ans > *`, never height. The collapsed card and the closed trace drawer get `visibility: hidden` after `--dur-slow` (a `visibility 0s linear var(--dur-slow)` delay that survives the reduced-motion kill) so hidden controls leave the tab order (ROADMAP gotcha) while the width transition still runs.
- 2026-09-05 · **The Ask grid slot** is `GRID_HEADER_H + min(rows, 6) × gridRowHeight(density) + 2` (its own two hairlines, border-box), `<Grid key={exchange.id} statement readOnly />` with the statement memoised on `(run, sql)`; a zero-row run renders the status line (`0 rows · N ms`) and no grid, since header-only chrome over nothing is dead space (DESIGN rule 2). Numeric alignment without column types sniffs the first 50 non-null values per column against the stats effect's own shape (`NUM_SHAPE`, a module constant mirroring that regex rather than hoisting it); a host that hands `colTypes` gets both the tokio short forms and the `format_type` long forms treated as numeric.
- 2026-09-05 · **The polite live region is the LATEST block, not only the streaming one** (`aria-live="polite" aria-atomic="false"`), so the final `answer.text` replacement is still announced; older blocks are `off`. Streamed text is plain pre-wrap text, no per-character work.
- 2026-09-05 · **Thinking strip overflow**: fixed 24px, never wraps; every chip arrival and strip resize assigns `scrollLeft = scrollWidth` (an instant assignment, never animated) so the newest chip stays visible, and an `.overflow` class masks the left edge so clipped older chips fade instead of cutting a pill. The `·` more-marker shows while the exchange streams and no tool is running (the model is thinking). Chips enter through the `menuIn` preset, which is `spring.snappy` (AGENT-UX §10); their element identity never changes when a running pill becomes a trace button, so finishing does not replay the entrance.
- 2026-09-05 · **SqlRow's expanded body is a read-only CodeMirror 6 view** (`sql({dialect: PostgreSQL})`, `qwryHighlight`, `qwryTheme`, `Prec.high` override for auto height / `--text-sm` / `--lh-data`, `lineWrapping`, `readOnly` + `editable(false)`), mounted in a layout effect so the reveal is one frame and remounted on theme flip like the editor. `Copy SQL` fires the copy cue; `Open in Tab` is `useTabs.newTab(sql, title)`, which selects the tab and never auto-runs. Neither carries an ellipsis: both act at once (WRITING rule 2).
- 2026-09-05 · **Follow-up chips morph by `layoutId = ask-q:<question>`** into the LATEST AnswerBlock's echo (a `motion.div` with the same id), so a question typed twice never pairs two echoes; `FollowUps` takes `disabled` and receives `busy` from AnswerBlock. Empty sections (`FollowUps`, `AssumptionChips`, `SanityLine`) return null so `.ans`'s flex gap never reserves a slot for an absent part.
- 2026-09-05 · **The trace drawer is NOT on escStack**: its capture listener makes every window chord stand down, right for a modal, wrong for a non-modal panel inside the card (⌘J/⌘W must keep working with the trace open). A scoped keydown on the drawer root owns exactly two keys, Tab (wraps among the drawer's tabbables, the inspector-style trap AGENT-UX §12 asks for) and Esc (closes, `preventDefault` + `stopPropagation` so AskPanel's ladder does not double-handle); every chord bubbles (LESSONS 10). Focus choreography (LESSONS 7): on open the opener is captured and rAF focuses the asked-for step's header button or the drawer root; on close rAF returns focus to the opener if still connected, else the panel root, standing down when `useAsk.open` is false (App.tsx owns the collapse restore) or when something else took focus deliberately. Drawer mousedown focuses the drawer root unless the target is a button, so Tab starts inside; body text stays selectable.
- 2026-09-05 · **Trace reveal and rows**: opening from a chip or sanity fragment expands `tool:<id>` and `scrollIntoView({block:'nearest'})` (instant); the footer link expands the first step (context). A fresh exchange resets the expanded set; a second chip on the same open exchange adds to it. Rows are context / turn N / tool / verdict in the sketch's kind colours (tier 2 / `--syn-keyword` / `--accent` / `--ok`); turn rows summarise the tool calls that followed (`describe_tables(users, orders) · peek_values(users.is_deleted)`) else the turn text's first line; tool rows are `<code>name</code> summary` (`run_sql 12 rows` parsed from the `(N rows)` line) with ` · error` in danger; the verdict row reads `answered · 12 rows · 3 assumptions · 1 warning`. Bodies: context = the exact block sent; turn = thinking then text; tool = args (run_sql/probe SQL shown as SQL, not escaped JSON) then result; verdict = assumptions + message/SQL. Mono, pre-wrap, 220px cap, `user-select: text`.
- 2026-09-05 · **Trace timing shows what was recorded, nothing derived**: `48 ms` / `2.9 s` per step, tabular and right-aligned; the verdict row reads `17.6 s total` because the loop records elapsed-since-start there. Reloaded threads (the store writes `turns: 0` and tool ms 0) hide the ms cells and the turns fragment rather than print `0 ms` (LESSONS 9). The drawer header mirrors the footer: `5 turns · 17.6s · 2 probes` (probes = tool steps named probe; zero fragments omitted). The footer note reads `Everything shown here is what qwry sent and received. Claude Code adds its own harness prefix that cannot be shown.` only when `exchange.provider === 'claude-code'`, else `Everything the model received is shown here.`
- 2026-09-05 · **FailureBlock's SQL field is CodeMirror** (the JsonField precedent, not the sketch's contenteditable): `sql({dialect: PostgreSQL})`, `qwryHighlight`, history, defaultKeymap, drawSelection, lineWrapping, 220px scroll cap, aria-label `SQL to fix`; the CM modules are chunks the editor already loads. ⌘↩ inside the field is Fix It at `Prec.highest` (App.tsx respects `defaultPrevented`, so the window's Run accelerator does not also fire); Esc hands focus to `.ask-panel`, never body; a new last attempt replaces the doc outside the undo history. Per kind: `sql` = danger-soft `role=alert` box, uppercase `stopped after N turns` (`query failed` at 0), the PG message with double-quoted identifiers rendered as mono `.ask-id` (quotes dropped: data wears data's clothes), `DETAIL:`/`HINT:` on their own lines under a tier-2 uppercase key, the field, then `Fix It ⌘↩` (primary, disabled while busy or blank) / `Open in Tab` / `Ask Differently`; `turncap` = the loop's `stopped after 12 turns` title plus the same field and actions when a SQL was tried; `provider` = `stopped`, the provider sentence verbatim (it already carries the next step), `Retry` primary and a live `try again in 20s` countdown from `retryAfterMs`; `cancelled` = `cancelled` alone in the status register.
- 2026-09-05 · **The model picker popover is an `AnchoredOverlay`** (src/app/overlay/Overlay.tsx), the app's non-modal anchored primitive (portal to body, escStack so Esc and outside click close it, viewport clamp, topmost-only `onKey`), not an in-card absolute panel; it hangs from the pill's right edge (`rect.right - 280`, `rect.bottom + 4`, `transform-origin: top right`). Keyboard nav (↑↓ Home End ↩) runs through `onKey` with a `.hot` row like ContextMenu; the menu is focusless (mousedown `preventDefault`) so escStack restores focus to the panel root on close, and AskPanel's Esc ladder still converges through `onClose`.
- 2026-09-05 · **Tier bubbles have a keyboard route** (DESIGN rule 8): a real styled `role=tooltip` span, a SIBLING of the badge positioned against the nearest positioned ancestor (the pill, `max-width: 220px` so it stays inside a 320px card; the popover row, 240px). It opens on hover, on the pill's focus (suppressed while its popover is open), and on a popover row when the arrow keys make it hot, never when the pointer does; the badge itself is never focusable (tabindex inside a button is invalid, the menu is focusless). Text `<b>tier</b> · <sentence>`; an unknown model reads `<b>mid</b> · unverified model, treated as mid · <sentence>` behind a `mid?` badge face. Sentences are AGENT-SPEC §3's, lowercase status fragments (small: writes the SQL in one shot with a repair loop and never calls tools; mid: runs probes but may not act on what they show; large: runs probes and acts on their evidence).
- 2026-09-05 · **Local-runtime probes in Settings guard on probe currency, not component liveness** (`probed[id] === url` after the await): liveness let StrictMode strand the chip on `checking` and let a slower stale probe overwrite a newer result.
- 2026-09-05 · **Picker groups** (`modelSources.groupsFrom`): Claude Code always (`· subscription`); every hosted provider with a Keychain key (`· key saved`); every local runtime whose live `/models` probe answered (`· 127.0.0.1:8080`); the currently chosen provider even when it did neither (`· no key` / `· not running`, its row kept visible above the hand-off so the pill is explained); and ONE teaching group, Anthropic without a key. A provider whose Keychain/probe state has not answered yet appears only when chosen, titled `· checking`, so nothing paints and then vanishes; every other provider is a Settings matter behind `Manage in Settings › Models…`. Selection writes `setAgentConnModel(profileId, …)` and ALSO `setAgentModel` when no app default exists yet, so `modelChoice()` resolves at once (the exchange records provider/model at ask time; there is no separate current model).
- 2026-09-05 · **`src/ask/modelSources.ts` is the one place three components share provider knowledge** (labels, auth kind, `PROVIDER_ORDER`, `shortLabel` ('Claude Haiku 4.5' → 'Haiku 4.5', strips ' (local)'), `contextHint`, `TIER_SENTENCE`, `defaultModelFor`, `probeLocal` with a 3s race timeout answering `[]` on silence, `loadKeys`, `groupsFrom`, `encode/decodeChoice` for select values) instead of ~80 duplicated lines each.
- 2026-09-05 · **Settings › Models** lists ALL providers in `PROVIDER_ORDER` as a 3-column grid (name · field · status) inside the existing settings-section skin, mounted after the Query section via `React.lazy` + Suspense (fallback = the `Models` heading so the modal does not jump) to keep the provider registry out of the cold-start path. Key fields are `type=password` `autoComplete=new-password`, save on blur/↩, clear after save, status `saved` + `Forget Key` linkish, else `no key` / `checking` / `not saved` (danger); saving a key while no app default exists adopts the provider's default model, so Ask becomes usable from Settings too. URL fields probe on blur/↩ (skipped when unchanged) → `1 model` / `N models` / `not running` / `checking`. Claude Code's row reads `No key needed` / `uses your subscription` (no CLI-presence IPC exists). Defaults: `Model for <conn in mono>` with an `App default` option and `App Default`, options `Haiku 4.5 · mid` grouped per provider from the same `groupsFrom`.
- 2026-09-05 · **SetupCard**: provider select (Claude Code first, `· your subscription`; local presets `· local`; routers without registry rows excluded, since a key alone yields no model to ask with), then `Key` + `Save Key`, or `Base URL` + `Save URL`, or `Use Claude Code`; save writes the Keychain, then `setAgentModel(provider, defaultModelFor)` and `onConfigured` → `useAsk.requestFocus()`. The note line is ONE fixed slot: the Keychain sentence at rest, the error register on failure (`nothing answered at 127.0.0.1:8080. Start it with llama-server --jinja`), so feedback never moves the form (DESIGN rule 2, LESSONS 9). Claude Code's default model is claude-haiku-4-5, the sketch's active row. Registers: menu rows Title Case (`Add a Key in Settings…`, not the sketch's `Add a key in Settings…`), status chips lowercase, the sketch's parenthetical option faces (`Claude Code (your subscription, no key)`) became `·` faces (WRITING rule 3), and the `claude` command name wears mono (`.ask-id`).
- 2026-09-05 · **Starters are de-snaked prose over a deterministic pool**: nouns from table names (`order_v2` → orders, `wardrobe_products_v2` → wardrobe products, a naive pluraliser on the last word); pool = top 8 by reltuples ∪ top 4 FK hubs (inbound FK count), ties broken by name so one snapshot always yields the same three; migration ledgers (schema_migrations, alembic_version, flyway, knex, ar_internal_metadata, spatial_ref_sys) excluded alongside LEGACY twins, partitions and system schemas. Shapes: `How many orders were created each month this year?`, `Which users have the most orders?`, `How are orders split by payment status?`, filling with `What are the 20 most recently created orders?` / `What do 10 sample X look like?`. Locked by starters.test (determinism, no underscore, parent → child, `noun()`).
- 2026-09-05 · **Sanity fragments name their producing call** (`SanityFragment.stepId`, loop.ts): a peek records `{ column, id }` and `sanityLine` emits `checked <col> values` with that id, a probe fragment is pushed as `{ ...fragment, stepId: call.id }`, and `sanity_json` round-trips it. `sanityStep()` returns the id when the trace carries a tool step with it and falls back to the structural text/SQL match only for fragments persisted before the field existed, so the section-4 click is structural (LESSONS 4) rather than text-matched; loop.test asserts every emitted fragment's id is a trace step. Cancelled and provider-failed verdicts now pass `sanity` too: the tool step stays in the trace, so what it found stays in the answer.
- 2026-09-05 · **The `claude -p` child gets an env ALLOWLIST, and `USER` is on it**: PATH, HOME, USER, LOGNAME, TMPDIR, LANG, CLAUDE_CONFIG_DIR. Measured with `env -i`: PATH + HOME alone yields "Not logged in · Please run /login" because the CLI keys its Keychain credentials on the username; adding USER signs in. Still never the whole environment (a stray API key in a dev shell must not reach the child); a unit test pins the list. Found in the maintainer's first taste-pass Ask.
- 2026-09-05 · **The MCP gate declines `server/discover` (MCP 2026-07-28) on purpose**: rmcp 3.2.0 and Claude Code 2.1.261 both speak the new revision and together lose the tools: the probe succeeds, the CLI asks `tools/list` four times, gets the five tools each time, and still starts the model with an empty tool list. A toolless Haiku then writes `<function_calls>` as prose and invents the rows (seen in the first taste pass). The gate answers the probe with JSON-RPC -32601, the CLI falls back to `initialize` at 2025-11-25 (the revision W0 and the eval baseline measured), and `claude_p_end_to_end_lists_and_calls_the_tools` (opt-in, `QWRY_TEST_CLAUDE=1`) drives the real CLI at the real server to pin it. `QWRY_MCP_TRACE=1` dumps the wire.
- 2026-09-05 · **A connected MCP server is necessary, not sufficient**: the adapter's init check now also requires at least one `mcp__qwry__*` tool in `system/init.tools`; a connected-but-toolless child is killed before the model speaks. Belt for the gate above and for any future CLI change of the same shape.

### Agent A1 · W2b (taste, 2026-09-05; picture in ~/projects/qwry-agent-lab/docs/ask-sketch-v2.html)
- 2026-09-05 · **One right pane, two modes**: Inspector and Ask are modes of ONE card (`src/stores/sidePane.ts`: mode, open, width; it replaces the open/width halves of `inspector.ts` and `ask.ts`), never two cards fighting for the right side. The titlebar pair is a radio (lit icon = mode; Ask wears lucide `MessageSquare`, the sparkles go); ⌘J / ⌘I open the pane in that mode or switch it, and the chord of the showing mode closes it. Width is shared, floors per mode (Inspector 220, Ask 320), switching clamps. Supersedes the W2 480px width budget (the open effect AND `sideCardDragCap`), the second drag handle and the second persisted width: one pane needs no budget.
- 2026-09-05 · **Header diet**: the Ask header is `Ask` (text) · `Threads` · `New Thread` and nothing else (DESIGN rule 12, the Inspector's own header shape). Avatar, name, database, the READ-ONLY badge, the model pill and its tier leave it: the connection is the pane's binding (sidebar + placeholder + footer avatar), PROD stays the titlebar chip, and read-only is a promise the anatomy keeps (a SQL row on every answer), not a badge. Supersedes the W2 READ-ONLY-badge line and the header half of the per-block-provenance line; DESIGN rule 5's 14px avatar exception now names the footer alone.
- 2026-09-05 · **The composer is two rows and has no hint line**: textarea, then model pill (left) · accent send/stop at the 28px icon-button tier (right). The W2 hint line (`↩ ask · ⇧↩ newline · read-only · every answer shows its SQL · thinking · ⌘. cancels`) is deleted under DESIGN rule 11; chords live in the send button's tooltip and the Keyboard Shortcuts sheet, and the Stop face IS the cancel affordance. The configured empty state is the three starters docked above the composer and nothing else; the slogan sentence goes.
- 2026-09-05 · **The picker moves to the composer's control row** and opens UPWARD from the pill (AnchoredOverlay anchored to the pill's left edge, not the W2 right-edge drop): pill = short name + chevron, `small` alone badges the pill; rows = name · optional ctx · tier badge; groups = provider name (a local runtime appends its URL; a provider that cannot answer appends `· no key` / `· not running`; the W2 `· subscription` / `· key saved` / `· checking` qualifiers go, rule 11); footer `Manage Models…`. Nothing explains tiers anywhere: the tier bubbles (`role=tooltip` on hover / focus / arrow-hot), `TIER_SENTENCE` and the `mid?` face are removed and AGENT-UX §8's one-sentence-per-tier rule is repealed. Supersedes the W2 tier-bubble line and the `Manage in Settings › Models…` face.
- 2026-09-05 · **Answer text = the model's LAST text block**, one or two sentences: `src/agent/loop.ts` stops concatenating pre-tool narration across turns (`text += ev.text` ran over every turn, so "Now retrieving…" became the answer's prefix); the final turn's block is the answer and every earlier block is a trace row. Before display the SQL fence, the `Assumptions:` line and any markdown table are stripped (DESIGN rule 14: each has its own slot), bold and inline code render, headings / lists / tables / links never; the raw text stays in the trace untouched. The prompt asks for one or two sentences and ≤6-word assumption labels.
- 2026-09-05 · **Footer = avatar · turns · time · model · `Trace`** (`1 turn · 20.4 s · Sonnet 5`): the connection name is dropped (the avatar is the provenance mark), `How did it get this?` becomes `Trace`, and the line fits at 320 on one line (DESIGN rule 13). Units take a space (`20.4 s`, WRITING status register, `12 rows · 3.1 ms`); reloaded threads keep hiding zero turns and zero ms (LESSONS 9).
- 2026-09-05 · **Assumption chips wrap** (`min-height` 24px, `line-height` 1.35, `white-space: normal`): a long label grows the pill downward and the row onto further lines, never clips inside the pill or scrolls; the row leads with `Assumed`. Labels are capped at the model (≤6 words in the prompt), never at the pill.
- 2026-09-05 · **One ms formatter**: the Ask status line prints `run.ms` in the results pane's shape (`toFixed(1)` + ` ms`, `1861.9 ms`, ResultsPane's own literal), never the raw float (`1861.922375 ms`); the footer's seconds share the one-decimal, spaced-unit rule (`20.4 s`).
- 2026-09-05 · **A readOnly grid fills its slot**: when the columns' natural widths fit the slot the last column stretches to the right edge; otherwise the grid scrolls as everywhere else. At 320 the header cut `notification_count` mid-name at natural widths while empty space sat to its right.
- 2026-09-05 · **Pixel evidence comes from a fixture harness, not from AX-driving the app**: a vite-served route renders AskPanel with a canned thread per state (answer / empty / busy / picker / failure) at a given width and theme, and `scripts/ask-frames.ts` drives headless Chrome over it to write `<out>/<state>-<width>-<theme>.png` for 320 / 392 / 560 × dark / light. The W2 gotchas (Spaces, menu tracking loops, single-instance) made synthetic input lose eight events in one gate; the dev build remains the final eyeball, not the evidence.
- 2026-09-05 · **`.claude/skills/taste-gate` is the gate's procedure**: frames → rules 11–14 as yes/no per frame → FINDINGS with frame evidence and a pass / block verdict, S1 blocks and a block means a fixer round, never "awaiting taste pass". A Fable-class reviewer signs the verdict (restraint is judgment, not rule-matching). Law stays in DESIGN.md; the skill only says how to run it, so every session and workflow agent runs the same gate.
- 2026-09-05 · **DESIGN.md rules 11–14** (every string earns its pixels · a strip states one thing · floor first · data once) plus the rule 9 extension (frames at floor / default / max of a live state, both themes): W2 satisfied every rule its reviewers had and still shipped a hint line, a ten-item header, a single-width sketch and prose that repeated the grid. DESIGN was a consistency law and WRITING a register law; neither spoke of restraint, and "which rule made producing this okay?" had the answer "none forbade it".
- 2026-09-05 · **Both mode holders stay mounted**: the one right pane renders the Inspector holder and the Ask holder at once and the one off stage is `visibility: hidden` via `data-mode`, never unmounted, so a mode switch keeps the inspector's target and scroll and Ask's streamed thread alive; the collapsed pane hides both holders after `--dur-slow` (hidden controls leave the tab order, DESIGN rule 2, ROADMAP_log W2 gotcha). The App effect moves focus out of a hidden holder explicitly (to `.inspector-fixed`, or the composer via `requestFocus`), never trusting the engine to blur it.
- 2026-09-05 · **`qwry.sidePane` adopts `qwry.inspector` once**: a fresh sidePane store with nothing of its own reads the legacy inspector blob's open + width, so the maintainer's dragged inspector width survives the store move; its own key wins from then on. Ask defaulted closed and never owned the launch state, so its old key is not consulted. `useInspector.open/width` and `useAsk.open` are read-only mirrors of the pane for their existing readers (TraceDrawer, Inspector, JsonTree); the setters (`useAsk.setOpen/toggle/setWidth`, `useInspector.setWidth`, `ASK_W_*`) are gone.
- 2026-09-05 · **AskEvent `narration`**: a tool call closes the text block before it; the loop emits the block as `narration`, the store clears the answer slot, and `AskAnswer.text` plus the persisted `agent_turns.content` are the LAST block (live/reload parity, LESSONS 9: `openThread` re-derives SQL and assumptions from `content`, both of which live in the last block). Earlier blocks stay raw inside the turn's trace row; the trace keeps one `turn` row per qwry turn, so an ownsLoop run is `context · turn · tool… · verdict`, and splitting narration into its own rows is the TraceDrawer's later call. `runSmall` is untouched: the small tier has no tool calls and no block boundary.
- 2026-09-05 · **`src/lib/duration.ts` is the status register's one clock**: `msText` (`1861.9 ms`) and `secondsText` (`20.4 s`), extracted from ResultsPane byte-identically, so the Ask status line, the footer and the results pane move together; `footerStatus` lives in `src/agent/display.ts` (testable without the grid) and hides zero turns and zero ms.
- 2026-09-05 · **No rowless teaching group in the picker**: `groupsFrom` no longer emits `Anthropic · no key` when Anthropic is not the chosen provider. A heading with no rows teaches what `Manage Models…` directly beneath it already offers (DESIGN rule 11; the locked sketch's picker shows Claude Code and llama.cpp only). Supersedes the W2 picker-groups line's "ONE teaching group"; the chosen provider still shows `· no key` / `· not running` above the hand-off so the pill's choice is never orphaned. Group titles are the bare provider name except the three exceptions the sketch draws (`· 127.0.0.1:8080`, `· no key`, `· not running`); `· subscription`, `· key saved`, `· checking` and the `mid?` option face are gone.
- 2026-09-05 · **A failed statement is titled `query failed`**, never `stopped after N turns`: that sentence is the turn cap's own (AGENT-UX 7) and a SQL error wearing it reads as a turn budget, a false message (LESSONS 9). `FailureBlock` drops its `turns` prop; the turn-cap message carries the count itself.
- 2026-09-05 · **Disabled chips win over motion's inline opacity**: every `layoutId` element (the follow-up and starter chips) gets an inline `opacity: 1` from motion's shared-layout projection, which outranked `button:disabled`, so a disabled chip looked pressable (disconnected starters; every earlier exchange's follow-ups while a run is busy). `.chip:disabled { opacity: var(--o-disabled) !important }` in ask.css is the one sanctioned `!important` here, found by a CDP probe of the harness page, not by reading CSS (LESSONS 8).
- 2026-09-05 · **Frames are stills of the settled state, captured over CDP**: `scripts/ask-frames.ts` drives each headless Chrome through the DevTools protocol (navigate, poll the harness's `data-harness-ready` mark, a real 700 ms settle, `Page.captureScreenshot` at scale 2) with `--force-prefers-reduced-motion`, so every springs.ts preset is its instant variant and tokens.css collapses the CSS transitions. Chrome's own `--screenshot` was the flaky half: under `--virtual-time-budget` one busy frame in three shot before the thinking strip's entry spring had advanced, and with instant transitions the portaled popover's layer had not rastered (an empty popover box, a missing row). Motion is the dev build's eyeball, never a still's.
- 2026-09-05 · **The harness matrix is seven states** (`answer` `empty` `busy` `picker` `failure` `disconnected` `small`, 42 frames): `disconnected` and `small` are the composer's two exceptions the sketch does not draw (no session: textarea disabled, pill and starters dimmed; the one pill that wears a badge), each one prop away from the configured empty state. The fixture's trace mirrors the loop's shape (one `turn` row with the whole raw text, then the tools), carries a fence and an `Assumptions:` line the display strip must remove, and a third five-word assumption so the chip row wraps at the floor (DESIGN rule 9: real long content).
- 2026-09-05 · **Follow-up and starter chips wear `ArrowRight`, not the sketch's `?` glyph**: each label ends in a question mark, so a leading `?` doubles it; `→` is the house go-mark (ImportWizard's step arrow, WRITING's `Open Referenced orders →`) and names the action the chip performs when it morphs into the echo. The sketch's `.chip.q` glyph is redrawn to match.
- 2026-09-05 · **The send button disables until the draft is non-empty** (`disabled={!busy && !canSend}`, `canSend = connected && !busy && draft.trim() !== ""`, AskPanel.tsx): the locked sketch draws the accent button live over an empty composer, but a live-looking button that does nothing is a false affordance (LESSONS 9, DESIGN rule 2), so the `--o-disabled` face is the truthful one and the sketch's empty composer is the side to redraw. The Stop face is never dimmed while a run is busy.

### Agent A1 · W2c (taste round 2, 2026-09-05)
- 2026-09-05 · **The answer footer's provenance mark is the connection's 8px dot, not a 14px avatar**: at 14px the avatar's icon read as noise ("so small it can just be a normal dot"); the titlebar already marks the connection with `.conn-dot` at 8px, so the footer uses the same mark. DESIGN rule 5 loses its 14px avatar exception; AGENT-UX §2 item 9 and §9 say dot.
- 2026-09-05 · **SQL surfaces show the finished statement**: the expanded SQL row, `Copy SQL` and `Open in Tab` carry the same text, the statement through the user's format preset (`formatSqlText`, the editor's lazy sql-formatter path) and ending in exactly one `;`. The stored and gated SQL stays bare (the gate classifies it; `extractSql` strips semicolons on purpose). Found: the model's one-liner wrapped mid-clause at 320, and a pasted statement did not end.
- 2026-09-05 · **The composer textarea paints no focus ring of its own**: tokens.css rings every `textarea:focus-visible` (specificity beats `.ask-ta`), which doubled the box's focus-within border. `.ask-ta:focus-visible { outline: none }`; the box is the one focus face (DESIGN rule 2).
- 2026-09-05 · **The trace drawer sits at z-index 10 and its step bodies span the list**: the grid's sticky header and gutter (grid.css z-index up to 9) painted through the drawer; the 64px body indent starved a 320 pane.
- 2026-09-05 · **Assumption chips toggle a pending set; one pill runs it**: a chip click flips the chip's WANTED state in `useAgent.pending[exchangeId]` and runs nothing; the chip renders the wanted state (`aria-pressed`); one floating pill over the composer (`RetryPill`, the Action button species in pill shape, absolute inside `.ask-input`) applies every flip of the newest exchange with a pending set as one re-ask (`applyPending`). Faces `Retry Without Assumption` / `Retry Without Assumptions` / `Retry with Changes` (`retryLabel`; WRITING rule 1 lowercases `with`). Esc over an empty composer discards the set. Found: a click that fired a model run took control away from the user (round 2, finding 3).
- 2026-09-05 · **A cancelled retry restores the prior answer exactly**: `stashPrior` keeps the landed exchange (text, thinking, chips, answer, error) while a retry (`applyPending`, `fixIt`, `retry`) streams over it, the prior prose, grid, status and footer staying on screen under the new run's strip; a cancel puts the prior back as it was and keeps the pending set so the pill returns; any other verdict drops the prior and clears the set. Found: Stop after a chip click left only `cancelled` and the footer (finding 4).
- 2026-09-05 · **The pending set lives until a verdict**: it is not disarmed at `applyPending` and re-armed on cancel; it stays through the run (cleared on answered or failed, kept on cancel), so the flipped chip never snaps back mid-run and flips again on landing.
- 2026-09-05 · **The thinking strip scrolls sideways, scrollbar hidden, both edges fading from position**: `.fade-l` once older chips sit under the left edge, `.fade-r` while newer ones wait past the right; a vertical wheel over it scrolls it and is consumed only while it can still move; the newest chip is followed only while streaming and only until the user scrolls. Found: the faded older chips were unreachable (finding 5).
- 2026-09-05 · **Threads is a sheet over the thread, not a menu**: `ThreadsSheet` wears the trace drawer's species and geometry (inset below the header, z-index 10, slides from the right; `useAsk.threadsOpen`, `openThreads` / `closeThreads`, the two slide-overs never show together), header back · `Threads` · `Delete All…` (`.soft-danger`), one `.trow` per thread `title · relative time` (`threadTime`) with a delete revealed on hover and on the hot row; a listbox with `aria-activedescendant` whose `.hot` follows keyboard and pointer, ⌫ deletes through the danger confirm, ↩ opens, Esc closes, Tab wraps; focus returns to the header's Threads button. `deleteAllThreads` joins the agent store. Found: the dropdown was frustrating (finding 9).
- 2026-09-05 · **The mode swap animates with `spring.slide`** (`SLIDE`: stiffness 400, damping 40, mass 1, critically damped, no overshoot): Ask leaves and returns on the left, the Inspector on the right (`slideOffset`, ±32px), both holders mounted; the leaving holder keeps visibility at opacity 0 until its spring settles (`onAnimationComplete`, an 800ms timer as the net) and then leaves the tab order; a switch that also opens or closes the pane is instant. Reduced motion is the instant variant with no travel, the house rule that every preset collapses (finding 10).
- 2026-09-05 · **A result of one row and up to four columns renders as values, not a grid**: `ScalarResult`, one column = the value in the data register over its column name as caption, two to four = name over value, stacked; a value past `GLANCE_CHARS` (24) takes the long-value form. Everything else is the one grid species in readOnly mode, sized to the header plus up to six rows (`maxRows`) and scrolling inside for the rest, so the rows it promises clear its own horizontal scrollbar. Found: a one-row grid hid its only row behind the header and the scrollbar (finding 7).
- 2026-09-05 · **The trace header's summary prints its unit with a space**: `1 turn · 20.4 s · 1 probe` through `secondsText` (WRITING status register), no longer `20.4s`.
- 2026-09-05 · **`Fix It` carries its chord in its tooltip, not on its face**: `FIX_TIP` through `chordGlyphs("cmd+return")`, the `SEND_TIP` pattern; AGENT-UX §11 stands unamended (chords appear only in tooltips and the Shortcuts sheet). Closes the W2b gate's open item.
- 2026-09-05 · **The harness matrix is fifteen states**: answer · empty · busy · picker · failure · disconnected · small · pending · retry · strip · threads · scalar · kv · wide · trace; each builder's states live in `src/harness/fixtures.<builder>.ts` and are dispatched from `fixtures.ts`; transient chrome a state shows (picker, trace, sheet) is opened in the harness's post-mount effect, never seeded, because AskPanel's mount effect closes all three; `strip` parks at the top by default; `scripts/ask-frames.ts` runs Chrome with focus emulation so `:focus-visible` paints, and a bare run writes to `w2c-frames`, never over the W2b evidence.
- 2026-09-05 · **The re-ask carries every flip as a stated constraint**: `applyPending` is one run in the `toggleAssumption` shape, `Additional constraint(s): apply this assumption: X; do not apply this assumption: Y` appended to the question, and `applyFlips` reapplies the user's choice to the landed chips, so the re-run's own `Assumptions:` line never overrules a chip the user turned off (a chip the model dropped is kept, off, on purpose). `toggleAssumption` stays exported for callers and is wired to no click.
- 2026-09-05 · **The pill's geometry and motion**: absolute inside `.ask-input` at right `--sp-3`, bottom `calc(100% + --sp-2)`, 28px, `radius-pill`, one `shadow-pop`, z-index 10 (over the grid's sticky chrome; the trace drawer and the Threads sheet follow it in the DOM and win ties), never in the flow and never over the send button; it enters through `menuIn` (`spring.snappy`) and leaves as an opacity fade on the same spring, so no new preset. While a set is pending the scroller pads its end by the pill's footprint (`.ask-scroll[data-pill]`, LESSONS 8: floating chrome shrinks the viewport) so the footer's `Trace` link scrolls clear; pressing the pill runs `applyPending` and hands focus to the composer so ⌘. still reaches the pane.
- 2026-09-05 · **`runner` is agent.ts's loop seam**: `export const runner = { runAsk }` is what `runInto()` drives, so `agent-pending.test.ts` stands a scripted loop in it and drives the real `applyPending` → `runInto` → `cancel()` path. bun's `mock.module` was tried first and is process-global: it reached `loop.test.ts` in the same run.
- 2026-09-05 · **The sheet's keys act only where the list has focus**: ↩ and ⌫ fire only while the list or the sheet root is focused, so a focused Back or `Delete All…` keeps its own ↩; row delete buttons are `tabIndex -1` and ⌫ on the hot row is the keyboard route (DESIGN rule 8); ↑↓ wrap, Home/End jump, every ⌘/⌃/⌥ chord bubbles (LESSONS 10). On open, focus moves into the list synchronously in the effect (a deferred focus can land after the next keystroke); on close it returns through the trace's rAF so an escStack restore lands first and loses. No native `title` on the sheet's buttons (WKWebView); `aria-label` names them.
- 2026-09-05 · **The list's focus ring rides on the hot row**: `.threads .threads-list:focus-visible { outline: none }` with the same 2px accent ring inset (`outline-offset: -2px`) on `.trow.hot` in the same block (rule 2's replacement clause). Found in pixels: tokens.css's `[tabindex]:focus-visible` outranked `.threads-list` and the pane clipped the list's ring to a 2px accent line under the sheet header.
- 2026-09-05 · **`threads.css` repeats the `.trace` slide-over contract** (inset, transform, visibility delay, z-index: twelve lines) rather than sharing a class: TraceDrawer's CSS is fenced to its builder, so a shared `.pane-sheet` class needs an edit there; ledgered for the drawer's owner, and both files carry the same lines and the note.
- 2026-09-05 · **`threadTime` is the sheet's clock and the calendar picks the register**: today is a distance (`just now`, `12m ago`, `3h ago`, HistoryPanel's own form), the previous calendar day is `yesterday` however many hours ago (Notes, Finder), older is the date (`Sep 2`; `Sep 2, 2025` once the year differs). Pure with `now` injected; an invalid stamp renders nothing; a negative span clamps to `just now`; read once per opening so rows never re-tick under the pointer.
- 2026-09-05 · **`deleteAllThreads` captures the ids before its first await** (LESSONS 3), runs one `deleteThread` each in sequence (the session closes before its rows go), then `loadThreads` in a `finally` so a failure midway still shows the truth. A delete that fails in the sheet flashes `delete failed` through the copy cue toast (the export-failure precedent, LESSONS 9) and the row stays.
- 2026-09-05 · **The swap's bookkeeping reads the store's own change**: `useSidePane.subscribe((s, prev) => …)` sets `{ exiting: prev.mode, instant: !prev.open || !s.open }` inside the same synchronous notification, so the render that moves the mode already carries the right transition; no render-phase ref mutation. `spring.slide` measured: 55% of the travel at 100 ms, 87% at 183 ms, 95% at 262 ms, at rest by ~420 ms; the floor clamp (an Inspector at 240 becoming an Ask at 320) rides the card's CSS width transition alongside the spring with no fight (probed 240.8 → 315 → 320 while x ran).
- 2026-09-05 · **The readOnly grid measures its own scrollbar**: `maxRows` sizes the grid to header + min(view rows, maxRows) rows + `offsetHeight - clientHeight` of its scroller (0 overlay, 15 classic); `GRID_HEADER_H` and `gridRowHeight` leave Grid's exports and the Ask host passes `maxRows={6}`. Root cause of finding 7: a host-fixed slot cannot know the bar's thickness, and under classic scrollbars the bar ate the last promised row, then the vertical overflow it caused added a second bar that ate the fill. ResultsPane is untouched: both new style props are `undefined` there and `layoutWidths` IS `colWidths` by identity.
- 2026-09-05 · **A filled readOnly grid locks its horizontal overflow** (`overflow-x: hidden` while the fill is active): it fits by construction, yet the last header cell's resize handle hangs 3px past the edge and a rounded `clientWidth` can exceed the true width, and either alone was a phantom horizontal scrollbar under classic bars (rows3 repro: scrollWidth 294 in a 292 viewport) or a sideways wobble under overlay ones. Content wider than the slot still scrolls as everywhere else.
- 2026-09-05 · **The trace drawer is titled `Trace`** (the footer link's word; `How did it get this?` goes, AGENT-UX §11), its one qualifier the run's `1 turn · 20.4 s` (rule 12; `· 1 probe` went under rule 11, the TOOL rows already say it); the verdict row prints no ms (its ms is the run's total and the header states that once, rule 14); the follow-ups call is a `model` row labelled `3 follow-ups` (the 56px kind column broke `FOLLOW-UPS` onto two lines); the note strip speaks only for Claude Code, `Claude Code’s harness prefix is not shown.`, one line at the floor, and no other provider gets a strip (rule 11: only the exception speaks). The sketch's long note and old title are the side to redraw.
- 2026-09-05 · **`PROBE_EXCHANGES` are repros, never gate states** (fixtures.anatomy.ts: one-wide, rows3, one-six): grid-slot repros for a builder's own CDP run; the maintainer's classic-scrollbar environment is reproduced by injecting `::-webkit-scrollbar` CSS on the grid scroller in headless Chrome, and a builder's frames for states the integrator has not wired come from reseeding the harness's stores through vite's own module URLs (the page's instances; a wrong instance is refused). scripts/ask-frames.ts stays the integrator's. The harness `threads` state (fixtures.shell.ts) lays five threads out relative to the clock so the column reads `3h ago · yesterday · Sep 2 · Aug 30 · Aug 28` on any day, and `shellAfterMount` moves the highlight one row down with a dispatched ArrowDown so the frame shows `.hot` and its delete beside `.active`, as the sketch draws.
- 2026-09-05 · **W2c taste gate: pass, no S1** (frames in the session scratchpad's `w2c-frames/`, ROADMAP_log W2c): the ten round-2 findings are each verified in a named frame or a CDP probe; three S3s are ledgered there (the kv stack's unused width at 560, the format preset's `AS MONTH` from HEAD, a fixture scroll that hides the chip under the left fade), none of them this wave's law.
- 2026-09-06 · **A chip still running when the turn is stopped shows a hollow ring, not a spinner**: after ⌘. / Stop the `peek gender` chips kept spinning forever under a `cancelled` line, claiming work that was not happening (LESSONS 9). ThinkingStrip derives it at render (`ms === null` and the exchange no longer streaming); no store field.

### Agent A1 · W2d (polish, 2026-09-06; picture: the "Discussion" rows of ~/projects/qwry-agent-lab/docs/ask-sketch-v2.html)
- 2026-09-06 · **The question echo is a tinted bubble at the right edge, the anatomy stays left** (the sketch's variant B; round 3, ask 1: the flat echo had "no distinction"): `.ans-echo` is `align-self: flex-end`, at most 86% wide, padding `--sp-2 --sp-3`, radius `16px 16px 6px 16px` (the 6px corner points at the answer), weight 450 at `--text-md` / 1.45, tier-1 text, selectable, `white-space: pre-wrap` so a ⇧↩ newline the user typed survives; exchanges sit 28px apart (`.ask-scroll` gap, was `--sp-5`). The `--accent-soft` fill and its 1px ring of 22% accent ride a `::before` under an `isolation: isolate` box, so they fade in with the section fade BEHIND words that are already there (`.ans > .ans-echo { animation: none }`: the words never fade, they travel). Reduced motion shows the bubble at once: tokens.css collapses every animation and the pseudo joins the `.ans > *` kill explicitly, the same face every other section wears under the setting. Found in pixels: `echo-*-{dark,light}[-top].png`, both themes read at 12% and 16% accent-soft.
- 2026-09-06 · **On send the draft's text lifts out of the composer into the bubble** (AskPanel `Lift`): ↩ renders `.ask-ghost`, the raw draft over the textarea glyph for glyph (same font, 20px lines, the textarea's width) inside the bubble's own padding and 1px ring, positioned in `.ask-input` at `--sp-2 / --sp-3` with `width: calc(100% - --sp-5)` so its border box IS the bubble's box at the composer, while `.ask-ta.ghosted` keeps the textarea's text unpainted (caret kept) so one set of glyphs shows. One rAF later `ask()` runs; motion snapshots the ghost as it unmounts and the echo mounting in the same commit resumes from it. Probed in a non-reduced-motion Chrome (`echo-lift.log`, `reframe-lift.log`): ghost box = textarea box + (13, 9) padding/ring at t=14 ms; at t=31 ms the ghost is gone and the echo sits on its spot at `translate3d(-67px, 85px)` with fill opacity 0, the draft empty and focus still in the textarea; the fill reaches 1 at ~280 ms and the transform is `none` by ~410 ms. No timer net: a hung `agentThreadCreate` shows as a stuck send, never a silent revert.
- 2026-09-06 · **The ghost and its echo pair through a nonce id, never the question**: `Lift.id = ask-lift:<uuid>` is the ghost's `layoutId`, and AskPanel hands it to the newest AnswerBlock as `liftId` in the render that mounts it (the echo takes `liftId ?? questionLayoutId(question)`), so an OLDER echo of the same text in the thread never joins the pairing: motion fixes a node's layoutId at mount, an echo that was latest once stays in its `ask-q:<q>` stack for life, the old `isLatest ? id : undefined` drop was inert against it, and Ask Differently re-sends the same text and used to collide (probed, `reframe-retype*.log`: the thread's latest question retyped word for word lifts clean). A chip pick keeps the question id and renders no ghost: the chip is the thing that travels (AGENT-UX §6). Older echoes carry neither `layoutId` nor `layout`.
- 2026-09-06 · **Shared-layout text is position-only**: the echo and the ghost set `layout="position"`, so the spring carries the words' top-left and never scales them (a one-liner lifting from a 344px textarea into a 298px bubble would otherwise be squeezed to 0.87× wide and stretched 1.85× tall on its first frame; a scaled glyph is a distorted glyph). The W2b follow-up chip → echo morph becomes a slide the same way: probed (`echo-pick.log`), the echo appears at the chip's top-left and springs 90px right / 44px down into the bubble; no ghost renders on a pick. `spring.layout` (PANEL) settles the ~110px lift in ~280 ms and the pick's slide in ~250 ms: no new preset, springs.ts untouched. AGENT-UX §6 and §10 say travel, not morph.
- 2026-09-06 · **The draft clears in the tick the exchange lands**: the lift effect subscribes to `useAgent` and, when the active thread's exchange count exceeds the count at send, clears the draft inside that same store notification, so React commits the append, the textarea's shrink and the scroller's bottom pin together and motion (which measures in a microtask after the commit) sees the echo where it will stay; a new thread's `agentThreadCreate` wait therefore shows the ghost over the unchanged composer, never over a collapsed one. Text typed after ↩ during that wait is kept (`typed.slice(raw.length)`); a lift whose `profileId` no longer matches the pane's is dropped unsent (LESSONS 4); `ask()` resolving without an append (refused: busy, connection changed) or rejecting (thread row failed) drops the ghost and leaves the draft in place; `sending` refuses a second ↩ while one is on its way so a new thread is never created twice. Reduced motion renders no ghost and clears synchronously as before.
- 2026-09-06 · **The thread scroller is a motion element with `layoutScroll` and no `layout` of its own**: motion's projection tree measures a layoutId descendant (the follow-up chips of every exchange, the echo) net of an ancestor's scrollTop only when that ancestor declares its scroll, and without it the bottom pin that follows a landing read as a layout change: every chip of every older exchange sprang up through its footer by the pinned distance (95.7px in the echo fixture) while its plain siblings jumped (DESIGN rule 2: an interaction that moves its own container). Probed (`reframe-lift.log`): 0 of 54 sampled frames after a send carry a transform on an older exchange's chip; the scroller itself never animates or wears a transform.
- 2026-09-06 · **The empty state is its own component and its two triples crossfade**: `Starters` (AskPanel.tsx) mounts `useStarters(profileId, snapshot, asked)` only while the thread has no exchanges (each show advances the connection's cursor by three, per the hook's contract) and renders the three inside a `motion.div` keyed on the questions under `AnimatePresence mode="popLayout"` on the app's one content-swap preset (`swapIn`: the breadcrumb's and the results' crossfade, DESIGN rule 6): the leaving row is parked absolutely where it stood inside `.ask-starters` (`position: relative` for that; `pointer-events: none` on the popped row, a chip on its way out asks nothing) while the arriving row takes the flow, so the swap is a fade in place, never a height change; a question that survives the rotation slides through its own layoutId. Probed (`reframe-xfade.log`): both rows at one rect, the old at opacity 0.86 falling, the new at 0.14 rising with `y` 4 → 0. A CSS `ans-in` on the row was the first attempt and lost: a CSS animation outranks the inline opacity motion writes. Reduced motion is the instant swap. Verified: `empty-392-dark-w2d.png`, `starters-*.png`.
- 2026-09-06 · **Starters rotate through a pool of twelve, model-generated once per connection and schema shape, the heuristic twelve as the instant fallback** (`src/agent/starterPool.ts`, `src/stores/starters.ts`, `src/ask/useStarters.ts`; round 3, ask 2: the starters never rotated): the empty state shows three, advances by three on every show (the cursor is per connection and persisted, so rotation survives launches), never a question already asked in any thread of the connection (the panel's set for the open thread plus every thread title from `useAgent.threads[profileId]`, compared through `questionKey`: trimmed, lowercased, whitespace collapsed, a title's trailing `…` dropped), and no shuffle button. The model's twelve and the heuristic twelve are never blended (one register per pool), and a generated pool is kept only at six or more questions (`STARTER_POOL_MIN`: a thinner reply cannot rotate and would replace twelve heuristics with a couple of lines). AGENT-UX §1's starter paragraph is the side amended.
- 2026-09-06 · **The starter call is followups.ts's shape and refuses `ownsLoop` providers for the same structural reason**: one tool-less `provider.chat` with its own system prompt, text collected, `[]` on error, abort, throw or an unaskable schema, trace-free (no exchange exists for a step to belong to). The claude -p adapter needs a thread with a live database session (`platform.tauri.ts` `mcpServer` throws without one) and the empty state has neither, so Claude Code connections keep the heuristic pool until the adapter grows a thread-free side call; ledgered as open. `STARTER_SYSTEM_PROMPT` and `starterMessage` live in `src/agent/starterPool.ts`, not prompt.ts: like `FOLLOWUP_SYSTEM_PROMPT` they must never touch the main loop's cached prefix or `PROMPT_VERSION`, and moving them beside the follow-up prompt would make prompt.ts import the pool's caps from a module that imports the prompt back, a cycle read at module evaluation. The prompt asks for plain words ("say orders, not order_v2"), trends, joins and distributions, no generic row count, nothing outside the listed tables.
- 2026-09-06 · **`schemaHash` is FNV-1a (32 bits, eight hex digits) over the sorted `schema.table(col,col,…)` lines of the snapshot**: stable across table order and machines, changed by any table or column added or renamed, untouched by row estimates and comments (they are not shape). It lives in `src/agent/starterPool.ts` so the store, the hook and the harness share one function.
- 2026-09-06 · **The starter prompt's schema summary is the whole schema's largest askable tables** (plain and partitioned parents only; partitions and inheritance children, system schemas, the migration ledger and LEGACY twins are out, read from the snapshot's `parent_oid` because `buildMeta` keeps a child whose parent is a plain table): at most 25 tables by row estimate, `name(cols, … +N)  -- ~rows; comment` per table plus the FK edges among them, kept under 16k characters (~4k tokens) by dropping the smallest tables to eight, then halving the column cap from 40 to eight, then dropping tables to one. The excluded names ride along as `banned` and the parser drops any question that names one as a whole identifier, so `order_v2` never bans "orders".
- 2026-09-06 · **`parseStarters` reads a JSON array or one question per line** (bullets, numbering, quotes stripped), keeps only complete questions of at most `STARTER_WORD_CAP` (12) words ending in `?`, drops lead-in lines, the generic row count in every costume (`rows … each/every/per/all … table`, `table … row count`, `how many tables`), any banned identifier and any repeat (case- and punctuation-insensitive), and caps at twelve: the same rules starters.test.ts pins for the heuristic pool.
- 2026-09-06 · **`useStarterPools` (`qwry.starters`) persists `{ pools, cursors }` only**, sanitising at hydrate (well-formed pools, finite cursors, never a throw); `ensurePool` is a no-op when the connection's pool carries the current hash, fires at most one generation per connection at a time (a module-level `inFlight` set, not state), swallows every failure, and `generator` is its test seam in agent.ts's `runner` shape. The cursor steps by `CURSOR_STEP` (3) and wraps at `CURSOR_WRAP` (27 720, the lcm of 1..12), so `cursor % n` stays continuous across the wrap for every pool size a triple can be cut from. A schema change while a generation is in flight lands a pool keyed to the older hash and regenerates on the next empty-state mount, not at once; `generatedAt` is stored and unused.
- 2026-09-06 · **The triple a mount shows is fixed at that mount**: `useStarters` reads the cursor into a per-connection ref in its mount effect and advances the store once; a re-render (the generated pool landing, the thread list arriving) recomputes the three from the same start, so the swap on landing is the pool's, never a cursor jump, and StrictMode's second effect pass is guarded by the ref. The hook resolves `modelChoice(profileId)` itself (its signature is fixed at profileId, snapshot, asked) and calls `ensurePool` whenever profile, snapshot or choice change.
- 2026-09-06 · **The heuristic pool grows to twelve through eight shapes over the pool tables and FK edges** (`starterPool` in `src/ask/starters.ts`): the trend, join and distribution the empty state always opened with stay its first triple (`starterQuestions` is `starterPool(...).slice(0, 3)`, its six tests untouched), then rounds of `How many <parents> have no <children>?`, `How many <X> were <verb> in the last 30 days?`, `Which <status> is most common among <X>?` and the opening three over the next table or edge, then the `20 most recently …` / `10 sample …` fills; deterministic per snapshot, a small schema says less rather than repeating itself. `SYSTEM_SCHEMAS`, `HOUSEKEEPING` and `LEGACY` moved to `src/agent/starterPool.ts` so both pools exclude the same relations.
- 2026-09-06 · **`qwrying…` is status text in the strip, never a chip** (round 3, ask 3: "no infinite transition something"): ThinkingStrip ends in `<span class="tchip-wait">qwrying…</span>` (`--text-sm`, base `--fg-muted`, a `--fg` band through `background-clip: text`, the sketch's `.qwrying`) while the exchange streams, no chip runs and no answer text has arrived; the caller says the last part through a new `waiting` prop (`exchange.text.length === 0`; omitted reads as no text yet), the strip knows the first two. Gone the instant text streams, a chip starts or the strip unmounts; the old lone `·` placeholder (`.ans-more`) is deleted. The scroll-follow treats the word like the newest chip (the layout effect keys on it), so it parks at the right edge past the chips at 320. Reduced motion is the CSS as it stands: parked at background-position 120% the band sits off the text, so the static word needs no second rule.
- 2026-09-06 · **The sweep is a Web Animation on the word, not a CSS keyframe and not a spring**: one `el.animate` per pass (background-position 120% → -40%) with the `--ease-std` token read from the computed style, in the cadence the maintainer set: slow pass 1800 → breath 300 → quick pass 650 → a second quick pass 40% of the time (150 between) → long breath 900, every span jittered ±20% (`SWEEP` in ThinkingStrip.tsx). No springs.ts preset was added: a tween over a gradient is a cadence, not a spring, and DESIGN rule 6's one easing is kept by reading the token; `prefersReducedMotion()` from springs.ts gates it live. Cancelled in the effect cleanup (`anim.cancel` + `clearTimeout`) so `waiting` turning false or an unmount stops it mid-pass.
- 2026-09-06 · **A probe chip names its subject**: `chipLabel` for `probe` is `probe <noun>` through `probeNoun(sqls)` (src/agent/probeNoun.ts, a tokenizer not a parser): the first column the FIRST statement's SELECT list measures (`min(sent_at), max(sent_at)` → `sent_at`), then the WHERE column when the list measures only `count(*)` (`WHERE created_at < signup_at` → `created_at`), then GROUP BY, then the first table (`SELECT count(*) FROM payment` → `payment`), else the bare `probe`. SELECT outranks WHERE because a min/max probe's filter is not its subject (`min(created_at) … WHERE NOT is_deleted` is about created_at). Strings, comments and numbers are skipped, a qualified name keeps its last segment, a quoted one its spelling, a word after AS or `::` is an alias or type, a word before `(` a function, and a function's parens switch no clause (`extract(year from x)` → x). Consecutive probes with different nouns no longer collapse into `probe ×2`; rendered mono like describe's ident through the existing splitLabel; the fixtures' probe chips read `probe sent_at` / `probe started_at`, as the loop now emits. Closes the W2b and W2c S3 (a bare `probe` beside chips that name their object).
- 2026-09-06 · **One curated Postgres dialect for every surface that formats SQL** (src/editor/sqlDialect.ts): sql-formatter's `postgresql` with the six interval fields YEAR MONTH DAY HOUR MINUTE SECOND (`IDENT_FIELDS`) removed from `reservedKeywords`, via `formatDialect`. Measured on 15.8.2: the list has 116 words and those six are the only identifier-shaped ones in it (NAME, TYPE, STATUS, VALUE … are not keywords there); `identifierCase` never reaches a word the tokenizer has already called a keyword, so the list is the one lever, and a casing-based post-pass was rejected because models write `as month` and `AS month` alike. As identifiers the six keep the model's spelling under every preset (`AS month` stays, `EXTRACT(YEAR …)` stays as written). `formatSqlText` (the Ask row, Copy SQL, Open in Tab) and `formatWithPreset` (the editor's ⇧⌘F and context menu) both format through `postgresDialect(mod)`, so one preset has one behaviour in the editor and in the Ask row (LESSONS 11); pinned by sqlDialect.test.ts on the sketch's statement. Closes the W2c S3 (`AS MONTH`).
- 2026-09-06 · **The one-row key-value stack flows as a row from a 480px slot**: ScalarResult wraps the `dl.ans-kv` in `div.ans-kv-slot` (`container-type: inline-size`; an element cannot query its own inline size); `.ans-kv` is a one-column grid until `@container (min-width: 480px)`, then `repeat(auto-fit, minmax(140px, 1fr))` with a 16px column gap, so three pairs sit in one row of 167px columns at 560 (`kv-wide-560-*.png`) and stack at 320 and 392 (`kv-wide-320-*`, `-392-*`). Content reflow, no new chrome (DESIGN rule 13). A value past ~20 mono characters (a timestamptz) wraps inside a 167px column at 560; whether such a result should fall back to the stack is the maintainer's call. Closes the W2c gate's first S3.
- 2026-09-06 · **The harness matrix is twenty-two states**: the fifteen plus echo · echo-long · starters · starters-fallback · qwrying · qwrying-trail · kv-wide (`fixtures.echo.ts`, `fixtures.starters.ts`, `fixtures.strip.ts`, dispatched from `fixtures.ts` and read by AskHarness; `stripSeed(state)` → `{ exchange, busy, phase }`, `echoExchangesFor(state)` an ARRAY, neither file importing from fixtures.ts so there is no cycle to order). `echo` is the sketch's discussion thread, `can you check the revenue in last month` (three chips, a four-row grid the status line is honest about, two assumptions, follow-ups) then `what is the total in INR for all that, converted` answered in prose with `sql: null, run: null`; a 640px card cannot hold both, so the bottom frame shows the second bubble over the composer, the `-top` frame the first and, at 560, both. `echo-long` is a 155-character question that wraps to five lines at 320 (the bubble's inner width is 227px there) and three at 560, over a one-row two-column result. `starters` seeds a twelve-question generated pool over the fixture schema at cursor 3, so the mount shows the SECOND triple and moves the cursor to 6; `starters-fallback` is the heuristic twelve at cursor 0; the harness seeds `useStarterPools` per state and resets it for every other state, so a pool persisted by an earlier harness page never leaks into a frame. `qwrying` is a question just sent, the word alone; `qwrying-trail` describe + peek landed, the word trailing; `kv-wide` one row × three columns, dates not timestamptz so no value wraps in a 167px column, parked at the top like `strip`. The fixture thread's title IS the first heuristic starter, so `empty` and `starters-fallback` show the heuristic pool from its second question: the "never a question already asked in any thread" rule at work, not a fixture defect. A bare `scripts/ask-frames.ts` run writes to `w2d-frames`.
- 2026-09-06 · **A builder reseeds the harness through the page's OWN store URL**: after any HMR round vite serves an invalidated importer chain under `?t=` URLs, so `import('/src/stores/agent.ts')` from a CDP evaluate creates a second instance and the seed is refused (`WRONG MODULE INSTANCE`); the driver finds the URL the page actually loaded in `performance.getEntriesByType('resource')` and imports that (scratchpad `w2d-frames.ts`). Frames of states the integrator has not wired come from that path, never from editing ask-frames.ts.
- 2026-09-06 · **W2d taste gate: pass, no S1** (frames in the session scratchpad's `w2d-frames/`, ROADMAP_log W2d): the four round-3 asks are each verified in a named frame or a CDP probe, the stills post-date the last source edit; six S3s are ledgered there, five of them the sketch's or a fixture's, one this wave's to weigh: on a chip PICK the picked question joins `asked`, FollowUps drops it, the row compacts by one chip height (~29px) and the layoutId chips below spring from 29px to 0 over ~300 ms while their plain siblings jump (`reframe-pick.log`: 25 of 43 sampled frames carry a transform on a non-picked chip; the send path carries none). Ordinary shared-layout behaviour on `spring.layout`, pre-existing since the chips got layoutId in W2; the fix, if the maintainer wants it, is a visibility-hidden placeholder in the picked chip's slot until the echo's layout animation completes, so the landing commit moves nothing but the traveller.
- 2026-09-06 · **The claude -p adapter has a thread-free side call, and the follow-up and starter-pool prompts go through it** (`Provider.sideChat`, optional; `providers/side.ts` `sideText` picks the door: `sideChat` for an `ownsLoop` provider, a tool-less `chat` for the rest, a refusal for an `ownsLoop` provider with neither; the three `ownsLoop` refusals in followups.ts, starterPool.ts and the store's `followUpsInto` are gone, closing W2d open item 3). The spawn is a fresh `claude -p` through the same `agent_claude_spawn` and env allowlist as the main path (no Rust change): `--tools ""`, `--strict-mcp-config` with NO `--mcp-config`, `--max-turns 1`, `--setting-sources ""`, `--system-prompt`, no `--allowedTools`, no `--session-id`/`--resume`, no `--include-partial-messages` (the reply is read whole from the assistant message, the result line's string as the fallback), the prompt on stdin. The strict flag stays against the brief's "none of the MCP flags" for a measured reason: without it the CLI loaded the user's own claude.ai MCP servers into the side call (`mcp_servers`: Slack pending, Sentry and Google Drive needs-auth), and a connected one would list its tools the way `mcp__qwry__*` rides along in the main path, since `--tools ""` drops only the built-ins; with it and no config, `mcp_servers: []`, `tools: []`. The init gate (`toollessInit`) requires no MCP server and refuses any listed tool, ours included, killing the child; a `Not logged in` result or exit still maps to `auth`; a spawn that rejects is held at once so nothing surfaces as an unhandled rejection. Live (2026-09-06, haiku 4.5, signed in, no API key): the raw probe answered in 9.8 s wall (14.1 s on the first try, 945 thinking tokens), 459 in / 756 out; through the adapter with the real follow-up prompt (scratchpad `side-live.ts`, a node child-process Platform) 24.9 s wall, 519 in / 2374 out, the reply parsed to three questions and `mcpServer` never asked for. The call is slow for a courtesy, since the model thinks before three lines; it runs off the answer's path, so nothing waits on it.
- 2026-09-06 · **The answer is the last block WITH PROSE, and a SQL-only closing block rides behind it**: a model that wrote its answer, ran once more and closed with only the fence lost its prose (the slot went blank, then the table and SQL landed). `loop.ts` keeps `lastProse` across tool calls and turns and answers with `${prose}\n\n${closing}` when the closing block strips to nothing, so the display shows the prose while the fence and the Assumptions line still parse. The store stops blanking the slot on a tool call (`textStale`: the next block's first delta replaces the old prose).
- 2026-09-06 · **The strip re-arms its follow at the right edge**: scrolling the strip away from the newest chip hands the position to the user; scrolling back to the edge (wheel, trackpad, or the strip's own measure on scroll) hands it back, so `qwrying…` and new chips stay in view again. Found: after one swipe left the word ran off to the right for the rest of the stream.
- 2026-09-06 · **`peek_values` falls back to a bounded sample when the exact DISTINCT times out**: on `videos_store` every peek died at the 5s cap (a full scan per column) and the probe batch with it, so the model lost the turn to red chips. The exact query now gets 2s; on `statement timeout` the peek re-runs as `SELECT DISTINCT col FROM (SELECT col FROM t TABLESAMPLE SYSTEM (0.5) WHERE col IS NOT NULL LIMIT 20000) s` under 5s and the result carries `sampled: true`, which `formatPeek` turns into `(sampled from a large table: other values may exist)` so the model never treats a sample as the whole set. The eval's node twin mirrors it. Also: the `run_sql.sql` description now says "never prose, never a markdown fence" after Haiku sent its answer text as the statement.

### Agent A1 · W4 (message actions and edit, 2026-09-06; picture: the "W4 · message actions and jumping back" rows of ~/projects/qwry-agent-lab/docs/ask-sketch-v2.html)
- 2026-09-06 · **The question bubble caps at `calc(100% - 76px)`, the cluster's room at every width** (amends W2d's `86%`): 76 is three 18px icon buttons 4px apart (62), the 8px gap to the bubble and 6px to spare; measured 218px at 320, 290 at 392, 458 at 560 (`w4-bubble-geometry.json`), 6.0px spare in every frame, and 86% wins nowhere in the pane's range, so the cap is written as what it is (the W4 gate's S3) and the trio never pushes a bubble down a line: a rule-2 stable-chrome reserve, the hover reveal never moves the bubble. Its cost, measured against the W2d frame set: the 65- and 48-character questions take one more line at 320 (254 → 218px) and everything below shifts by a fraction of a line, nothing else moved (`w4-diff-answer-320-dark-{w2d,w4}.png`). Sketch: the W4 `actions` row at 320 · 392 · 560.
- 2026-09-06 · **Copy · Restart · Jump Back sit left of the bubble in `.ans-echo-row`, bottom-aligned with its last line, on the strip chips' CSS register, no spring**: `.ans-echo-acts` (three `.iconbtn.iconbtn-sm`, lucide glyphs at `--icon-sm`) rests at opacity 0 and `translateX(4px)` and reveals over `--dur-quick` / `--ease-std` on `.ans-echo-row:has(.ans-echo:hover)`, its own hover, `:focus-within` (opacity, never visibility: the trio stays in the tab order, ROADMAP distilled 2026-08-23) and `.ans[data-hot]` (the harness's forced face); the row's empty left half reveals nothing and a pointer crossing an answer flashes nothing (DESIGN 8: the hover target is the bubble row, never the exchange). The 8px gap is the cluster's own `padding-right`, not the row's `gap`, so a pointer crossing from bubble to buttons never leaves the hover zone; its `padding-bottom` is the bubble's padding plus its ring (9px, `/* optical */`), so the 18px buttons centre on the 18.85px last line (cluster bottom = bubble bottom in every frame, 0.0px). Jump Back is nearest the bubble: it duplicates the bubble's own click, so a stray pointer meets the harmless Copy first. Tooltips are the control names in Title Case; Copy goes through `copyCue`, the app's one copy path, cue included (LESSONS 9).
- 2026-09-06 · **Restart's glyph is lucide `Repeat`, not `RotateCcw`** (the W4 gate's S2; the sketch drew RotateCcw): at 12px the counterclockwise arc is the header's Threads glyph (`History`) minus its clock hands, two verbs on one arc in one pane; the judge's `RefreshCw` was rejected because it is the app's refresh glyph (TableBrowser, UpdateToast, Palette) and would lend Restart a third meaning. Copy is `Copy`, Jump Back `CornerUpLeft`, as the sketch drew them.
- 2026-09-06 · **Restart and Jump Back disable while the thread is busy or the pane is in edit mode; Copy stays live; nothing hides** (`held` in EchoActions): the base `button:disabled` face (`--o-disabled`, the one value) does the work, no re-authoring; measured 0.55 on both in every `actions-busy` frame beside a live Copy at 1 and the Stop face in the composer (rule 2's matrix in a still). A click on a bubble while busy is refused before it arms (probed: `edit` stays null).
- 2026-09-06 · **Restart on an older bubble asks through `confirmDanger`, the DangerModal untouched, with `Restart from Here?` · `The 2 questions after this one and their answers will be deleted.` · `Delete 2 Questions`, singular `The question after this one and its answer will be deleted.` · `Delete 1 Question`** (`restartConfirmText` in EchoActions.tsx): the gate is `later.some(e => e.answer !== null || e.error !== null)` over the exchanges after it, the count is every later exchange (all of them go), the title is Title Case (WRITING's dialog-title register, the house question form of `Delete This Thread?`), the detail is one sentence naming the count and both halves of the loss (the later questions go with their answers; the restarted exchange's own answer is replaced, which is the point, not the collateral) and wears the modal's mono `<pre>` as every confirm does, and the button is verb + object, `Delete` not `Discard` because the rows are persisted like a thread's (WRITING terminology: discard is for drafts and staged edits). Then `useAgent.restartFrom(id)`; on the newest exchange nothing is asked, `restartFrom` is the retry shape. Probed: the danger store's prompt carries exactly these strings for the first and second bubble of the three-exchange fixture. Sketch: `restart-confirm` at 392, both themes.
- 2026-09-06 · **Edit mode is variant B: the edited bubble leaves with its words and the later bubbles stand as the dimmed stack**: on Jump Back or a plain click the text travels into the composer (the lift reversed), the edited exchange unmounts entirely (AnswerBlock's `isEditSource` renders nothing once its anatomy has faded: the words are in the composer, the box goes), every later exchange (`folded`, by index after the edit source in `useAgent.exchanges[threadId]`) renders its echo row alone, `.ans-echo.folded` at `var(--o-disabled)`, no cluster rendered, no click (`armEdit` refuses), the words still selectable, and the exchanges before it keep their anatomy with Restart and Jump Back held. Variant A (a hairline ring at --o-disabled holding the slot) was drawn beside it and rejected: an empty bubble is chrome around nothing (the one-cell table rule, AGENT-UX §2 item 4), a second after the travel it reads as a bubble that lost its text, and B's return on Esc is the send lift the app already has (the bubble re-forms as the words arrive, fill fading in) where A would need a third motion. B's one cost, accepted: editing the latest exchange leaves no thread-side mark but the answer's absence; the composer holding a question the user clicked into is the mode's sign, and Esc is free. Sketch: the `edit` row at 320 · 392 · 560, the A / B pair at 392, `edit-latest` at 392.
- 2026-09-06 · **The bubble is a div of selectable text, never a button**: a plain click (`getSelection().isCollapsed`) is Jump Back, a drag-select stays a select, no `tabIndex`, no `role`; its keyboard route is the cluster (DESIGN 8's inverse: a bubble of text wears no control costume). `cursor: default` on the live bubble (an I-beam would promise a caret the click never places) and the ring steps from 22% to 40% accent on the bubble's hover over `--dur-quick` (a state, not chrome; the click answers on the thing clicked): the W4 gate's optional S3, two CSS lines to strike if the maintainer prefers the bubble silent.
- 2026-09-06 · **A bubble joins the edit travel by remounting under `editLiftId(exchangeId)` one frame before edit begins** (AnswerBlock `arm`): motion fixes a node's layoutId at mount and the bubble mounted under the send lift's nonce or a chip pick's `ask-q:` id (or none), so a click sets `arm = 'leaving'`, which re-keys the bubble under `ask-edit:<id>` with `layout="position"` for ONE frame (`.ans-echo.arming::before { animation: none }` so the fill does not blink through the section fade), and the next rAF calls `beginEdit`; the exchange then unmounts as the composer's ghost mounts under the same id, so motion carries the words from the bubble's measured box. Once the mode has held it the bubble stays `'back'` under the edit id for life: Esc remounts it in the commit the ghost leaves (the words travel home, the `::before` fade plays behind them) and a later edit of the same bubble needs no arming frame (probed: the second click leaves within one frame). The orchestrator's `key={isEditSource ? 'edit' : 'echo'}` was not enough: a fresh key in the same commit as the ghost gives motion no snapshot to travel from.
- 2026-09-06 · **Edit mode's travel is the send lift run backwards and the composer owns both ends** (AskPanel `travel`, ask.css `.ask-ghost[data-edit]`): `in` mounts under the bubble's edit id in the commit the bubble unmounts, so motion pairs the two and the words spring from the bubble's box to the textarea's text origin on `spring.layout`, position only, while the bubble's fill and 22% ring, carried on the ghost's `::before`, fade out on `ans-out` at `--dur-slow`; the textarea holds the same text unpainted (`.ghosted`) until `onLayoutAnimationComplete`, with a 600 ms net for a mode entered without the bubble's arming frame (the fixture path); `out` stands one bare frame over the composer on Esc as the box the returning bubble springs from, its own fill fading in on `ans-in`. Probed under full motion at 392 (`w4-edit-probe.json`, `w4-int-probe.json`): the ghost leaves the bubble's box with fill 1, its fill is gone by 265 ms and its transform by ~415; on Esc the bubble returns from the composer's box to its own with the fill fading 0 → 1 on the same clock. Reduced motion renders no ghost either way: the textarea holds the text and the stack stands. No preset was added.
- 2026-09-06 · **The fold is one gesture in the app's two registers: what stays travels on `spring.layout`, what goes fades where it stood on `--dur-slow`** (AnswerBlock `Body`, ask.css's edit-mode section): every bubble is a layout node whose `layoutDependency` is the mode, so a later exchange's bubble springs into the stack on the fold and home on the unfold and moves with its plain siblings on any other re-render (a bubble measured every render would spring on a chip pick while its anatomy jumped: the W2d gate's S3, twice over); the anatomy under a bubble is one node (`.ans-body`) handed to `AnimatePresence popLayout`, which parks it against the scroller (`position: relative` on `.ask-scroll`) while `data-leaving` runs the section fade backwards (`ans-out`), the stack settling over it, and the node asks to go when the fade ends; the edited article is `display: contents` for that fade so the flow reads as if it were already gone, then unmounts. `clampToFlow` applies the browser's own scrollTop clamp at the click, to the end of what stays in the flow, instead of letting it snap at the end of a settled spring. Reduced motion removes the anatomy in a layout effect before the frame paints and the browser clamps. Closes the integrator's two ledgered items (the anatomy left in the commit, the folded bubble jumped).
- 2026-09-06 · **Neighbouring folded bubbles close from the thread's 28px to `--sp-2`** (`.ask-scroll > .ans + .ans` and its `:has(.folded) + :has(.folded)` rule, ask.css; the 28px moved from the scroller's `gap` to the exchanges' `margin-top` for this, with no transition of its own: the spring carries the bubbles to the closed geometry, and a margin easing beside it would be a second motion for one move): 28px is air between a bubble at the right and an answer at the left, and between two bubbles with no anatomy it is a hole, so the exchanges a send would delete read as one stack, not as scattered questions. Amends AGENT-UX §2 item 1's "exchanges sit 28px apart" for edit mode only. Verified in pixels through the `edit-stack` harness state (fixtures.edit.ts: a four-exchange thread edited at the second, so the 91-character third and a 41-character fourth fold together): `edit-stack-{320,392,560}-{dark,light}.png`, gaps 28 · 8 at every width, four lines over two at 320, two over one at 560. The `edit` state folds one exchange and could never show the rule (review finding, DESIGN rule 9).
- 2026-09-06 · **The folded bubble's dim is `!important`** (`.ans-echo.folded { opacity: var(--o-disabled) !important }`, the W2c chip precedent): the newest bubble carries a layoutId and motion leaves `opacity: 1` inline on it after the lift, so under normal motion a click on an older bubble folded the latest exchange at full strength beside its dimmed neighbours; reduced motion clears the inline value, which is why every round-0 still (shot under `--force-prefers-reduced-motion`) read 0.55 and hid it. Found when the `edit-stack` frames' fourth bubble computed 1 against the third's 0.55, then confirmed on the product's own path in a non-reduced Chrome (scratchpad `w4-fold-opacity-probe.ts`: inline `opacity: 1;` after the click, computed 1 before the fix, 0.55 after with the inline value still there). LESSONS 8 extended: a motion-written style hides from reduced-motion stills the way alpha hides from layout probes.
- 2026-09-06 · **No dialog on a send from edit mode: the fold is the preview and ↩ is the commit** (the semantics the maintainer locked, the sketch's Calls list): ↩ from the mode cuts the thread at the edited exchange, inclusive, appdb rows too (`askFrom`: the truncation, then the ask), and asks the composer's text in its place; the dimmed stack already showed exactly what goes, one bubble per exchange at `--o-disabled`, and Esc or an emptied draft was the way back the whole time, so a confirm here would ask the user to approve what they are looking at. Restart on an older bubble is the other case and keeps its `confirmDanger`: nothing on screen previews that loss. Reduced motion changes nothing: the stack stands, the send commits.
- 2026-09-06 · **Edit mode is `useAsk`'s, the truncation is `useAgent`'s**: `edit: { profileId, threadId, exchangeId, question }` with `beginEdit` (sets the mode, writes that CONNECTION's draft, bumps `focusSeq`) and `endEdit` (clears the mode only, so the caller decides the draft: a cancel travels the words back into the bubble, a send has already spent them); `setDraftFor` on another connection cancels the mode and leaves the draft where it was typed (LESSONS 4). Nothing about the fold, the travel or the appdb cut lives in the chrome store. The composer takes the caret after the last word one frame after `beginEdit` (the `focusSeq` rAF; `setSelectionRange` runs only when focus is arriving, so a focus request into a composer already focused moves no caret).
- 2026-09-06 · **Edit mode ends on its own when its exchange leaves the thread on screen** (a cut, a thread switch, New Thread; the connection switch ends it in the store's `setDraftFor`), the draft left where it was typed (LESSONS 4). Esc and the draft emptied are the two deliberate exits and the only ones that clear the draft: `cancelEdit` clears it and ends the mode in one commit, so the exchange remounts as the ghost leaves and the fold unfolds in the same frame. The Esc rung sits after the sheet, trace and picker rungs and before the pending set's on the pane's ladder; the retry pill hides with the anatomy while the mode is on, since a set on a folded exchange has nothing on screen to retry.
- 2026-09-06 · **A landing is read against the cut, never by count alone** (AskPanel `arrived`): a send from edit mode lifts with `count` = the edited exchange's index, and before the cut the thread is LONGER than that while still holding the edited exchange at it, so `exchanges.length > count` read as a landing on the first render, dropped the ghost before the words moved and left the ask to a race with the lift effect's cleanup (the send probe: no ghost, one ↩ in two lost). `arrived` = one more exchange than `count` AND, from edit mode, a different exchange at the cut; the same test gates the store subscription that clears the draft in the landing tick, and `cutFrom` keeps a thread the cut emptied on its scroller so the starters never flash between the cut and the landing. Probed after the fix (`w4-edit-send-probe.json`): the ghost mounts on ↩, the cut lands (3 → 2), the new exchange stands at index 1 where the edited one stood, the draft clears in that tick, and the new bubble springs from the composer (y=556) to its place (y=472).
- 2026-09-06 · **The thread scroller keeps its end when the composer's height changes**: the composer's growth shrinks the scroller from below and hides the last footer behind it (measured: the 91-character question brought back for editing at 320 wraps to three lines and took 40px of thread with it, `edit-latest-320-*-build.png`), so the textarea's height effect re-pins a scroller that was at its end, instantly (never animated), and leaves one the user scrolled up where it is. Not scoped to edit mode: a ⇧↩ newline typed at the end of a thread keeps the footer in view the same way.
- 2026-09-06 · **A cut re-mints the thread's provider session; the thread id stays the row's identity**: appdb migration v7 adds `agent_threads.session_key TEXT` (SCHEMA_VERSION 7, `add_column_if_missing`, NULL and no backfill because both selects read `COALESCE(session_key, id)`), `ThreadRef` gains `session` beside `id`, and claudecode.ts passes `thread.session ?? thread.id` to `--session-id` / `--resume` while `platform.mcpServer(thread.id)` still names the thread's own MCP session and its appdb rows. The reason is provider memory: `claude -p` resumes a session that remembers the turns a cut deleted and has no rewind, so the cut writes a fresh uuid (`agent_thread_session_set`) and the store forgets `resumed` for that thread, making the next call a first call again. The TS mirrors (`AgentThread.session_key`, `Thread.sessionKey`) are optional and mean exactly what the column means: absent = the thread id, which is every thread until its first cut.
- 2026-09-06 · **The first call after a cut carries a compact replay of the KEPT exchanges as a prefix of the user message, never in the system prompt**: `replayOf()` writes `Earlier in this thread:` then one `Q:` / `SQL: <final sql or none>` / `A: <first sentence>` block per kept exchange, the answer taken through `answerText()` first so the SQL fence and the `Assumptions:` line stay in their own slots (DESIGN rule 14), capped at 2000 characters with the oldest blocks dropped first; `loop.ts withReplay()` prefixes it to the user message on both the hybrid and the small path, so a stateless provider gets the same one behaviour per cut. PROMPT_VERSION and the eval's prompt bytes do not move: the eval passes no thread, and loop.test pins that no-thread and thread-without-replay send byte-identical messages and systems. The exchange being asked is never in its own replay.
- 2026-09-06 · **A cut's replay is carried once but not lost by a failure**: `cutPending` is cleared when the run LANDS, next to `resumed.add`, not when the provider call starts, because a cancelled or errored first attempt after a cut is the one case where the new session has still never heard the kept exchanges; clearing at the call site would drop that thread's history for good.
- 2026-09-06 · **`agent_thread_truncate(thread_id, turn_ids)` cuts NAMED rows, answers first, then turns, in ONE rusqlite transaction, and the store names them** (replaces the wave's first shape, `keep_turns` = kept exchanges × 2, which the order probe broke): neither `idx` nor write order is a boundary, because an exchange whose run failed before `persist()` owns no rows at all and a Retry on it writes its pair after its own successors' (`w4-order-probe.log`: q2 failed, q3 landed, q2 retried, and a count-based cut at q3 kept q3's rows and deleted q2's), so `Exchange.userTurnId` joins `turnId` (read from the row on reload, recorded by `persist()` before the answer's own write so a failure there costs the answer row and never a doubled question), `rowsOf` lists the two, and the cut sends that list, `thread_id` scoping it so an id from anywhere else deletes nothing. The answers go first inside the transaction because they key on turns that must still be there; a half-applied cut would leave answer rows pointing at nothing. rusqlite: turns 0..5 with an answer each, four named → two turns and two answers remain, no orphans, the other thread untouched; rows written out of order list in `idx` order and survive their successor's cut. Store: a rebuilt thread's cut names `[5, 6]`, not twice its position (agent-truncate.test).
- 2026-09-06 · **A re-run rewrites its assistant turn in place** (`agent_turn_update` / `AgentTurnPatch`; thread, `idx` and role never move): Restart, Fix It and a chip toggle answer the same question in the same place, and before this the old prose stayed beside the new answer row so a reload paired them and showed an answer nobody was given (LESSONS 1). `persistUserTurn` is gone from `RunArgs`: `persist()` writes the user turn when `userTurnId` is null and returns without writing when a cut or a closed thread took the exchange off screen while the answer was in the air.
- 2026-09-06 · **The store cuts, the UI confirms**: `askFrom` truncates inclusive and asks in place with no dialog (the fold is the preview and ↩ is the commit; the send is checked against the thread it was aimed at after the cut's round trip, LESSONS 3), `restartFrom` sends the LATEST exchange to the existing retry shape (nothing is deleted, `prior` restores on cancel, no confirm) and an older one through an exclusive cut, a rearm and a re-run of its own question; `confirmDanger` is never called from `useAgent`, because only the UI knows what the user is looking at. A cut that would remove nothing (Restart on the newest exchange) is not a cut: the session still remembers exactly what is on screen and stands.
- 2026-09-06 · **The harness gains `actions` · `actions-latest` · `actions-busy`** (`src/harness/fixtures.actions.ts`): the sketch's three-exchange discussion thread (fixtures.echo's two, then a 91-character question over `217` · `first_time_customers`, `1 row · 188.2 ms`, `probe created_at` · `run`, `New = No Earlier Paid Order`, `1 turn · 33.1 s · Haiku 4.5`); the hot face is `data-hot` stamped on the hot exchange's `[data-exchange]` article by `actionsAfterMount(state)` after mount (no production prop for a harness costume), which for `actions` also parks the scroller at that exchange one padding in, the sketch's rest (`scroll=top` re-parks after it and wins); `actions-busy` (the gate's S3 #5) streams the third exchange (probe landed, run running, no text) with the second bubble hot. The third exchange's SQL keeps the sketch's first line (`SELECT COUNT(*) AS first_time_customers`) over a `SELECT DISTINCT o.user_id` subquery so the count is of customers, not orders. The discussion states' pill reads the thread's own Haiku 4.5 (`choiceFor`), as the footers do; the W2d `echo` frames are superseded on that one glyph run.
- 2026-09-06 · **The edit fixtures enter the mode through the store's own door after mount** (`fixtures.edit.ts`: `editSeed(state)` over the actions builder's three-exchange thread, `editAfterMount(state)` → `beginEdit` in the harness's post-mount rAF, after `setDraftFor` has run, or the store would clear an edit seeded against another connection): the frame shows what the product shows (the draft written, focus requested, the caret after the last word one frame after the ready mark, inside the script's settle), never a seeded `edit` object the pane's mount would have to honour. `edit` folds at the second exchange (its question in the composer, the third dimmed alone), `edit-latest` at the newest (the first two whole, the composer holding the 91-character question: three lines at 320, two at 392 and 560), `edit-stack` at the second of four (the closed gap's one still). A folded thread's shim answers `agent_thread_truncate` and `agent_thread_session_set` with nothing: a cut in the harness moves the fixture thread on screen and has no appdb behind it.
- 2026-09-06 · **The sketch's W4 rows draw the discussion thread with the build's live anatomy**: the first exchange gets its four-row currency grid, the fixture's interpreting prose and three follow-ups, the second the loop's chip label (`describe erp_order_cost_snapshot, erp_fx_rate`, resting at the newest chip under the left fade at 320 and 392), and a third exchange, a 91-character question over a scalar (`217` · `first_time_customers`, `1 row · 188.2 ms`, `probe created_at` · `run`, `New = No Earlier Paid Order`), so the frames the gate compares against are no longer a status line over nothing (DESIGN rule 9, the W2d gate's sketch S3s). The Discussion rows above them stay byte-identical. Where sketch and build still differ it is data, not chrome: the second exchange's follow-ups (the fixture's three, one of them dropped by `asked` because the thread went on to ask it), the provenance dot's colour (the harness profile's green), and Restart's glyph (decided above).
- 2026-09-06 · **W4 taste gate: the pixels pass, the wave stands uncommitted on one store S1** (frames in the session scratchpad's `w4-frames/`, ROADMAP_log W4): every still of the six new states post-dates the last source edit, and the trio, the cap, the fold's stack, the confirm's three strings and the composer's re-pin are each verified in a named frame or a probe; the reviewer's S3 (the fixture header's line count) is ledgered. The S1 is not chrome: `persist()` places a new turn pair at `idx = position × 2`, and a reloaded thread with an idx gap (a question that failed before persist and was never retried) compacts positions without renumbering rows, so the next ask writes over the last kept exchange's slots and the following reload hangs its answer on the wrong question (LESSONS 1; the collision predates the wave in the running-count form, the cut's orphan-then-re-pair consequence is the wave's). A data-loss S1 means no commit: the fix (each exchange carries its rows' `idx`, a new pair is placed after its neighbour's, the tail is shifted in the same transaction when no slot is free) and a store test that replays the reload are owed before the branch moves.
- 2026-09-06 · **`idx` is allocated between NEIGHBOURS, never from the exchange's position on screen, and the tail is shifted first when the next exchange leaves no room** (`Exchange.idx` + `allocateIdx` in the store; `agent_turns_shift(thread_id, from_idx, by)` = `UPDATE agent_turns SET idx = idx + ?3 WHERE thread_id = ?1 AND idx >= ?2`, appdb → agent.rs → lib.rs → `agentTurnsShift` in commands.ts → the harness shim's no-op list): `Exchange.idx` is the USER turn's slot and its answer's is `idx + 1`, read from `turn.idx` on reload, recorded by `persist()` the moment it is allocated and null until then (a harness fixture's absent means null); a new pair takes `prev.idx + 2` for the nearest EARLIER exchange that owns rows and 0 when there is none, and when the nearest LATER one sits under `base + 2` the tail moves up by `base + 2 - next.idx` in appdb and on every in-state exchange holding a moved row before either row is written. This closes the W4 gate's S1: `idx = at * 2` was the exchange's position doubled, and a reloaded thread compacts over the gap a question that failed before persisting leaves while its rows keep the idx they were written with, so the next ask wrote over the last kept exchange's slots and the reload after that hung two answers on one question and left another with none, a cut then stranding the orphan to re-pair with someone else (LESSONS 1; AGENT-SPEC §9's `idx` sentence is amended with it). Shift then insert as two IPC calls rather than one transaction, accepted and recorded: a failure between them leaves a hole in the indices, which nothing reads, where a shared slot costs an answer its question. Probed on the store's own seams (agent-truncate.test, the mock now keeping the rows it is handed so a test can relaunch over what it wrote): the reviewer's replay — rows 0,1 + 4,5 → reload → ask → the pair at 6,7 → a second reload reads three exchanges each with its own answer; a Retry between two neighbours — shift from 2 by 2, then the pair at 2,3, the later exchange's in-state idx at 4; a first exchange at 0,1. rusqlite: 0,1,4,5 → shift from 4 by 2 → 0,1,6,7, same rows, answers intact, another thread untouched.

### Agent A1 · W5 (rich answer text, prompt v3 and the presentation score, 2026-09-06; picture: the "W5 · rich answer text" rows of ~/projects/qwry-agent-lab/docs/ask-sketch-v2.html)
- 2026-09-06 · **The answer slot is a block grammar, not a display strip** (`parseBlocks` in `src/agent/display.ts` replaces `renderInline`; `answerText` stays the plain projection loop.ts, the thread list and the eval read): Block = `p` · `lead` · `list` · `quote` · `code` · `table`; a SQL fence (`sql` / `postgres` / `postgresql` / `pgsql`, or a langless fence whose first word is a statement) never renders because the SQL row has it, the `Assumptions:` span strips as before, an image renders as nothing; pure, no React, 1000 parses of a 4k answer in 28.7 ms against a 50 ms test (237.8 ms before `unglue` was hand-scanned instead of a lookbehind). The renderer (`src/ask/AnswerText.tsx`) owns the elements and imports the parser's `Block` and `InlineToken`; nothing else in `src/` draws answer markdown, and the corpus (15 real answers under `src/agent/__tests__/fixtures/answers/`) is where a new shape a bench turns up gets pinned.
- 2026-09-06 · **Streaming holds the SETTLED-blocks invariant, one notch under the brief's wording** (display.ts tests; `w5-stream-probe.log`): every block but the LAST is settled (`parse(prefix).slice(0, -1)` is a deep prefix of the whole parse), and the last may still become another kind, because two shapes cannot be classified from their first line alone: a complete pipe line (a separator on the NEXT line makes it a table header, anything else prose) and a langless fence (SQL sniffed from its first word). Holding a trailing pipe line back would leave a lone pipe line at the end of a final answer unrendered, which the grammar's own "a lone pipe line is prose" forbids. Probed on the wired `insight-stream` page with 30 deltas of the insight text cut at arbitrary characters: 0 settled violations; the tail flickered twice on an INCOMPLETE line (a lone `*` as prose for one delta before its `**…:**` closed, a lone backtick before its fence opened), which the invariant permits; a hold on a trailing marker-only line would remove both and is the parser's one-liner if the eye wants it.
- 2026-09-06 · **A lead-in wears the trace-kind face with its colon as written, and a heading of any depth is a lead-in, never a heading** (`.ans-lead`: `--text-2xs` 700 .04em uppercase tier 2 on a 16px line; `code` inside keeps its case and mono; a figure inside takes tier 1 at the label's weight, the thinking chip's two tones): one label over its group, no hierarchy anywhere. The faithfulness cost, accepted: `## Answer`, which W2 deleted, now projects as a lead reading `Answer`; prompt v3 forbids headings and the corpus fixture pins the behaviour, so the first model that ignores the prompt puts one dead word in the lead register, and the fix, if the maintainer prefers the old silence, is one guard in the heading branch and a line here, not a quiet edit.
- 2026-09-06 · **Lists have no clamp** (`.ans-text li`: the marker tier 2 through `::marker`, tabular for an ordinal, 16px indent, items `--sp-1` apart): a clipped answer lies (LESSONS 9), so the two-line cap on a bullet is prompt v3's ask and the presentation score's check, never the renderer's knife. `<ol>` renders when the model numbered the list; the sketch's one `<ol>` is `insight-steps`' two steps, a sequence the reader performs, so its ordinals pass the deletion test (the designer's S3 call below).
- 2026-09-06 · **Figures are 600, tabular, never bold on their own** (`.ans-text .fig`; the sketch drew 500 and the judge's S3 asked the build to try 600 at 320 and 560 in both themes, `w5-frames/build/cmp-fig-*.png`): at 500 a figure is found on inspection, at 600 it lands at a glance; bold stays 650 above it, a figure inside a bolded phrase inherits the phrase's weight, and a bold that is ONLY a figure arrives from the tokenizer as a figure (the app has one face for a quantity and the model's bold is not it). The rule: a number with its sign, thousands separators, decimals, a leading ₹ $ € £, a trailing % or k M B x, or a status-register unit (`412.6 ms`); a date (`2026-08`), an id (`t1-01`) and a version (`v2`) are text, by the boundary (digits glued to a word character or continuing into `-` `:` `/` `.digit`). The accepted cost is a bare four-digit year (`in 2026`) lighting up, because every rule that kills it kills counts models write without separators (`1096`).
- 2026-09-06 · **Link text keeps whatever register the model gave it, and a backticked link text stays mono** (`InlineToken` link gains an optional `code: true`, `text` always the display text with the backticks stripped; AnswerText draws `<a><code>date_trunc</code></a>`): the judge's third S3, the sketch's `date_trunc` in the UI font beside mono identifiers. Links open through `@tauri-apps/plugin-opener` for http(s) hrefs only, anything else renders as its text, and the harness shim answers `plugin:opener|open_url` with nothing. The prompt hint the render builder asked for (`[\`date_trunc\`](url)` in step 3) was NOT added: prompt.ts says every string change bumps `PROMPT_VERSION`, the wave's three numbers were measured on v3's bytes as they stand, and a post-measurement edit would either fake the version or void the measurement (EVAL §4); the register is honoured either way.
- 2026-09-06 · **A markdown table renders only when no run is on screen, and `hasRun` is true from the moment a `run_sql` chip lands, not from the verdict** (AnswerBlock: `run !== null || chips.some(run_sql && !isError)`): with a grid on screen a table is the grid restated (rule 14) and strips, today's rule kept; without one it is the model's own comparison, drawn through the one Grid species in readOnly mode via `statementFromRun`, header plus up to six rows (`insight-prose`), with row numbers and the species' sideways scroll at the floor. The final text streams AFTER the run, so a verdict-based flag would show the table for the length of the stream. Cost: a provider that owns its loop and reports a run under another tool name is missed until the verdict lands; the five tools are law (AGENT-SPEC §5).
- 2026-09-06 · **Prompt v3 changes the answer shape and nothing else** (`PROMPT_VERSION` "v3"; `prompt.test.ts` pins rule 1, the column rule, LEGACY, the two-turn work plan, the Assumptions line and both fence phrasings byte for byte across v1, v2 and v3): the finish line of BASE_PROMPT and step 3 of HYBRID_RULES say what renders and WHEN, a direct question ONE sentence, an insight question two to four bullets of one finding each carrying its figure under an optional bold lead-in ending in a colon, none longer than 25 words (a countable cap; the two-lines wording measured 0.750 and `sentences_short` 3/8), never a heading, never a table of the result, with one GOOD and one BAD example over the orders question (neither names a Pagila noun, so a bench answer cannot be copied from them). Measured: `pagila.json` 32/33 at v3 against the v1 row's 33/33 (`t1-07`, the uppercase-values trap, `peek_values` skipped), within EVAL §4's one question, so accuracy forced no revision; the presentation revision (the GOOD example moved to BAD, the 25-word cap) landed before any v3 row existed, so the version names one text; `eval/baseline.json` is untouched and every gated v3 run reads as unmeasured until W3 records the rows, which is correct.
- 2026-09-06 · **The presentation score is five 0/1 heuristics over the same parse the slot renders, averaged, on `insight`-tagged questions only** (`eval/presentation.ts`; EVAL §3.x; `eval/bench/pagila-insight.json`, eight questions, tier 3, `gold_sql` null, status RAN): `bullets_in_range` (2 to 4 items), `sentences_short` (no sentence over 30 words, read off BLOCKS rather than `answerText`, whose flat projection cannot tell a soft wrap from the newline between two bullets and scored a hard-wrapped wall as five short sentences), `figure_per_finding` (every item carries a figure token AND there is at least one item: vacuously true over no items would pay a wall of prose 0.6, the same as a good direct answer), `no_grid_restatement` (no item names two cells of one grid row; 1 with no run; cells under three characters ignored, thousands separators stripped so `2,763` meets `2763`), `one_lead_in_at_most` (at most one lead, never two in a row). The ladder: a shaped insight answer 1.0, a direct answer 0.6, a wall of prose 0.4 (not the brief's illustrative 0.2: the restatement check is locked to list items, and reading paragraphs too is one line in `restatesGrid` plus a line here). The judge-model rubric stays unbuilt, the fallback only where these five and the maintainer's eye disagree on a 20-answer sample.
- 2026-09-06 · **Measured 2026-09-06 (Haiku 4.5 via claude-code, lab Pagila, `--jobs 3`): the insight bench 0.514 over 7 at v2, 0.771 over 7 at v3's first draft, 0.850 over 8 at v3 as shipped; `pagila.json` 32/33 at v3** (artifacts under `eval/results/`, gitignored; logs in the scratchpad's `w5-eval/`; every presentation number is the shipped scorer recomputed from `answer_full` + `run_shape`, the mean over the insight answers whose SQL RAN): v2 to the first draft, over each run's own seven, `bullets_in_range` 2/7 to 7/7, `figure_per_finding` 1/7 to 7/7, `one_lead_in_at_most` 5/7 to 7/7, `sentences_short` 3/7 to 5/7, and `no_grid_restatement` 7/7 to 1/7, for a reason the prompt owned: v2 had no bullets for the check to read, and the first draft's GOOD example read three cells of one result row (`611 of 2,763 orders were COD, 22% of the month and the year's highest share`), so the model followed the example over the rule beside it (`**New** leads with 8,088 rentals (6.6% of total)` beside `New | 8088 | 6.6`). The shipped text moves that bullet into the BAD example, says what a bullet IS (a comparison the grid cannot make for itself, ONE figure, never a second finding stacked on after a dash or a semicolon) and caps a bullet at 25 words; over eight answers, all of which ran, `no_grid_restatement` 4/8, `sentences_short` 8/8, `figure_per_finding` 8/8, `bullets_in_range` 7/8, `one_lead_in_at_most` 7/8 (both misses `ins-05`, a second lead-in over five bullets). Rule 14 is arriving and has not landed: four answers still name two cells of one row inside a comparison, the finding carried to the next prompt iteration. Two bench questions are expensive by design and were left so (`ins-05` burned 12 turns against the 10 s run cap at v2 and at the first draft, one turn on both runs of the revised text, 81 s on the two-lines text and 54.5 s on the shipped one; `ins-06` EXEC-FAIL at v2, RAN since); presentation is independent of accuracy, so a wrong answer in a good shape scores well, which is why accuracy is gated beside it and never traded for it.
- 2026-09-06 · **The eval artifact carries what its own number needs** (`Outcome.answer_full`, `Outcome.run_shape` capped at 200 rows by the ONE cap the score reads, `Outcome.presentation`, `summary.presentation` and `presentation_n`; `--baseline` reads an optional `presentation` on a row and fails when the run's mean is lower by more than 0.05): the two measured runs' JSON predates the wiring (RUN 1 had to precede any prompt edit and therefore the import of `parseBlocks`, RUN 2 was chained behind it), so both means are recomputed from `answer_full` + `run_shape` with the shipped scorer, which is what the cap is for; the wiring is proven end to end by `insight-wiring-check` (`--ids ins-03`: `presentation 1 over 1` in the summary, the per-question object in the JSON). The 0.05 slack: on eight questions one check flipping on one answer moves the mean 0.025 and a whole answer collapsing 0.075, so two checks of noise pass and one lost answer does not; a row without the field does not arm the rule, which is how every pre-W5 row reads.
- 2026-09-06 · **The harness gains `insight` · `insight-prose` · `insight-steps` · `insight-code` · `insight-stream`, one exchange each, each what v3 writes for its question, parked at the top by default** (`src/harness/fixtures.rich.ts`; the text is the subject, as for `strip` and `kv-wide`): the order_v2 thread on Haiku 4.5 (`RICH_CHOICE`: the pill reads the footer's model); `insight-stream` streams with the run landed on its chip and the second bullet cut mid-sentence, the Stop face in the composer. `scripts/ask-frames.ts` gained an explicit `--scroll bottom` that reaches the harness as `scroll=bottom` (the default alone never did), so a top-parking state's footer can be framed (`w5-frames/bottom/`). The wired frames post-date every source edit; the render builder's earlier stills came through a scratch route (`w5-rich.html`, since deleted) because the dispatch files are the integrator's.
- 2026-09-06 · **AGENT-UX §2 item 3 and DESIGN rule 14 are amended to v3's shape**: `one or two sentences of interpretation` becomes a direct question one sentence, an insight question an optional one-line lead-in and two to four bullets of one finding each; item 3 now names the rendered subset (lead-in, bold, italic, code, lists, quote, links, non-SQL code blocks, the table only without a run), the figure rule and the streaming invariant. The taste-gate checklist's `Is the prose one or two sentences?` needs the same amendment and is requested, not made (the skill is the orchestrator's).
- 2026-09-06 · **A filter the question did not state is a chip, never a sentence, even when the SQL row already shows it** (the W5 sketch gate's S1; `insight-code`): the coupon answer lost `Counted where the code is set, so an empty coupon object is left out.` and gained `Coupon = Code Set` beside `August = created_at`, so the count reads as v3's direct-question shape, one sentence (`Coupons sit inside metadata, in this shape:`) over the JSON block, the value, the SQL row and two chips, and `Retry Without Assumption` can undo the filter; the chip is cased per WRITING rule 1 (`Code Set`, not the judge's `code set`), the right side of `=` being prose here, not an identifier, as `Expensive = Signature Tier` is. The rule: the SQL row is the query and the chip is the interpretation, two facts in two slots; prose that says the filter again is a third slot with no control.
- 2026-09-06 · **One no-run answer per question, each what v3 writes for it, never a specimen sheet of the subset** (the W5 sketch gate's S2 and DESIGN 9): `insight-prose` (`which timestamp should I use for when an order was placed`) keeps the `Use created_at…` sentence, the three-column comparison as a readOnly grid and the closing `timestamptz` sentence with its `date_trunc` link, three blocks; the lead-in, the quote and the list moved to a new state `insight-steps` (`how do I count orders placed each month`, strip `describe order_v2` · `peek payment_status`, no run): `Comment on order_v2:` over the quoted `One row per checkout attempt, failed included.`, the inference the comment does not state (`So a bare count is attempts, not orders. Counting the orders placed is two steps:`) and the two steps; the list that restated the table's row 2 is gone with the split. The section is now five rows, rows 3 to 5 each at 320/392/560.
- 2026-09-06 · **An ordered list is a sequence the reader follows; findings are bullets** (the W5 sketch gate's S3; the Lists call in the sketch): the sketch's one `<ol>` is the two steps of `insight-steps` (`1. Drop the failed rows. 2. Bucket the rest by created_at. paid_at lands days later for COD.`), an order the reader performs, so the numerals pass the deletion test; two independent findings numbered 1 and 2 are dead ordinals and render as `<ul>`. The marker stays tier 2 tabular through `::marker`, 16px indent, items 4px apart, no clamp.
- 2026-09-06 · **A quote earns its hairline only when the answer builds on it, and its lead-in names the source** (the Quote call, amended): `insight-steps` quotes the table comment because step 1 drops `failed` on the comment's word; the lead-in `Comment on order_v2:` is the source line, tier 2 with the identifier in code at its own case, one per answer, never stacked.
- 2026-09-06 · **W5 taste gate: pass, no S1; frames in the session scratchpad's `w5-frames/`** (ROADMAP_log W5): the insight row beats the wall of prose at a glance in both themes at 320, 392 and 560 (the lead-in and three bullets land before the grid; every figure in the bullets is a derived number, none a cell); rules 11 to 14 hold per frame, chrome identical across widths, nothing new clips at the floor beyond the strip resting at its newest chip (W2d's ledgered S3) and the SQL row's own ellipsis; the `answer` and `echo` regression frames show the W2 prose unchanged but for its figures now tabular at 600. Ledgered as data, not chrome: the sketch caption's `the table fits` at 392 against the build's real column widths (`insight-prose` scrolls sideways at 392 and stretches its last column at 560), the provenance dot's colour (the harness profile's green), and the grid's scrollbars hidden by Chrome's `--hide-scrollbars` in every frame.
- 2026-09-06 · **Only `*…*` marks emphasis, never `_…_`, and markers that drift inside one group are one list** (display.ts's inline scanner and list loop): `created_at and paid_at` is not "at and paid" in italics, and snake_case is how every identifier in this app is spelled, so the underscore is text; a model that drifts between `-`, `*` and `1.` inside one group writes ONE list whose FIRST marker decides the register (an ordered list is a sequence the reader follows, and the register must not change under a list while it streams), rather than three blocks a reader takes for three groups. One level: a nested marker is an item of the same list, an indented line without a marker continues the item above it, a line back at the margin ends the list. Inline emphasis is a register, not a leaf: bold and italic spans pass through `inlineTokens` again (AnswerText `inline`), so `**Total 4,647,023 orders**` draws the figure inside the bold and a link inside a link is its text; the recursion stops at links and never enters code, which is opaque, so a quantity inside a code span stays in the editor register, a cost accepted beside the figure rule above.
- 2026-09-06 · **Every block in the slot wears a register the pane already has, and the link is the one thing in the text that answers a pointer** (ask.css, the answer-text section): the quote is a 1px `--fg-muted` left rule with a `--sp-3` inset and tier-2 text, the code block the trace body's face at tier 1 (mono `--text-xs`, `--bg-app` behind a `--border` hairline at `--radius-sm`, `pre-wrap` with `overflow-wrap: anywhere`), inline code `--font-mono` at `--text-sm` (it was the UA monospace before), the model's table `.ans-grid` around the one Grid species; blocks sit `--sp-2` apart as each block's own top margin so no UA margin can out-rank it, a lead-in's group `--sp-1` under it, and `white-space: pre-wrap` moved from the slot to `p` alone, so a list item or a lead-in is one line however the source wrapped it. The link (DESIGN 2 and 8): accent with a hairline underline at 40% at rest, the full underline on hover, the `.linkish` accent-soft fill on press and the app's one focus ring; the anchor keeps its `href` (the status bar and the context menu read it) and the click is `preventDefault`ed, because a navigation in the webview replaces the app with a website.
- 2026-09-06 · **Blocks are keyed by index and kind, nothing inside the slot animates, and the slot is the section and the live region** (AnswerText; `.ans-body > *`): a streaming answer re-renders on every delta, a block that grows keeps its node, a block whose kind changed (only the last can, the parser's promise) remounts, no per-block fade and no count-up; `aria-live` polite rides the slot of the newest exchange and `.ans-text:empty` collapses it when parseBlocks returns nothing, the W2 contract unchanged. The render test (`src/ask/__tests__/AnswerText.test.tsx`) stubs the Grid species and `statementFromRun` with `mock.module`: both reach the stores, which touch `document` at module load, and neither is what the test checks; the stub records the columns, row count, `readOnly` and `maxRows` the table block handed it, so a table's route to the grid is still asserted while the grammar stays display.test's.
- 2026-09-06 · **The presentation mean is taken over the insight answers whose SQL RAN, never over the bench** (`presentationOver` in scripts/agent-eval.ts; EVAL §3.x and §4): an EXEC-FAIL puts no grid on screen, `no_grid_restatement` is 1 where there is no grid, and averaging that answer in would let a prompt buy presentation by breaking the query, a gate that can be bought. Measured on the first v3 run: the one question that timed out (`ins-05`) was the only 1.0 on the board and lifted the run from 0.771 over 7 to 0.800 over 8, 0.029 of the gate's own 0.05 of slack, paid for by a failed query. The score still sits on that question's row and `presentation_unscored` names it; what it may not enter is the number. This is why the wave's brief and its first reports read 0.525 and 0.800 over 8 where the record reads 0.514 and 0.771 over 7: the same artifacts, the mean taken over the rows it may be taken over.
- 2026-09-06 · **W5 final gate: pass, committed, the three remaining S3s closed in the gate** (frames in the session scratchpad's `w5-frames/`, the `answer` set re-shot; ROADMAP_log W5): the W2 `answer` fixture's prose read `4.65M this year, 2.95M of it in August` and the second figure was the grid's `2026-08-01` cell rounded, a duplicate W2 shipped unnoticed and W5's 600 face now lands at a glance beside the grid (rule 14), so the fixture and the sketch's two W2 rows read `63% of it in August`, a derived share the grid cannot show, and the twelve `answer` frames were re-shot after the edit (the parser corpus keeps `2.95M`: there it is a figure under test, not a fact beside a grid); the ledgered S3 on the grid's sideways scroll now names `insight` at 320 (`amoun`) beside `insight-prose` at 392 (`COD in tra`), the species' viewport edge and not chrome; and the phrase on `ins-05`'s one-turn runs now names both texts it was measured on (81 s on the two-lines text, 54.5 s on the shipped one), since the two runs were two texts. Gates re-run after the edit: tsc clean, design-lint 0 finding(s), bun test 445 pass / 0 fail.

### Agent A1 · W6 (@ context tags, 2026-09-06; picture: the "W6 · @ context tags" rows of ~/projects/qwry-agent-lab/docs/ask-sketch-v2.html)
- 2026-09-06 · **A column row in the mention popover is two spans, and the ellipsis eats the table, never the column** (the W6 sketch gate's S1; `.mention-path` / `.mention-pre` / `.mention-col` in ask.css, mention-popover at 320): the table prefix (`erp_order_cost_snapshot`, with its `schema.` when not public) owns `min-width:0; overflow:hidden; text-overflow:ellipsis`, the column rides in a second span with its dot in front (`.order_id`) at `flex:none`, so the floor reads `erp_order_cost_snapsh… .order_id` and `erp_order_cost_snaps… .order_ref` as two rows; a title discriminates at its start (the Threads sheet's rule) and a dotted path at its end, so the two species overflow at opposite ends. The hint slot is `flex:none` (the picker's `.picker-ctx`) and never cut. The `ord` fixture carries two columns of one table so the 320 frame proves the rule, not just states it.
- 2026-09-06 · **The mention-echo and mention-trace rows are drawn at 320 · 392 · 560 like every other state of the wave** (the W6 sketch gate's S2; rows 3 and 4 of the section): at the floor the bubble at its `calc(100% - 76px)` = 220px cap wraps to three lines with both pills whole; the split across a wrap, rounded on both fragments through `box-decoration-break: clone`, is the 392 picture; at 560 the question is one line. The echo frames park the scroller at the new exchange (the W4 `actions` device) because the bubble is the subject and the answer is 15px taller than the floor's viewport either way. The trace frames drop the sketch's 300px cap on the block: an `overflow:auto` item in a flex column gives way before its siblings, so the step rows stay whole at every width and only the block scrolls at 320, where the TAGGED header, the table line, the saved query's name and the SQL's first line are on the page and its last line is under the block's fold; the build's own cap stays 220px.
- 2026-09-06 · **The mention pill wears the user's own glyphs; the pill, the `@` and the quotes are the data costume** (the W6 sketch gate's S3; `.mention` sets fill, ring, radius, margin and clone, no font): the textarea paints the draft and the backdrop cannot set a mono span of it without drifting off the glyphs it paints behind, so the composer's pill cannot be mono, and the bubble matches it so the lift ghost carries one face into the echo. WRITING's identifiers rule is honoured by the costume, not the face: the `@`, the quotes around a two-word name and the accent-soft pill mark the span as data the way mono does elsewhere. The trace's `tagged` line, plain chrome, keeps the identifiers mono. The brief's strings line, `mono in rows and chips`, reads `mono in rows, the pill in chips` (the sketch judge's S3 #4): WRITING's mono form applies to the popover rows and the trace line, never to the chip.
- 2026-09-06 · **One chip face for the draft and the bubble: accent-soft fill, a 1px ring at 22% accent, 4px corners, outdented 2px over its own margin** (`.mention`; mention-draft and mention-echo frames in both themes): in the draft the 16% fill sits on `bg-app`; in the bubble the same declaration stacks on the bubble's 16% tint and its 22% ring and reads one step deeper, the way `.chip.active` reads over any surface. A ring alone would draw an outlined box on prose, a control costume on text (rule 8's inverse); a hand-tuned stronger fill would be a second face for one thing. The outdent keeps the glyph run where the textarea put it, so a pill appearing or leaving never moves a letter. Measured in the `mention-echo` frames: dark pill rgba(74,144,226,.18) over the tint with a color(srgb … / 0.22) ring, light rgba(53,131,223,.13), radius 4px; 2 client rects for `@"Monthly revenue"` at 392, 1 each at 320 (three lines, both whole, the bubble at its 218px cap) and 560 (one line). The brief's `padding 0 4px` and its `.ans-echo .mention` selector were not built: the picture is the wave's law and one declaration cannot drift into two faces.
- 2026-09-06 · **The mention popover is the picker's box opened upward from the composer box at the composer's width, with no title, no footer and no empty row** (`picker-anchor mention-anchor` + `picker-pop mention-pop`, portaled to `<body>` at the composer box's top-left, 4px up, at its outer width; the picker's classes with one modifier and nothing re-authored, DESIGN rule 1: the only new CSS is the modifier's `width:100%; max-height:320px`, the 28px row, the identifier faces `.mention-name` / `.mention-path` › `.mention-pre` / `.mention-col` / `.mention-title` and the hint's mono tabular): its edges sit on the box's outer edges 4px above it at every width (chrome identical across 320 · 392 · 560, rule 13), rows 28px, hot = `bg-hover`, headings in the picker's uppercase face, over 320px of rows it scrolls. The `@` under the caret is the title; nothing to pick closes it, because a `No results` row is rule 11 dead text; the caret stays in the textarea in every frame (focusless, like the picker).
- 2026-09-06 · **The hint slot carries data only: a table's row estimate, a column's type, nothing for a saved query or a thread** (`.mention-pop .picker-ctx`, mono, tabular, tier 2): `~2.1M rows`, `~318k rows`, `text`, `uuid` in the status register; a saved query's and a thread's name is the whole fact and the Threads sheet already owns the time, so their rows carry no hint. A table the planner never counted shows its column count instead (`5 columns`): a view or an unanalysed table has no estimate and `6 columns` is still data, never a blank slot. `rowsHint` writes `~2.1M rows`, `~318k rows`, `~412 rows`, `~1 row`, one decimal under 10, nothing for -1; `shortType` writes psql's spellings (`timestamp with time zone` → `timestamptz`, `character varying(120)` → `varchar(120)`, `double precision` → `float8`), because a 24-character type would squeeze the identifier off the floor and the hint is flex:none by the designer's call. Legacy tables are never offered (AGENT-SPEC §6 rule 3): a row that cannot be the answer is a dead row, not a faint one; the ban is the starters' own (`isBannedRelation`, starterPool.ts) plus foreign tables.
- 2026-09-06 · **Groups run Tables · Columns · Saved Queries · Threads, each drawn only when it has rows, columns from two typed characters and capped at 40** (the popover's group order): the order is the loop's own, a table is a must-include candidate, a column narrows one, a saved query and a thread carry text rather than a table and come last; deleting any heading merges a table with its same-named column, so all four earn their pixels. Row 1 shows what `ord` matches and no specimen of a group the filter cannot produce. The filter is not cmdk's scorer: `mentionRows` matches by substring on the name the user would type (a prefix outranks a substring, ties keep the connection's order), a column row on the COLUMN name alone (the table part is shown, not searched; `order_v2.ord` spells the table), with `kind:identity` as the cmdk value so Enter routes by value (the 2026-07-02 rule).
- 2026-09-06 · **The mono ellipsis takes a full cell, so a truncated table stands a cell off its column's dot (`snapsh… .order_id`); accepted** (mention-popover at 320): the alternative is an ellipsis in the UI font, a second face inside one identifier, and the gap reads as the boundary it marks. The build shows the same geometry in SF Mono.
- 2026-09-06 · **`token` is the tag without its `@`, `canonicalToken` the tag with it, and a saved query or a thread is always quoted** (mentions.ts): `token` (`order_v2`, `users.email`, `"Monthly revenue"`) is what the trace's `tagged` line prints, so the sketch's line needs no post-processing; `canonicalToken` is what a pick inserts, `@` + that, a non-public table as `@schema.table`, a quoted name with its quotes doubled the way Postgres doubles them (parse and write are property-tested as a pair, LESSONS 1). A one-word saved query inserted bare (`@revenue`) would resolve to a TABLE named revenue first, so the quotes are never dropped for a saved query or a thread; a quoted name still runs the whole ladder (`@"customer"` is the table), so nothing reachable becomes unreachable. The one thing the grammar cannot spell is a non-public table whose name needs quotes (there is no quoted PATH form): that row is never offered, and the case is a test, not a comment.
- 2026-09-06 · **Tags resolve at call time in `locateExchange`, so retry, Fix It, a chip flip and Restart re-resolve too, and a thread tag's replay travels on its ref** (agent.ts `mentionsFor`, `withThreadReplays`; `ThreadRef.replay?`): the lock named `ask`, `askFrom`, `restartFrom`, but Restart on the newest exchange IS retry, so every re-ask resolves the persisted question against the connection as it stands now (LESSONS 3: captured before the first await, which a store test proves by taking the schema and the bookmarks away while the run is in the air; LESSONS 5: a dropped table stops being context, the question still runs). The resolver is sync and store-free by law, so the appdb reads a `@thread` tag needs (its turns AND its answers, `agent_turns_list` + `agent_answers_list`, paired by `replayPairs` the way openThread pairs them and replayed by the W4 `replayOf` under its own 1,500-char cap with no header; the SQL is the answer row's, a FENCED SQL in the text the fallback, else `SQL: none`, because extractSql's `raw` fallback would send a paragraph under `SQL:` and the model would read it as one, LESSONS 9) happen on the way to the run and land in `ref.replay`, which nothing in the UI reads; a second AskRequest field would have broken the locked `mentions?: Mention[]` shape instead. The small tier's message gets no tag block: a tagged table still reaches it as a must-include candidate.
- 2026-09-06 · **The harness has one W6 seed** (fixtures.mentions.ts `MENTION_SNAPSHOT` · `mentionSaved` · `mentionThreads`, read by fixtures.mentions-echo.ts too): the popover and echo builders each seeded an order_v2 schema and a `Monthly revenue` query; a tag the popover frame offers must be a tag the echo frame resolves, so the echo's copies went and every other harness state resets `useSaved` the way it resets the starter pools, so a bookmark persisted by an earlier page never reaches a frame. The seed is what `ord` finds AND two decoys (`orders_legacy` with a LEGACY comment, `Monthly revenue` and `revenue last month` beside `Orders by day` and `how many orders were refunded in August`), so the popover frame proves the filter and the ban rather than a specimen sheet; `mention-echo` and `mention-trace` (fixtures.mentions-echo.ts) add the tagged exchange whose two figures are derived (2301880 − 2277416 = ₹24,464, 1.07% → `1.1%`, no cell repeated), a `run ×2` chip for the saved query's row and the comparison, one turn step under a `1 turn` footer (the ownsLoop shape; the sketch's `turn 1` / `turn 2` rows were the sketch's), and a context text that is askMessage's bytes with the tagged table first and the two TAGGED lines last.
- 2026-09-06 · **The `@` grammar is one regex and no state: `@` at a Unicode word boundary, then an identifier path of at most three segments or a double-quoted name whose `""` is one quote** (`parseMentions` in `src/agent/mentions.ts`; `MENTION`, `WORDY` = `[\p{L}\p{N}_]` under `u`): `a@b`, `sales@orders.com` and `नाम@customer` are one word each and never a tag, because the boundary is Unicode-aware and not ASCII; `@users.` is `@users` (the path never eats a trailing dot), `@a.b.c.d` is `@a.b.c` and the `.d` is text; a quote that never closes and a quote spanning a newline are plain text, so an unfinished `@"` cannot swallow the rest of the draft, and `@""` names nothing. The `""` escape is Postgres's own, which makes serialize and parse a pair (LESSONS 1): `canonicalToken()` re-doubles a quote and the round trip is a test over every kind.
- 2026-09-06 · **Resolution is one ladder, first hit wins, and every Map is built for that one call** (`resolveMentions(raw, { snapshot, saved, threads, currentThreadId })`): table (bare name or `schema.name`, case-insensitive, `public` preferred, then the snapshot's own order), column (`table.column`, `schema.table.column`), saved query by name, thread by title, never the thread being asked in; a quoted name is ONE literal identifier and is never split on its dots (`@"sales.orders"` resolves to nothing), and an unresolved span is dropped, which is what leaves it plain text with no chip and no context. No cache anywhere: a resolution cache is a stale cache (LESSONS 5), and the caller passes what it has, so the module makes no network call and imports no store at runtime (AGENT-SPEC §2 rule 2, pinned by contracts.test.ts). `mentionSegments` is the module's one cut of a text at its spans: the backdrop, the lift ghost and the echo render the same segments, so the three homes cannot disagree about where a pill starts, and a span that overlaps or overruns is skipped, never cut (the chips builder's first draft carried a segmenter of its own, written against the locked signatures before the module landed, deleted the moment the module exported one).
- 2026-09-06 · **The tag block is one line per tag under `TAGGED BY THE USER:`, under the candidate block and above the RISK CHECK block** (`mentionContext()` builds the lines, `askMessage({ context })` owns the header): `table public.order_v2` (always schema-qualified, valid SQL whatever display name the candidate block used), `column users.email` (qualified only outside `public`), `saved query "Monthly revenue":` then the SQL verbatim, `thread "…":` then its replay, each text capped at 1500 characters with `\n… (truncated)` when it is cut, because a silently halved query reads as the whole query and gets answered as one (LESSONS 9). The same thing tagged twice is one line; nothing tagged is an empty string, which keeps the header off the message entirely, so a question with no tags sends the bytes the eval measured and `PROMPT_VERSION` stays v3 (a loop test pins the untagged message byte for byte).
- 2026-09-06 · **A tag is a must-include candidate: the tagged tables lead the block and the lexical picks give way rather than adding** (`mustIncludeFor(meta, mentions)` + `candidates(question, meta, k, mustInclude)`, context.ts): a tagged table and the table of a tagged column go first in the order they were typed, deduped, and the IDF picks fill the rest to the same k, so the model still reads k names plus the hubs and the one hop, and the one-hop expansion now hops out of what the user pointed at. A tagged LEGACY table IS included: the popover never offers one (AGENT-SPEC §6 rule 3), so only a hand-typed token can name it, the prefilter guesses and the user does not, and the prompt still says a LEGACY table is never the answer. A ref the snapshot no longer carries is dropped, never sent as a name.
- 2026-09-06 · **A `@thread` tag replays through the cut's own helper, headless and under the tag's cap** (`replayOf(exchanges, { head, cap })` gained the two options and a structural `ReplayExchange`; `replayPairs(turns, answers)` pairs a thread's appdb rows): one replay format, not two, because a second one would have drifted by its second wave; oldest exchanges drop first until the rest fits 1500 characters. History stays a convenience (the cut's rule): a thread whose rows cannot be read is still tagged, by name, with no replay under it.
- 2026-09-06 · **The trace's context step carries `mentions: { kind, token }[]` and carries the field only when something was tagged** (`TraceStep`, `MentionKind` declared in `src/agent/types.ts` so that module still imports nothing): absent rather than empty, so the `tagged …` line has nothing to draw when there is nothing to say (rule 11), and the tags themselves stay inside the step's `text`, under the block, which remains exactly what was sent.
- 2026-09-06 · **The `@` under the caret is read from the textarea, never derived from the draft** (`mentionQueryAt(text, caret)` on onChange · onKeyUp · onClick · onSelect in AskPanel; a selection is no caret; blur closes): the fragment is the grammar read up to the caret, an identifier path ending at any character outside `[A-Za-z0-9_.]` and a quoted name running to the caret WITH its spaces until its closing quote or a newline (the brief's `a space closes it` holds for the path form; inside `@"…` a space is the name's); the caret placed inside a finished token counts and a pick replaces the WHOLE token (`mentionTokenEnd`), the way an editor's completion replaces the word, then parks the caret after the appended space; a query Esc dismissed is remembered until the caret's `@` or its fragment differs, else the Esc keyup would reopen what Esc closed. The state is `useAsk.mentionQuery` with `openMentions(at, filter)` / `closeMentions()` as its DOM-free door (a slide-over, a connection switch and the pane closing clear it), so the harness opens it a frame before focus lands the way `beginEdit` does and a still shows what a keystroke shows.
- 2026-09-06 · **The `@` popover is the composer's completion, not an overlay: it never joins the escStack and puts no click catcher on the page** (MentionPopover.tsx: `createPortal` into `<body>` under `.mention-anchor` at `z-index: 40`, over the app's 20/30 overlays and under the toast's 45 and the escStack's 50–59 of the 2026-07-20 ladder, so a real overlay opened over it always wins; the fixer round's reversal of the first build's AnchoredOverlay): a stack entry stood App.tsx's chord handler down for as long as an `@word` was under the caret (`overlayOpen()` returns early: ⌘J, ⌘K, ⌘W and the native menu dropped, LESSONS 10) and the overlay's full-screen catcher made the first click on Send or the model pill a click that did nothing (a false affordance, LESSONS 9). The SQL editor's completion is the precedent: the editor's own, never a popup over it. Keys reach it through AskPanel's `onComposerKey` by the imperative handle it exports, which owns ↑↓ (the hot row), ↩ and ⇥ (the pick) and Esc (closes this and nothing else; the pane's ladder gets the next Esc) and no other key: Home, End, typing and every chord are the textarea's, whose next keyup re-reads the fragment. A mousedown outside the box and the textarea closes it on capture and the press lands where it was aimed (probed: Send sent, the pill opened the picker, one click each); the box refuses mousedown so the caret never leaves the textarea. 26 assertions on the wired page after the fix, all pass.
- 2026-09-06 · **The draft's pills are a flex wrap with an absolute backdrop, and the textarea rises above it** (`.ask-ta-wrap{position:relative;display:flex}`, `.ask-ta-back{position:absolute;inset:0;overflow:hidden;color:transparent;…}`, `.ask-ta{position:relative;z-index:1}`): flex, not block, so an inline-block textarea adds no line-box slack and the composer keeps its exact height; the backdrop renders `MentionText` (Mention.tsx, the echo's class) plus one trailing `\n`, because a textarea shows the empty last line a trailing newline makes and a pre-wrap div does not, and without it the two scrollHeights disagree and the mirrored scrollTop clamps a line short; it hides (`.ghosted`) while either ghost carries the words, so the pills exist once, and both ghosts render the same segments.
- 2026-09-06 · **The echo re-resolves on render, never from a stored resolution** (AnswerBlock: `mentionsIn(question, { snapshot, saved: visibleSaved(saved, profileId), threads, currentThreadId: threadId })` under `useMemo` over question · snapshot · saved · threads · profileId · threadId, the three store reads selector-stable): the question persists as typed and a mention that stops resolving stands as plain text (LESSONS 5), a reloaded thread and a live one take the same path, and `MentionText` returns the bare string when nothing resolved so an untagged bubble's children are byte-identical to before (the `echo` and `trace` regression frames at 392 unchanged). The bubble stays the same motion.div under the same layoutId: only its children changed, so the lift and the edit travel are untouched. The cost is the ladder's two table Maps per exchange whenever snapshot, saved or threads change; a long thread over a 2,000-table snapshot is the profile to watch.
- 2026-09-06 · **The trace's tagged line is `Row.above`: one `.trace-tag` div between the context row and its block, `tagged` at tier 2 and each token in `<code>` at tier 1, `·` joined, the tokens exactly as the grammar defines them, absent when `step.mentions` is empty or missing** (TraceDrawer.tsx; the `.trace-tag` rules in ask.css's TraceDrawer fenced section, not the `.mention` block: the line is TraceDrawer chrome and the pill's one declaration carries no stranger): the block below already carries the qualified TAGGED lines, so the line is their summary and never a second slot (rule 14; the judge's S3 #2 kept in the sketch's form, `tagged order_v2 · "Monthly revenue"`, the semantics example's `public.users` amended by it); it wraps as the chips row does with `overflow-wrap: anywhere` (a cut tag is not a summary). Measured: one 18px line at 294 · 366 · 534 wide in both themes, the step list never scrolls, only the 220px block does, and the TAGGED lines sit under that block's fold at every width, which is why the line carries information (rule 11).
- 2026-09-06 · **A dotted fragment names the table and filters its columns; a quoted fragment offers saved queries and threads only** (`mentionRows(filter, quoted, ctx)`): `order_v2.` lists every column of order_v2 capped at 40, `order_v2.pay` one, `erp.order_` the prefix-matched table's; `@"mon` finds `Monthly revenue` and no relation. The grammar's own shapes, so what the popover offers is what the resolver reads back; `mentionRows.test.ts` pins it, and the same function decides the mount, so an empty result never renders a box for one frame.
- 2026-09-06 · **W6 final gate: pass, committed, the two remaining S3s closed in the gate** (frames re-shot in the session scratchpad's `w6-frames/` after the fixer round, 36 in 23.6 s; ROADMAP_log W6): the record's popover line and the session note still described the first build (AnchoredOverlay, `margin={0}`, keys through the overlay's `onKey`, a click catcher, the thread SQL from `extractSql`) where the tree shipped the fixer's (a portal off the stack, keys through AskPanel's handle, a capture-phase mousedown, the answer row's SQL first), so both were rewritten to the tree and the reversal got its own line above: the ledger records what was done, not what was planned (CLAUDE.md steps 4 and 5, LESSONS 11). AGENT-UX gained §1a (the completion and the pills), the §2 item 1, §10 and §12 sentences, AGENT-SPEC §6 the line that `PROMPT_VERSION` did not move. Gates on the tree as committed: tsc clean, design-lint 0 finding(s), bun test 513 pass / 0 fail (36 files, 4158 expect() calls).

### Agent A1 · W3 (close, 2026-09-06; review, re-baseline, staging numbers, PR #41)
- 2026-09-06 · **A baseline row is one artifact, and a measured loss is a recorded loss** (`eval/baseline.json`, the W3 note in its `note`): the v3 rows carry `pagila.json` + claude-sonnet-5 at 28/33 and `pagila-hard.json` + claude-haiku-4-5 at 3/5 as measured, the second sample evidence about the row and never a better version of it; the fix for either is a new `PROMPT_VERSION` with its own re-baseline, not an edit to v3. EVAL §4's "a baseline never moves in the same PR as the change it measures" is broken once, by the maintainer's W3 brief, because the branch that introduced v3 (W5) is the branch that closes on it.
- 2026-09-06 · **Staging numbers are release-notes numbers, not gate rows, until the maintainer's own baseline commit** (EVAL §2, §4): the staging artifacts stay in the session scratchpad (the shell holds the credentials, the artifact holds answers over the maintainer's data; its `config` carries no DSN, checked), the numbers go into EVAL §4's table and the ROADMAP_log note with the rows written out ready to paste, and `--baseline` keeps reading staging as unmeasured, which is red and not green. A docs-only close commits no `eval/` change.
- 2026-09-06 · **The `statement_timeout` setting's 0 is a session shape, not a tool-call shape** (AGENT-SPEC §8.2; `RUN_SQL_TIMEOUT_MS` in `tools.ts` and `agent_mcp.rs`): `agent_connect` passes 0 through as "no session timeout" and Postgres reads 0 as disabled; `run_sql`, `probe` and the MCP server fall through to the §5 default of 10 s and never to `run_readonly`'s `MIN_TIMEOUT_MS` floor, which would hand the tightest timeout to whoever switched the timeout off. The code half landed in `e67d16c`; the re-verify lens found the spec silent and the doc half landed with the close (LESSONS 12: one review is not review).
- 2026-09-06 · **The staging bench's schema is counted as measured, not as written**: EVAL §2 said 202 tables from the 09-03 lab runs and the W3 runs saw 203; the doc now says 203 and the log names the drift. Recall stayed 1.00, so the new table is in no gold SQL.

### Agent A1 · W3b (prompt v4, 2026-09-06)
- 2026-09-06 · **Prompt v4 adds three SQL rules and moves nothing else** (`PROMPT_VERSION` "v4"; `SYSTEM_PROMPT` is the v3 string with ONE insertion of 721 bytes, 3475 → 4196 chars, and deleting the three lines reproduces v3 byte for byte; `prompt.test.ts` keeps the v1–v3 MEASURED list intact and adds the three sentences, the version, and their placement inside "Rules that override your instincts:" and above the work plan; `contracts.test.ts` follows the version): v3 bought presentation (`pagila-insight.json` 0.514 → 0.850 on Haiku) and paid in SQL — claude-sonnet-5 `pagila.json` 32/33 → 28/33, claude-haiku-4-5 `pagila-hard.json` 5/5 → 3/5 — and EVAL §4 refuses a trade in that direction, so v4 is the losses written down as rules and not a softened answer shape. Measured on the ids that lost: Sonnet t3-08 · t4-02 · t4-04 · t5-01 4/4 where v3 read 0/4 (v1 read 3/4), repeated 3/3 on a second draw; Haiku ph-03 · ph-05 2/2 twice where v3 read 0/2; the presentation guard `ins-01` 1.0 and `ins-03` 0.8, the same per-question checks v3 measured, mean 0.900 over 2.
- 2026-09-06 · **A rule that forbids a column has to say where the figure comes from instead** (HYBRID_RULES line 2, beside the untouched `- Return exactly the columns the question asks for, no extras.`): v3 already carried that column rule and Sonnet broke it three times in one shape, always to feed its own prose — `SELECT rating, COUNT(*)` for "return only the rating" (t3-08), `actor_id` and `film_count` beside the asked-for names (t4-04), `length` beside `rating, title` (t5-01), `total_amount` beside `currency, order_id` once on staging (s5-02) — because W5's "each ONE finding carrying its own figure" was the only line in the prompt that said where a figure may come from, and a column is the cheapest place to get one. So the new line names the three traps as traps (the column it orders by, the count it ranked with, an id) and then names the escape: a query already run or one more `run_sql`, never a column added to the final SQL. Measured: both draws answer "Gina Degeneres … with 42 film credits" over a two-column final SQL, the figure read out of the first of two `run_sql` calls, tool calls flat at 2 and output tokens flat.
- 2026-09-06 · **The join fan-out is arithmetic, so the rule states the arithmetic before the remedy** (HYBRID_RULES line 3): `ph-05` joined rental and payment to customer in one pass, got `COUNT(DISTINCT r.rental_id)` right and `SUM(p.amount)` multiplied by the rental count, and failed identically in both v3 samples where v1 pre-aggregated each side in its own CTE and passed. The line says what happens (two one-to-many relations on the same parent multiply each other's rows and inflate every SUM and COUNT) before it says what to do (aggregate each in its own CTE, join the aggregates), because a model given only the instruction re-derives the one-pass join whenever the query gets long. Measured: two draws, both three CTEs and both PASS — and the hard bench's output tokens fell from 5375 / 6934 to 2024 / 3169 and 2346 / 2395, so v3's least careful SQL was also its most expensive.
- 2026-09-06 · **The extra column and the padded month are one instinct, so one rule covers both halves** (HYBRID_RULES line 4): rows first — return the rows the data has, never padded with periods that have no rows, no `generate_series` spine unless the question asks for every period, which is staging `s3-04`'s 12-row calendar over three months of signups — then the cast `ph-03` lost to twice, integer division before `ROUND`. Two facts in one line because they are the same trade the other two rules make: give the question what it asked for, in the type the arithmetic needs, instead of what looks complete. `ph-03` passes in both v4 draws with the numeric cast inside the LAG division; the row half is measured only in intent until a staging run exists.
- 2026-09-06 · **v4 is measured on its losses, so every v4 row reads UNMEASURED until the re-baseline** (`eval/baseline.json` still carries the six v3 rows; EVAL §4): eleven questions across five probe runs are evidence about the fix and not a bench, and `--baseline` matches on bench + model + provider + `PROMPT_VERSION`, so every gated v4 run is red-not-green until `pagila.json` is re-run on both models, `pagila-hard.json` on Haiku, and the insight mean re-measured over all 8 — the presentation numbers the gate protects (0.850 Haiku, 0.825 Sonnet) stand on two answers at v4. The baseline commit stays separate from this one, which is the rule W3 broke once by explicit brief and should not break twice.
- 2026-09-06 · **The re-baseline landed the same day, and it does not clear the gate on one row** (`eval/baseline.json`, `c6494b6`, all ten rows at `PROMPT_VERSION` v4): the full-bench runs recover both v3 losses at no cost (Sonnet `pagila.json` 33/33, Haiku `pagila-hard.json` 5/5) and add four staging rows where none existed before at any version (`staging.json` 23/23 both models, `staging-hard.json` 5/5 Sonnet and 4/5 Haiku, `h-01` still lost and not a v4 regression). They also show what the five-probe sample above could not: `pagila-insight.json` + claude-haiku-4-5 over its full 8 reads 7/8 ran and presentation 0.714 over 7 against the v3 row's 8/8 and 0.850, 0.136 down where EVAL §4's slack is 0.05 — NOT MET, `ins-05` EXEC-FAILing twice in a row where it ran in one turn at v3c. Sonnet's insight row moved up instead, 0.825 → 0.900. Two readings, neither measured: sampling variance (the bench's first repeat of any kind) or the three new rules crowding the answer shape (avg_out_tokens 2302.8 → 3438.5, avg_turns 1 → 2.5). Written into the baseline as measured, not as met, then resolved the same hour by two more draws run alone (0.875 and 0.850 over 8, `ins-05` in one turn both times, avg_turns 1.1): the row is re-transcribed from the median artifact (`insight-haiku-v4c.json`, 0.850, the v3 level), all three draws are named in the baseline note and EVAL §3.x, and the first draw stands as the measurement that one run of an eight-question bench reads about 0.08 either side of its level, so a single insight run under a row is re-sampled once before it counts.
