# qwry — Roadmap

> Session protocol: pick the next unchecked item in the current phase. Build → verify per the phase gate → tick → append session note at bottom. One phase ≈ one session; finishing early, pull from the next phase.

> **▶ ACTIVE PUSH (since 2026-07-18): v0.7 → v0.9 "the launch push".** Plan, wave map, and quality gates live in **[`docs/ROADMAP_v0.9.md`](./ROADMAP_v0.9.md)** — read that next; tick items there + append session notes here as always.
>
> *(previous push, complete: v0.3 → v0.6 — see below)*
> **▶ ACTIVE PUSH (since 2026-06-26): v0.3 → v0.5 "releasable".** The current plan — 5 milestones, 4 cross-cutting tracks (perf · bug-polish · context-menu system · find system), deferred list, and open questions — lives in **[`docs/ROADMAP_v0.5.md`](./ROADMAP_v0.5.md)**. Read that next; tick items + append session notes there. This file keeps the v0.1–v0.2.5 history + the shared session log below.

## P0 — Scaffold ✅
- [x] rustup install (rustc 1.96.0)
- [x] create-tauri-app scaffold (React-TS, bun)
- [x] CLAUDE.md + docs/
- [x] git init + initial commit
- [x] Gate: `bun run tauri dev` opens window, hello IPC works

## P1 — Connect + run
- [x] `driver/mod.rs`: shared types (Profile, ColumnMeta, StatementResult, DriverError)
- [x] `driver/postgres/`: connect (tokio-postgres, rustls no-verify ≈ psql sslmode=require), session registry in AppState
- [x] `secrets.rs`: keyring save/load password per profile (service `app.qwry`)
- [x] `appdb.rs`: rusqlite init, `profiles` table (no passwords)
- [x] Connection profiles UI: list + create/edit modal (host/port/db/user/ssl, prod flag)
- [x] Execute v0 via **simple protocol** (universal wire-text values, multi-statement free) — `tests/staging_smoke.rs` passes live against staging
- [x] PG error display (message + code + position)
- [x] Gate: connect to staging PG in-app, query renders ✅ (verified by user: 10 rows from product_flatlay_generations, 69.7ms)

## P2 — Streaming + grid
- [x] Statement splitter lexer (strings, dollar-quotes, nested block comments) + 8 unit tests
- [x] `execute.rs`: QueryEvent stream via `simple_query_raw` (public, streams!), ≤500-row batches, per-statement timing/affected — live-tested (120k rows → 50k cap + full count)
- [x] CancelToken wiring + ⌘. shortcut — live-tested (pg_sleep(30) killed <5s)
- [x] `ipc/`: Channel-based QueryEvent decoder, resultsStore with rAF-batched row flushing
- [x] Grid: virtualized rows+columns (TanStack Virtual), sticky header + sticky rownum column, column resize
- [x] Cell selection (drag/shift/arrows/⌘A), ⌘C TSV, right-click copy menu (TSV/CSV/JSON/Markdown/INSERT)
- [x] Row cap 50k (status shows "50,000 of N (capped)"); >8KB cell truncation marker …⧉
- [x] Gate: `SELECT * FROM generate_series(1,1000000)` scrolls smooth, ⌘. cancels mid-stream ✅ user-verified
- [x] **CHECKPOINT passed: custom DOM grid is smooth in WKWebView at 50k rows — Glide fallback not needed**
- [x] Fixes from user testing: clipboard via tauri-plugin-clipboard-manager (navigator.clipboard dead on dev origin), copy-menu backdrop swallowed clicks, native text-selection during drag, row/col/select-all via rownum+header+corner

## P3 — Schema + basic completion
- [x] `introspect.rs`: 4 pg_catalog json_agg queries in one round trip → SchemaSnapshot (tables+columns+PKs, FKs, functions incl. pg_catalog, schemas) — live: 128 tables/3475 fns in 268ms (indexes deferred to P7 structure tab)
- [x] Schema fetch on connect + DDL sniff after runs + manual ⌘R
- [x] Sidebar: schema → tables/views tree under connected profile, fuzzy filter ⌘⇧F, double-click → SELECT LIMIT 100
- [x] CodeMirror SqlEditor: lang-sql PostgreSQL dialect, snapshot-fed schema completion (Compartment-swapped), dark theme, history/brackets/active-line
- [x] ⌘Enter runs selection-if-any else buffer; ⌘. cancel
- [ ] Gate: completion offers tables after FROM, columns after SELECT (user verify)

## P4 — Intellisense deep
- [x] `completion/context.ts`: token walk → clause + FROM/JOIN/UPDATE/INSERT tables + alias map + dot-qualifier
- [x] `completion/engine.ts`: clause-scoped sources — columns from in-scope tables (alias detail), `alias.` exact-table columns, `schema.` tables, aliases, functions w/ signatures, keywords; CM fuzzy + boost ranking
- [x] FK-aware suggestions: after JOIN → `table ON cond` one-shot; after ON → `a.col = b.col` pairs (in engine.ts joinOptions)
- [x] Usage ranking: accepted completions bump localStorage counts → log boost (appdb persistence later if needed)
- [x] Snippets: sel/ins/upd/cnt templates at statement start
- [x] PG error position → squiggle + lint gutter (when executed text == buffer)
- [x] Gate: column scoping + FK joins + function gating ✅ user-verified
- [x] User-feedback round: functions out of typed flow (settings toggle + ^Space), ⌘⇧U/right-click searchable function palette, editor context menu

## P5 — Editable results
- [x] `edit.rs`: editability via `prepare()` (no re-execution) — table_oid/column_id → PK presence; reasons for read-only (computed / no PK / PK not selected). ctid fallback NOT implemented (deferred; reasons are actionable instead)
- [x] `editability`/`edits_preview`/`edits_apply` commands; UPDATE gen with `'val'::type` casts, one transaction, RETURNING ::text refresh; rows-matched≠1 reported per edit — live-tested on staging scratch table incl. JOIN base-col edit + SET NULL
- [x] Grid editing: double-click or Enter on focused cell, input overlay, Esc revert, ∅ button / ⌘⇧⌫ sets NULL, editing-back-to-original clears the edit
- [x] editsStore: pending keyed (stmt,row,col), dirty ✎ amber cells, PK values always taken from ORIGINAL row data, discard all
- [x] ⌘S (or status-bar Commit) → SQL preview modal → apply → RETURNING patches grid in place
- [x] Gate: edit base-table cell from a JOIN query in-app, persists; computed col shows reason tooltip ✅ user-verified
- [x] Keyboard flow polish: Enter commits in preview modal, esc cancels, ⌘⇧D discard all

## P6 — Inspector + jsonb
- [x] Inspector panel (right, ⌘I toggle): focused cell full view, auto JSON detection, raw/tree modes, Copy + Copy-pretty, type + read-only reason display
- [x] Truncated cells: full value auto-fetched by PK when editability map allows
- [x] JsonTree: collapsible (depth-2 default), click value copies, ⌥-click copies json path
- [x] jsonb edit: pretty textarea with live validation → staged into pending edits (⌘S commits like any cell)
- [ ] JsonTree search (deferred — palette/⌘F later)
- [x] User-feedback round: ⌘I intercepted in editor (CM binds Mod-i to selectParentSyntax) + defaultPrevented guard on the window keymap (double-fire = no-op toggle), resizable inspector (drag left edge, persisted), Raw→Tree dead-end fixed, hide button + right-edge reopen tab
- [x] Gate: inspector flows ✅ user-verified

## P7 — Table browser
- [x] Sidebar single-click → browser (double-click → SELECT in editor); Data/Structure tabs, close ✕ back to editor, refresh
- [x] Filter builder: col + op (=,!=,<,>,<=,>=,LIKE,ILIKE,IN,IS [NOT] NULL) + value, enable checkboxes, AND-joined; sort dropdown; generated SQL runs through normal pipeline → grid stays editable (P5 reuse for free)
- [x] Infinite scroll: LIMIT grows by 1000 near grid bottom (nearEndHook in Grid)
- [x] Structure tab: columns (type/nullable/default/PK badge), FKs both directions, indexes (added to introspection — 5th json_agg query)
- [ ] Insert row / delete row (deferred — needs guarded UX, do with P10 safety work)
- [x] Feedback rounds: AND/OR filter connectors (parenthesized, left-to-right), cell-editor Enter-bubble bug (reopened editor — stopPropagation), blur=save/Esc=discard semantics, Set NULL/EMPTY in grid context menu for selections, inspector plain-text Edit, JSON cells double-click → inspector edit mode (never inline), sort = searchable popover (PK & time group first, click flips direction, Clear sort row)
- [x] Gate: browse + filter + edit without writing SQL ✅ user-verified

## P8 — Tabs, history, palette
- [x] Tabs: bar above editor, ⌘T new / ⌘W close (last tab resets, never zero), double-click rename, active tab mirrors editor buffer, debounced replace-all persistence in appdb `tabs`, restore on launch
- [x] History: every run logged to appdb `history` (profile, sql, total ms, total rows); LIKE search command
- [x] ⌘K palette (cmdk): Actions (run/new tab/inspector/refresh schema), Tables (→ browser), Connections (→ connect, PROD badge), History (live-searched, → new tab)
- [x] Feedback round: ⌃Tab cycle, ⌘1-9/0 jump, ⌘⇧T restore-closed (20-deep stack), ⌘W context-aware (table view first), saved-queries section pinned to sidebar bottom, tab↔saved linking via tabs.saved_id (⌘S upserts same entry, two-way rename sync, click focuses existing tab), history clear actions in palette
- [x] **GOTCHA: `window.confirm()` is a silent no-op stub in Tauri WKWebView** — never gate actions on it; use two-click arm pattern (see SavedQueries/ProfileList delete)
- [x] Gate: tabs/saved/shortcuts ✅ user-verified (restart-restore trusted, retest later)

## P9 — EXPLAIN viz
- [x] ⌘E / Explain button → EXPLAIN (ANALYZE, BUFFERS, FORMAT JSON), selection-aware
- [x] Plan tree: per-node self-time bars (hot ≥20% red / warm ≥5% amber), actual-vs-est rows with 100× misestimate badge, ×loops, relation/index names, execution+planning summary, esc closes
- [x] Gate: readable tree for join query ✅ user-verified
- [ ] NOTE: ⌘E on UPDATE/DELETE executes it (ANALYZE) — P10 adds the guard

## P10 — Polish
- [x] Springs (`design/springs.ts` = only spring source): pop-in for palette/fn-search/edit-preview/danger modal, snappy menus (context menus, sort popover); commit flash (green fade on RETURNING-confirmed cells); never on scroll/typing
- [x] Traffic-light inset: titleBarStyle Overlay + hiddenTitle, drag regions on sidebar header + tab bar, sidebar top padding
- [x] Safety: UPDATE/DELETE-without-WHERE → DangerModal (custom — window.confirm is a stub) listing exact statements; EXPLAIN ANALYZE on mutating SQL → same guard; 3px red prod-strip when active connection is_prod
- [ ] Vibrancy sidebar (deferred — needs transparent window + effect tuning, risky)
- [ ] Light theme (deferred — user is dark-only; needs token set + second CM theme)
- [ ] App icon (deferred — needs artwork; template icon shipping)
- [ ] `tauri build` dmg (ad-hoc) — run at session end
- [x] Feedback fixes: drag-region needed explicit `core:window:allow-start-dragging` permission (dblclick-maximize worked but drag silently denied — asymmetric!); spring transform overwrote fn-search's translateX centering → use flex centering on backdrops, never transform, anywhere motion animates
- [x] Gate: keyboard map + danger guard + window chrome ✅ user-verified

---

# v0.1.5 — remaining backlog, phased (session 2+)

Same protocol as v0.1: build a phase, hit its gate, get user review, tick, next.
Order = daily-value + risk first, cosmetic mid, big-build last. Glide reeval (#11) is NOT a phase — a checkpoint only if grid feels slow.

## P1.1 — Edit power
- [x] Batched multi-cell row UPDATE: `plan_edits` groups by (table, row) → one `UPDATE … SET a=,b=,c= WHERE pk RETURNING a::text,b::text` per row; results map back per-cell via RETURNING order. Preview + apply share `plan_edits`.
- [x] ctid-fallback editing: result column named `ctid` (type `tid`) with a source table is detected as a row locator; when no usable PK is in the result, `pk_cols[oid] = [ctid_col]` and cells become editable with `warn` = "editing via ctid". `is_ctid` columns themselves read-only. WHERE uses `ctid = '…'::tid`; ctid name hardcoded (no pg_attribute row for system cols).
- [x] Gate: 3-cell row edit → ONE update; PK-less table edit via ctid ✅ user-verified
- NOTE: ctid moves on UPDATE — a 2nd edit to the same row before re-running matches 0 rows → EditResult.ok=false ("0 rows matched"), surfaced not silent. Acceptable for a fallback.

## P1.2 — Row lifecycle (browser)
- [x] Insert row: `+`/"Add row" in browser header (data tab, ordinary tables) → springy `InsertPanel`, one field/column. Blank = DEFAULT (col omitted), ∅ = NULL, typed = value. ⌘↵ insert / Esc cancel. Backend `insert_row` (text literals coerce to col type), then reload.
- [x] Delete row: grid context menu → red "Delete row(s)" (shown only when exactly ONE source table has a locator) → DangerModal with WHERE preview → backend `delete_rows` (one txn, RETURNING ctid, must match 1) → reload via re-run of executedSql.
- [x] Browser auto-includes `ctid` for PK-less ordinary tables (`SELECT ctid, *`) → fully editable/insert/delete via the P1.1 ctid engine.
- [x] Fixes: header icon `+` tooltip "Add row"; global autocorrect killer (`app/noAutocorrect.ts` — WKWebView forces macOS autocorrect/autocapitalize/substitution on every field; no global switch, so stamp `autocorrect/autocapitalize/spellcheck=off` per field + MutationObserver for new ones; opt-out `data-allow-autocorrect`).
- [x] Gate: insert + delete in browser ✅ user-verified

## P1.3 — JSON power (inspector)
- [x] JsonTree search: in-tree search box + ⌘F; `computeSearch` walks once → {visible, forceOpen, hits}; filters to matching subtrees, highlights match (`<mark>`), matched-key reveals its whole subtree (`forced` flag); hit counter + ⏎/⇧⏎/↑↓ nav with scrollIntoView; Esc clears.
- [x] Tree-mode editing: click leaf → inline type-preserving edit (number→number, bool→bool, invalid number rejected); click key → rename (order-preserving `renameKeyIn`); each commit `setIn`/rebuild → stages whole JSON into pending edits (⌘S commits). ⌥-click copies path; read-only cells keep click-to-copy. JsonTree gained `editable`/`onChange` props; Inspector wires `editable={!!editMeta?.editable}`.
- [x] Node ids via `JSON.stringify(path)`; `displayPath` for `a.b[0]` copy. (Earlier draft used a `` separator — replaced, don't reintroduce invisible-char joins.)
- [x] Gate: search keys + edit nested value/key in tree → commits ✅ user-verified

## P1.4 — Transactions (per-tab sessions)
- [x] Dedicated session per query tab: `connections.tabSessions` keyed `skey(profile,tab)`, `ensureTabSession` creates lazily on first run (backend was already multi-session — frontend-only change). `results.executedSessionId` records the running session; edits/insert/delete/EXPLAIN/full-value-fetch all reuse it so they share the tab's txn/temp state.
- [x] Schema introspection isolated on the per-profile **primary** session (`sessions[profileId]`), never a tab session — DDL refresh + ⌘R use it.
- [x] Open-tx indicator: amber dot on tab when a BEGIN is open (sniffed from executed statement heads, batch-abort aware); clears on COMMIT/ROLLBACK/END. `txTabs` keyed by skey.
- [x] Lifecycle: tab close → `closeTabSessions` disconnects its session(s); reconnect (`connect`) drops stale tabSessions/txTabs for that profile.
- [x] Gate: BEGIN+mutate visible in same tab, invisible in a 2nd tab, ROLLBACK reverts ✅ user-verified
- NOTE: tx indicator is a SQL-sniff heuristic (tokio-postgres doesn't expose ReadyForQuery status). Each tab = one extra PG connection.

## P1.5 — Light theme
- [x] Dark = `:root`; light = `:root[data-theme="light"]` full token override. Tokenized syntax colours (`--syn-*`) so editor highlight + json tree + structure switch; search highlight got `--hl-bg/--hl-fg` (was dark-on-amber, illegible in light); `--cm-active-line`, `--shadow-pop`, `--warn-soft` tokens added.
- [x] CodeMirror: theme fully CSS-var driven; `qwryTheme(dark)` parametric `dark` flag; editor **remounts on theme change** (SqlEditor effect dep `isDark`) — text preserved, cursor/undo reset (fine for a rare toggle).
- [x] `settings.theme` = system|dark|light persisted (partialize); `resolved` recomputed each launch; applied to `<html data-theme>` at import (no flash) + `prefers-color-scheme` listener for system-follow. Palette → Appearance group (Dark/Light/System, ✓ current).
- [x] Gate: every surface legible light+dark, system-follow, persists ✅ user-verified
- NOTE: all `color: white` in CSS sit on accent/danger backgrounds (fine both themes); box-shadow/backdrop rgba(0,0,0,…) left as-is (shadows read in both).

## P1.6 — Native skin
- [x] Vibrancy sidebar: `window-vibrancy` applies NSVisualEffectMaterial::Sidebar (FollowsWindowActiveState) in lib.rs setup; window `transparent:true` + `app.macOSPrivateApi:true` + tauri feature `macos-private-api` (feature MUST match conf flag or build.rs fails). body+sidebar transparent so material shows there; `.main-area`/`.inspector-pane` opaque so content never bleeds.
- [x] App icon: `src-tauri/icons/qwry-icon.svg` (gradient squircle + DB cylinder + cursor) → `rsvg-convert` 1024 PNG → `bun tauri icon` regenerated full set. Dock/Finder icon only shows in the bundled .app (dev runs the bare binary).
- [x] Gate: vibrancy light+dark + real icon in Finder/dock ✅ user-verified
- NOTE: built .app + dmg at `src-tauri/target/release/bundle/` — rebuild after P1.7 tunnel work for the final artifact.

## P1.7 — SSH tunnel
- [x] `tunnel.rs`: spawns `ssh -N -L 127.0.0.1:<free>:<dbhost>:<dbport> [user@]<sshhost>` with BatchMode=yes (key/agent only, no prompts), ExitOnForwardFailure, keepalives; honors ~/.ssh/config (aliases/ProxyJump/keys); free-port pick, ~10s TcpStream health check, ssh-stderr surfaced on failure, `kill_on_drop` reaps it. Needed tokio `net` feature.
- [x] One tunnel **shared per profile** (`AppState.tunnels`, `ensure_tunnel` get-or-start, race-safe), reused across the profile's per-tab sessions; lives for app lifetime (kill_on_drop on exit).
- [x] `postgres::connect(profile, password, addr: Option<(&str,u16)>)` — addr overrides host/port for the tunnel local endpoint; TLS no-verify so hostname mismatch is fine. Profile gained `ssh_host/ssh_port/ssh_user/ssh_key` (JSON blob → no DB migration). ProfileForm "SSH tunnel" section.
- [x] Gate: connect + query through an SSH tunnel ✅ user-verified
- NOTE: couldn't self-test (localhost sshd off; didn't touch prod) — user verified with their bastion.
- [x] (v0.2) Tunnel **liveness**: `ensure_tunnel` health-checks the cached tunnel (`Tunnel::is_alive` — TCP to the local port) and restarts it if the ssh died (bastion idle / network drop) instead of reusing a dead one → fixed "connection failed" on a previously-working tunnel after it dropped.

---

# v0.2 — visual overhaul + UX/QOL (session 3+, iterative)

Major UI overhaul to "best-looking DB client" + per-tab results. Iterative & communicative — features grow with user feedback, not fully pre-planned. Branch `v0.2` off `v0.1.5`.

Design (from user wireframes): floating rounded cards on a themed-glass gutter — left **connection rail** (circular avatars) · **sidebar card** (Databases / Tables / Saved) · **main card** (tabs / query / results) · inspector slides in. Top breadcrumb. Feel = Linear/Arc (soft, springy).

## P2.1 — layout shell ✅
- [x] `v2-shell`: transparent window, vibrancy in gutters; floating cards (`.card` = radius-panel + shadow + hairline border + top sheen) with `--gutter`/`--rail-w` tokens. ConnectionRail (basic) + SidebarCard (DB head + SchemaTree + SavedQueries) + main-card; inspector as its own card + flush reopen tab. `panelIn`/`railItemIn` springs. Old `.app-shell/.sidebar/.main-area` removed.

## P2.2 — theme system + colour overhaul ✅
- [x] Engine `design/theme.ts`: palette = seeds → derive full token set as inline CSS vars at startup (no flash). TWO kinds: **hue** (curated: accent+hue+tint → tinted neutral ramp) and **anchors** (custom: bg/fg/primary/secondary → surfaces derived by sRGB mix). `--accent-fg` auto-contrast (Pikachu yellow → dark text); swapped all `color:white`-on-accent.
- [x] 8 Pokémon palettes (Blastoise default) + mode system/light/dark (independent axis). Themed `--glass-tint` (gutter matches scheme).
- [x] Custom themes = explicit anchors (AMOLED/neutral/high-contrast all reachable, no mangling). **Dual-mode**: authored once, engine synthesises the opposite light/dark variant (flip surface lightness, keep character+accents, contrast-adapt accent) → mode toggle flips custom themes too. Edit (pencil) custom; fork (copy) built-in → anchors. ThemePicker modal (swatchbook button in titlebar / ⌘K → Customize theme); active = ring (no check). Stable custom ids.
- [x] Gate: palettes + modes + custom AMOLED/neutral + dual-mode flip ✅ user-verified

## P2.3 — connection rail + customization + home ✅
- [x] **P2.3a**: shared `Avatar` (color + glyph: letter/emoji/curated lucide icon, auto-contrast glyph) + `glyph` on Profile (JSON, no migration). Rail uses Avatar + 🏠 home button; ＋/right-click → editor; click green → instant open. Home surface (`homeMode` in connections store): **Dashboard** (connection grid + New, click connects / green opens instantly) + **ConnectionEditor** (full panel, replaces modal: avatar preview + name + color swatches/picker + glyph picker + connection fields + SSH + Delete/Cancel/Save/Save&Connect). App opens on home; sidebar hidden during home.
- [x] **Connection liveness**: `connect` takes an `on_close` Box callback → driver task fires it when the socket dies (abort on intentional disconnect skips it); `commands::connect` emits `session-closed`. Frontend `markDisconnected` flips the dot gray + drops dead sessions; `ensureTabSession` auto-reconnects on next run; query-failure also flips the dot. Work view stays mounted when disconnected (gate = activeProfile, not live `connected`).
- [x] Run/Explain **buttons** now honour the editor selection (via `editorRunText`), matching ⌘↵.
- [x] **P2.3b**: DB-switcher (`DbSwitcher`) in the sidebar DB header → lists `pg_database` → picking another clones the connection (`clone_connection`: new sibling, password carried over) + connects. Recent-activity strip on the dashboard (`history_recent` across profiles → avatar + sql + time, click connects + opens in a tab).
- [x] **Rail drag-reorder**: `motion` `Reorder.Group`/`Item` (spring layout, whileDrag lift); persisted via `set_profile_order` + a `position` column (additive migration, backfilled from rowid); debounced save after drop. Tap connects / right-click edits still work (threshold).

## P2.4 — per-tab results ✅
- [x] `useResults` + `useEdits` keyed **by tab** (`byTab`) with the active tab mirrored to the top-level fields — every consumer reads unchanged, and a background tab's stream can't corrupt the visible tab (each event/rAF-flush writes to its own tab id). Per-tab: statements/activeStatement/running/totalMs/executedSql/executedSessionId/globalError + editability maps/pending/flash. `committing`/`preview`/`lastError` stay global (one commit at a time). `patchStatement` patches the active tab on commit.
- [x] `setActive`/`syncActive` driven by `useTabs.activeId` subscription; closed tabs cleared (`clearTab`/`resetTab`) via the same subscription (diff tab-id set) — no tabs→results import cycle.
- [x] Gate: tab-switch keeps each tab's grid/edits; background run doesn't bleed; close drops state ✅ user-verified.

## P2.5 — inspector redesign ✅
- [x] Tidy chrome: header (col name + type pill + row + hide), status **chips** (read-only 🔒 / ctid ⚠ / pending ✎ / loading), compact **icon toolbar**. Sits transparently inside the floating inspector-card.
- [x] **Structured formatting**: JSON *and* PG arrays (`text[]`, `int[]`, …) parse via `format.ts` (`structuredValue`/`parsePgArray`) → tree view. Arrays are **editable** too: `jsToPgArray` serializes the JS value back to a PG array literal, staged + cast `'{…}'::_text`/`::_int4` (verified PG coerces quoted elements). JSON stages as JSON.
- [x] **Colorized raw view+edit** via a CodeMirror `JsonField` (`@codemirror/lang-json` + qwryHighlight, readOnly toggle). Replaced a transparent-textarea-over-`<pre>` overlay that WKWebView's native textarea background kept covering — 3 failed iterations; CM renders its own DOM, reliable. Raw = the editor (Stage/Discard on change); arrays read-only colorized.
- [x] **Split copy button**: click = formatted; caret → Copy formatted / Copy raw.
- [x] Gate: arrays + JSON formatted, colorized raw/edit, copy split ✅ user-verified
- NOTE: GOTCHA — don't try transparent-textarea highlight overlays in WKWebView (opaque field bg wins); use CodeMirror for colorized editing.

## P2.6 — breadcrumb + animation polish ✅
- [x] Breadcrumb: `connection / database / context` (context = browsed table name or active tab name); fades on change (`swapIn`).
- [x] Inspector open/close animates its **width** (CSS transition) so the main card reflows in lockstep — content right-anchored fixed-width (reads as a slide), transition disabled while drag-resizing (`resizing` flag), negative margin cancels the flex gutter when collapsed. (Replaced a motion x-slide that desynced from the instant flex shrink.)
- [x] DB-switcher fixes: query a **live** session (active tab via ensureTabSession, then executedSessionId, then primary) — the primary introspect session goes stale across dev rebuilds; and **reuse an existing connection** for the picked db (same host/port/user) instead of always cloning.
- [x] Gate: breadcrumb, smooth inspector push, DB switch reuses existing ✅ user-verified
- NOTE: smooth "push" needs the content area to reflow over time (animate width), not a transform — accepted the grid re-measure cost.

## P2.7 — connection-edit robustness + logo + inspector polish ✅
- [x] **Editing a saved connection takes effect**: `connSig()` (host/port/dbname/user/sslmode/ssh_*) detects connection-affecting edits → `invalidateProfile` closes the profile's sessions + drops its cached tunnel (`invalidate_profile` command); cosmetic edits (name/color/glyph) leave the live connection alone. Tunnel carries a `spec`; `ensure_tunnel` rebuilds on mismatch so a repointed host can't keep forwarding to the old one (was hitting a read replica → "cannot execute UPDATE in a read-only transaction").
- [x] **Commit/preview re-resolve a live session** via `ensureTabSession` instead of the result's `executedSessionId` — fixes "no such session" after a repoint / network drop / dev rebuild (edits are pk-based, apply cleanly on a fresh connection).
- [x] **ConnectionEditor shortcuts** Esc/⌘S/⌘↵ via a capture-phase window listener (WKWebView buttons don't take focus on click, so keydown wasn't bubbling). Rail icons update live on customise (re-map items by id, no reload).
- [x] **New app logo**: cylinder logo reshaped to Apple's icon grid (`scripts/make_icon.py` → `icon-master.png`), all sizes via `tauri icon`, favicon + window title refreshed.
- [x] **Inspector scalar polish**: scalar values render in a rounded value card with double-click-to-edit; the scalar editor auto-grows to its content (no more full-panel balloon, actions stay put) with Esc/⌘↵ hints; same hints added to the JSON/array editor buttons.

## P2.8 — v0.2.5 QOL batch ✅
- [x] **Tables open as tabs**: `Tab.kind` (query|table) + `table` ref; tables are session-only tabs beside query tabs with kind icons; multiple independent table tabs (per-tab `byTab` browser state); opening an open table focuses it.
- [x] **Default tab name** `new qwry` (no number).
- [x] **Data-type column-header icons** from the editability map's `type_name` (`grid/typeIcon.tsx`, `--syn-*` colored), in query results + browser.
- [x] **Inline add-row**: sticky blank draft row at the grid top (⌘↵ insert / Esc cancel / ∅ NULL), replacing the overlay; add-row button moved to the bottom toolbar (left; Filter+Sort right).
- [x] **Close-with-unsaved-edits prompt** (`closeGuard` + `CloseGuardModal`): Esc keep · Enter discard&close · ⌘↵ commit&close; covers query + table tabs.
- [x] **Connect-error surfacing** (bonus): TLS→plain fallback no longer masks server errors (`as_db_error`); global `ConnToast` shows the real reason on any view.
- [x] **Tab strip**: active tab smooth scroll-into-view (`inline:"nearest"`, reveals + for last tab); scrollbar hidden.

---

## Session log

### 2026-07-24 — v0.9.12-chameleon: per-connection themes + Switch species (session 12, continued)
User ask: each connection can hold its own theme, plus a toggle that locks one theme everywhere. Design ran through a 3-option artifact (scope chip / scope segment / connection ledger) → A-minimal won: NO scope chrome, just a bottom row `One Theme Everywhere` + owner dots on cards as the pick-landed-here feedback. ENGINE: sparse `connThemes {profileId → {paletteId, match}}` — absent entry follows the app theme (mode flip retints nothing until a pick lands); writes route through `scopedChoice` (active connection when the toggle is off, app-theme fields otherwise) and the picker radio derives from the same `themeChoice` resolution, so display scope ≡ write scope by construction; toggle ON promotes the on-screen choice to global and leaves the map dormant (OFF restores assignments); `activeConnId` runtime-only, pushed from connections (workspace identity, connected or not); profile delete prunes its entry; custom-theme delete falls holders to DEFAULT_PALETTE. Review (fable, no S1): F1 global paletteId sanitize accepted any string → orphaned id = ZERO active cards while the app silently wore Blastoise (now validated against the known-palette set); F2 rehydrate dropped stale-palette entries while runtime delete kept them on the default — unified on degrade-to-default. **Switch species** (DESIGN rule 1 row + `<Switch>`/`.switch` in tokens.css): hidden native checkbox is the truth (role=switch, Space, label association), ALL visual state via `:has()` (no class mirroring), spring knob on --ease-spring with the macOS press-stretch (width grows held, springs on release); adopted on 6 setting/feature rows (theme row, Wrap Long Lines, Functions in Autocomplete, Production Connection, SSH Tunnel, Header Row); browse filter tick + CM search panels stay checkboxes (selection-in-content, HIG). Scope row iterated to System Settings layout (name · ⓘ · switch right; only the control toggles) after the ⓘ-inside-label bug (reading the tooltip flipped the setting). GOTCHA: WKWebView's native `title` tooltip is slow/unreliable — read as "ⓘ does nothing"; replaced with an instant styled `.tp-tip` bubble (real element, keyed On/Off lines). LEDGERED: app-wide `title=` audit — every load-bearing tooltip rides the same slow native path; promote the .tp-tip pattern to a primitive if more surfaces need it. ADDENDUM (fix/switch-glass): switch went liquid-glass — 36px track (17px travel), --dur-slow spring, translucent fill over backdrop-filter blur(12px)/saturate(160%) with lens shadows, specular gradient knob; NSGlassEffectView deliberately rejected for in-content controls (refracts through the WINDOW, not the modal under the knob — the gutter is the window-edge special case where window-behind IS the right source). Ellipsis-contract audit both directions: 7 existing `…` controls verified correct, native menu compliant (Save to File bare per macOS Save convention), 3 Rename surfaces gained the missing `…`. User challenged the convention → verified against Apple's own apps (Safari AutoFill Edit…, System Settings Details…) → rule 2 kept, annotated owner-verified.

### 2026-07-23 — v0.9.11-truecolor: stable chrome + Match Connection + assorted user nits (session 12, continued)
Dogfood wave. **Stable-chrome law** (DESIGN rule 2 addendum): interactions must not resize their container — fixed: saved-row hover growth (visibility reveal + focus-within), tb-footer 28px + count states as words (`N rows · approx/exact`, the ≈ smudge killed), status-bar 24px, tab-strip pin growth (glyph 20→18), and the qb action slot saga (stretch→align→conditional→**equalized faces + reserved slot + centered labels** — the final law: equalize copy first, then reserve). Sweep agent verified the rest of the chrome clean; ledgered for user eyes: tab tx-dot inline insert, inspector tools wrap on narrow, inspector notice chips. **Browse scroll survives sort** (remount-loss: sort re-run cleared results → grid unmounted → scroller reset; browse tabs stash scrollLeft, vertical resets deliberately). **Underline-input family** (ce-name + 3 siblings): global focus ring double-chromed their underline treatment — ring killed where the underline IS the treatment. **Titlebar**: flat-wash active tools drop the boxy chip (icon-only accent), chip returns under [data-glassy]. **Match Connection** (ThemePicker card): theme derives from the active connection's color — danger-band steer, hex moat, neutral ramp, runtime-only connAccent, stable-hash colors for colorless profiles (no action-at-a-distance retints). Review (fable): 2 blockers fixed (custom-theme save broke the radio invariant; unvalidated profile hex bypassed the NaN-brick moat) + polish tier. Rule-9 evidence = user live-verified every state in dev (HMR loop). Late nits (release-build loop, user-driven one at a time): square keycaps (`.kbd-cap` 20px square base; cheatsheet + palette caps equalized to the same geometry), inspector empty-state teaching hint (⌘I cap leads, "Select a cell to inspect" follows — read hierarchy was inverted), Match Connection card truncation ("Mat…") → single conic-gradient rainbow wheel (user's idea: one universal any-color dot instead of four preview dots).


### 2026-07-22 — v0.9.10-lifeline: SSH control-lane cancel + instant feedback (session 12, continued)
User repro: ⌘. on a big SSH-tunneled scan → both cancel tiers time out, terminate does nothing, rows freeze at 50k until the server finishes the whole scan; even staging cancels felt dead for a beat. ROOT CAUSE: one `ssh -L` process = ONE TCP connection; every cancel needs a NEW connection whose handshake replies queue behind megabytes of buffered row bytes (head-of-line blocking) → 1.5s/5s deadlines always blow; ROW_CAP auto-cancel was a silent `let _ = token_cancel()` → full-table drain; disconnect removed the session but the execute's own Arc drained to completion. FIX: (1) CONTROL LANE — second pre-spawned ssh child per tunnel (own TCP; `ControlMaster=no ControlPath=none` or a user's mux config folds the lanes back together), token cancel dials it via `cancel_query_raw`, OOB/terminate try control cfg first with a 2s sub-deadline then fall back to the data lane; degraded gracefully when the lane can't spawn. (2) hard client-side abort (shared single-fire CloseNotifier + conn-task AbortHandle): disconnect always aborts after the bounded cancel, terminate aborts win-or-lose, cap-cancel aborts only when BOTH tiers fail and resolves the stream error as the capped completion. (3) instant feedback: per-tab `cancelling` flag set synchronously (Cancelling… button, ⏳ cancelling status), cleared only when the run truly settles. Two-lens review (both NO-SHIP→fixed): C1 cap-branch cancel() ladder polled a busy flag its own caller held → always escalated + killed healthy sessions on max_connections servers (now direct two-tier, abort only on double failure); half-dead-lane sub-deadlines; sslmode=require actually requires now (tokio-postgres Prefer default silently downgraded — pre-existing); ordered disconnects classify as cancels not errors (shared terminatedSessions module, dropSession marker on 10 teardown sites). LEDGERED: control lane never respawns mid-tunnel-life (is_alive probes data port only); capped-completion row_count may under-report on a real mid-drain death (cosmetic); F/H toast copy ("Connection lost" overstates a deliberate cap-abort; terminate toast races markDisconnected); concurrent lane spawns vs strict bastion MaxStartups untested. LIVE-TEST PENDING (user, real bastion): C-cell felt sequence, backlog drain time, two-spawn connect, abort-surfaces-as-Err.


### 2026-07-22 — v0.9.9-onelook: the design-law migration (session 12, continued)
The whole app migrated onto docs/DESIGN.md (born this session from a 2-agent audit: ~80 control rule-sets/9 species, pressed states on 2, 163 off-grid literals, 8 icon sizes, 5 motion dialects, load-bearing info at faint). PR #18 (law + primitives + design-lint, census 597) then this wave: 4 file-disjoint agents (GRID/PANEL/SHELL/SURFACE) + orchestrator seams took the census 597→0 — btnish/iconbtn/linkish adopted everywhere (locals stripped to layout deltas), `<Kbd>` renders all JSX chord hints (⌃⌥⇧⌘ order + ↩ structural), faint→muted re-tiering, spacing/motion tokenized, em dashes out of UI strings, `.on/.sel/.open`→`.active`/`.hot`, ProfileForm/ProfileList dead code deleted, Record View joined stepper + real ∅ affordance + ⇧⌘⌫ parity (all three editors). Two-lens review (correctness fable + design compliance): both SHIP; fixed in-wave — TRUNCATED_LOCATOR_MSG unified, status-link.danger pressed hue, tp-action keyboard-invisible reveal, tb-seg pressed state, jt-hitcount/capnote/rv-keys re-tiered, multi-line-JSX em-dash lint gap closed (block-comment state machine). INCIDENT: an agent ran `git stash` mid-fleet to measure baselines and the pop collided with a concurrent write — recovered fully (tree superseded stash on every file, verified by diff, then dropped). Lesson: fleet agents NEVER run repo-wide git state ops; measure via file reads. LEDGERED (design lens): extract .seg/.chipish bases; re-base rv-navbtn on iconbtn.bordered; ShortcutsModal+Palette hand-typed chords → `<Kbd>` (needs multi-chord row model) + cap-per-key vs cap-per-chord decision; palette kbd pill vs bare-in-menus; ZenScreen caps deviation; on-fill kbd mix drift (75/78/82%); tab-close species; tree-col-type tier; ZenScreen 0.8s JS literal; lint can't see hand-typed chords; "Close esc" word-key titles.

### 2026-07-22 — v0.9.8-onerow: RowPeek absorbed into Record View + inspector narrow mode (session 12, continued)
User flagged two things: the inspector at very low widths mangled its Stage/Discard buttons, and Space (row details) vs ⇧Space (record view) looked like two features for one purpose. They were — ROADMAP archaeology shows RowPeek accreted editing until RecordView shipped as its "big sibling" and never replaced it; three past incidents patched BOTH modals identically (hidden-column leak, portal keystroke leak, key-swallow sweep). **RowPeek deleted**; Space AND ⇧Space open RecordView; context menu unified on the sanctioned `Open as Record` (WRITING.md terminology exception); all `rowpeek-*` CSS renamed `rv-*` and consolidated into the record-view section (+ subtle `.rv-row:hover`); dead `.null` modifier dropped. Review round (fable) added: **Space closes what Space opened** (Quick Look grammar — Modal onKey, bails on focused controls; editors safe by construction via their own overlay layers) and the naming unification itself. Inspector narrow fix: `white-space: nowrap` on edit-action buttons (hint used to wrap INSIDE the fixed-height button and bleed over content) + `.inspector.narrow` (store width < 280) hides `.insp-key` hints — container query deliberately REJECTED: `container-type` on any ancestor becomes the containing block for the `position: fixed` `.insp-copy-backdrop`, shrinking click-away to the panel. Resize handler now writes the store on threshold-cross during the drag (was mouseup-only — narrow mode never engaged mid-gesture, exactly when it matters).

### 2026-07-21 — PR #17 audit round: chord fixes hardened (session 12, continued)
A TWIN session (user accidentally re-issued work) authored the key-swallow + inspector-⌘F-focus commits on fix/orphaned-global-chords; this session ran the missing adversarial round (LESSONS #12 — no wave merges unreviewed): 1 S1 + 3 S2 fixed on the branch. S1 (new in the twin's work): the inspector card never unmounts (collapses to width 0) — focus could TRAP inside the invisible panel (rail-button close, or ⌘I-close with a dead prev element) and ⌘F + typing landed in a hidden input; fixed with a close-path focus-restore effect covering ALL five toggle paths + both ⌘F claims gated on inspector open. S2s: grid onPaste (⌘V is a native paste event — keydown guards don't see it) now yields for peek/record/draft-pop/contenteditable, closing the stage-behind-two-modals corruption path; the "exhaustive" swallow sweep missed ResultsPane's row-filter + SavedQueries' filter (one in a file the same commit edited — counts lie, sweeps need extraction, same lesson as the copy pass); Inspector's scalar editor claimed ⌘↵ (also the Run accelerator) and dropped focus to body on Esc. Cheap S3s: draftPop added to the grid grammar deps (guard survived only via useSelection's per-render identity — a memoization away from dying), JsonTree Enter-commit refocuses the shell (success-gated; invalid values keep the error input). LEDGERED: ⌘F dead-end on scalar/raw inspector values (no search affordance), preInspectorFocus capture asymmetry across toggle paths (should move into the store), inline outline:none on the shell (tokens.css opt-out list + a visible scope cue).

### 2026-07-21 — fix/orphaned-global-chords (session 13)
User repro: ⌘I → ⌘F → ⌘I dead. Root cause: FindBar input's blanket `stopPropagation` killed every window chord while focused; sweep found the same pattern on 5 more surfaces (2 Settings inputs, ValuePop/RowPeek/RecordView portal containers). Fix: swallow only unmodified keys, ⌘/⌃ chords bubble (LESSONS.md #10, new). Adversarial review (house law #12) caught 2 regressions the blanket had been shielding: ⌘⇧Z undo-offer toast could revert a DB COMMIT from inside a text field (ResultsPane — now stands down for editable targets/overlays) and grid chords hijacking ⌘C/⌘V inside a structured draft-cell pop (Grid — draftPop guard); plus ⌘L overlay gate in TableBrowser. Gotcha: CM editing surfaces are contenteditable — tagName INPUT/TEXTAREA checks never catch them.

### 2026-07-21 — v0.9.7-copyedit: exhaustive copy leftovers + identifier register (session 12, continued)
User caught misses (ServerInfo modal) — the first sweep eyeballed files. This pass extracted 790 strings PROGRAMMATICALLY (titles/placeholders/labels/JSX text/confirm triples/store-surfaced errors/native menu) into an inventory and classified each: 33 more violations fixed across 21 files — alert-title register on every confirmDanger triple (title Title Case / body sentences / verb buttons), typographic apostrophes (’), status parens → · separators, shortcut-suffix style unified (`Close esc`, `Next row ⌘↓` — no parens), ExplainView headers. Code keys re-verified (STATUS_FILTERS tokens, `building copy…` prefixes, cmdk values untouched).
NEW WRITING.md rule from the user's screenshot: **identifier register** — user data (column/table/CTE names) never sits bare inside Title Case labels (`Sort quantity Ascending` read as chrome). Sanctioned forms: context supplies the object (`Sort Ascending`, `Hide Column`); the hint slot in mono (`Copy Name  quantity` — ctx-hint now mono, matching kbd/data register); inline only to disambiguate siblings (`Open Referenced orders →`). Applied: header menu Sort/Hide, `Run CTE Standalone` + name hint.

### 2026-07-21 — v0.9.6-plainspeak: copy polish + the law docs (session 12, continued)
User noticed the text didn't read "releasable" (mixed casing, `Copy (no password)`, register drift) and asked to LEARN the discipline first. Wrote the law: **docs/WRITING.md** (register system: Title Case controls / sentence-case prose, ellipsis contract, no parentheticals in labels, verb+object ≤4 words, terminology table with banned synonyms, typography register) and **docs/LESSONS.md** (11 bug-class lessons from this app's actual history — data integrity, UI, process). CLAUDE.md now binds both on every change AND instructs pushback when a request (incl. the user's) violates them — explicitly wanted.
Sweep: 2 agents, ~200 strings normalized across every surface incl. the native macOS menu (`Commit Edits or Save Query`, `Reopen Closed Tab`, `Actual Size`). Named offenders fixed: `Copy (no password)` → `Copy URL` under `Copy Connection URL`; `Production connection` → `Production Connection`; `Set empty string ''` → `Set Empty String` + hint; `Revert edits in selection` → `Discard Edits in Selection` (terminology: discard=staged, revert=post-commit inverse). SAFETY: every changed string grep-verified not to be a code key (cmdk value= match-keys, menu event ids, `building copy…` startsWith prefixes, store literals all preserved); HistoryPanel status chips got a display map so filter tokens stay lowercase. Deliberately-left registers documented in the agents' tables: terse status notes (`copy cancelled`), data-state markers (`NULL`, `∅ empty`, `auto`), gesture legends, SQL-keyword controls. LEDGERED: ProfileForm.tsx + ProfileList.tsx are dead code (imported nowhere) — delete next wave; ShortcutsModal descriptions keep prose register.

### 2026-07-21 — v0.9.5-truename: workspace identity honesty (session 12, continued)
User repro (S1): staging conn → ⌘K open internal_service_tools → switch to prod conn → ⌘K same table → Enter showed the STAGING tab's cached rows under prod branding, with no tab active in the strip. Root cause: openTable's already-open dedup matched schema+name over the GLOBAL tab list — selected another workspace's tab. Full investigation (14 findings) mapped the workspace model: v0.6 contract = pinned tabs run against the ACTIVE conn; data artifacts (results/edits/import/undo) follow their ORIGIN (executedProfileId/SessionId) — every bug was a seam where one half leaked unlabeled. Sharpest find (S1, worse than reported): prod-pinned tab under a staging rail rendered prod rows + committed ⌘S edits to prod with NO prod strip/chip, and a persisted write-unlock was invisible + un-relockable from that rail.
Fix set (2 agents + 2 inline): **stores** — openTable dedup profile-scoped (+ null-adopt via new adoptTab, making the old comment true); openSavedQuery same scoping (live sibling, reachable from sidebar + palette); select() refuses ids invisible in the workspace (dev-warn — activeId ∈ visible strip is now structural); run() gained {profileId} override — post-write reloads (insertRow via new reloadAfterWrite, import commit, Grid delete, undoLastCommit) re-run through the profile that EXECUTED the write; history/executedProfileId stamped with the profile actually used. **surfaces** — origin provenance banner above the grid when executedProfileId ≠ rail (origin-colored, "rows from X — edits & imports commit there · Run executes on Y"); prod ceremony keys on ORIGIN too (strip + chip bound to the origin session, labeled PROD·⟨name⟩, re-lockable from any rail; confirm names the profile); EditPreview prints "commits to ⟨name⟩" always; TabBar tx dot + TxChip scan all of a tab's sessions by skey suffix, ROLLBACK targets the session that holds the tx. GOTCHA ledgered: closeGuard.commit() selects then commits "the active tab" — safe today (modal traps), but a future workspace-switch-under-modal would need the pending id threaded explicitly.
**Ship review (no S1, single-conn path verified byte-identical no-op)**: S2 fixed pre-merge — exact-count footer counted the RAIL under origin rows (the one reader left rail-bound; now executedProfileId-first like loadMore, refuses "origin connection not available"). LEDGERED for next wave: (1) two-prod shadowing — rail-prod A unlocked + foreign prod-origin B steals the chip, A's unlock invisible (fix = dual chips or force-relock on rebind); (2) ensureTabSession's transparent reconnect sets activeProfileId+homeMode → an origin-pinned op can silently FLIP the workspace (fix = non-activating connect flag); (3) pageStale latch drops the origin pin for backgrounded writes on pinned/legacy tabs; (4) closeGuard.commit should thread the pending id. select() refusal now console.errors unconditionally (field breadcrumb for the next unscoped-lookup bug).
User repro: paste row into Add-row band, ⌫-clear the PK → committed '' instead of DEFAULT. Not a missed bug — the touched-but-empty='' convention was deliberate + documented; the INTENT was wrong (clear means reset, and '' is invalid for most types anyway). Full model rebuild per user's design + upgrades: DraftCell {text, state?: "null"|"empty"} — **empty input = DEFAULT, always**; explicit NULL/'' via per-cell right-click menu (Set NULL gated on not_null; Set empty anchored to scalar text-family — bare prefix matched text[] where '' is a malformed literal) + ⌥⌫-on-empty (with text present it stays macOS word-delete); chips reuse the grid's NULL/EMPTY pills (input stays mounted, typing replaces state, ⌫ resets chip→DEFAULT); ∅ button retired (also un-crams narrow cells). Placeholders self-document from the snapshot: default expression verbatim / "identity" / "required" (NOT NULL no default) / NULL; identity-ALWAYS + generated + ctid render "auto" — never focused, never staged, dup-row skips them (was smuggling invisible guaranteed-fail values). commitDraftInner pre-checks required columns CLIENT-side with names — resolved from the LIVE schema snapshot first (tab-frozen TableInfo could false-block a legal write after ALTER; reviewer S2, "app refuses legal work" = the invariant). draftHasContent helper unifies quit/Esc-arm guards. Review round: 4 S2 + 1 S3, all fixed (stale-snapshot block, dup-row auto smuggle, ⌥⌫ word-delete theft, double context menus, array-type ''). Reviewer verified the commit truth table: every (state × text) combo commits exactly what the band displays.
**Second-pass hunt (different lens — lifecycle/view-mapping seams): 1 S1 + 1 S2 + 6 S3, fixed.** S1 (introduced by THIS branch): de-inputting identity/generated cells broke full-row paste alignment — the copied row carries auto columns' values, and skipping-without-consuming shifted everything one column left (silent wrong-column data on identity tables, the common modern shape). Fix: full-row paste at the band's first cell aligns from view 0 with autos consuming their value; partial pastes keep the anchored spread; either way dropped/redirected values FLASH — never silent. S2: commit staged from the live column list, so a draft cell whose column was dropped/renamed/made-auto since tab-open silently vanished from the INSERT while the band showed it; now a set-diff refuses loudly ("changed or removed since this tab opened — reopen"). S3s: setDraftCell clears the pre-check error while typing; pop-cleared text stages explicit '' for text columns (clearing a value ≠ DEFAULT intent); chip + auto fonts zoom-pinned; chip vocab unified (∅ empty); per-column honest title. Reviewer cleared: filter/sort can't coexist with the band, state⇒text:"" invariant across ALL setters, double-commit guard, DEFAULT VALUES on all-auto tables, frozen-render stability.
User repro: copy row → Add row → ⌘V pasted a json cell as `"[""pinterest-scraper""]"`. Root cause: ⌘C quotes fields Excel-style (tsvEscape: tab/newline/quote → wrap + `""` doubling) but BOTH paste surfaces naive-split on \t/\n — quotes never parsed back, and a quoted cell containing a real newline split into two rows (the exact corruption the quoting exists to prevent). Fix: `parseTsv` in clipboard.ts — exact inverse of the writer (leading-quote field = quoted, `""` = literal, may span tabs/newlines; malformed input → null → callers fall back to the naive split so non-TSV text is never mangled). Wired into grid `pasteIntoSelection` and the draft band (row-spread uses parsed fields; a single quoted field unwraps, and if unquoting leaves a one-liner it lands in the input, not the pop-out). Property-tested against the writer: 13 cases incl. hard round trips (newline+tab+quote cells), scratchpad/tsv-test.ts. Boundary kept: single-line pastes never unquote (a pasted `"hello"` JSON literal stays verbatim).
**Review round (no-ship → fixed)**: S1 — parser pre-normalized \r\n→\n INSIDE quoted fields (Windows-origin bytes silently mutated on round trip, invisible in the ⌘S preview); line endings are now record separators only outside quotes, and all draft-band multiline checks widened \n→[\r\n] (WebKit strips \r from a live input's .value — same desync class). S2 — draft band had dropped the naive fallback (malformed tabbed text stopped spreading); restored, both paste paths now share semantics. Also adopted: single-cell ⌘C quotes ONLY when the value holds tab/CR/LF (Excel's own rule; 1x1 unwrap on paste ⇒ exact qwry→qwry round trip; quote-only values still copy raw). Suite now 21 cases. ACCEPTED (documented): trailing empty/NULL last row in a single-column copy drops on paste (join("\n") has no terminator — Excel-identical); non-TSV text that coincidentally parses as quoted TSV gets unwrapped (Excel-identical); editing a \r-bearing value via textarea/CM pop-outs may normalize it (pre-existing edit-surface behavior, not a paste issue).

### 2026-07-20 — v0.9.2-lookingglass: UI audit sweep (session 12, continued)
User screenshotted a real bug (add-row draft cell visible inside the sticky row-number gutter when scrolled right) and asked for a systematic way to catch/polish this class. Ran a 4-agent audit fleet (empirical bug-hunter w/ write access + geometry + interaction + design lenses) → 49 grounded findings → 4-agent fix wave (disjoint file ownership) → all fixed.
**The screenshot bug**: NOT z-order — `.vgrid-draft-corner`'s only background was `--accent-soft` (12–18% alpha in every theme); cells sliding under the stuck corner showed straight through. Proven in a real WKWebView harness driving the actual Grid.tsx (scratchpad/draft-bug/, Swift driver + vite mount; kept for future geometry bugs). Fix = tint layered over opaque `--bg-app`. GOTCHA for future audits: `elementFromPoint`/rect probes report such bugs as "fine" — transparency bugs need pixel evidence, not layout probes. Adjacent find: draft-input `autoFocus` re-fired on every virtualization remount and WKWebView's focus-reveal yanked scroll to (0,0) — replaced by focus-once ref + preventScroll everywhere.
**Headline fixes**: virtualizers got `scrollPaddingEnd` (keyboard nav used to park the focused row invisibly under the fold — verified against virtual-core source); cell editor now positions from stable math (was: teleported to origin when its cell left the virtual window, while still staging to the far-away cell) and sticky chrome raised above it (z ladder: celledit 5 < rownums 6 < header 7 < band 8 < corner/dropline 9 < error 10); draft band Tab handled explicitly across the virtualization boundary (data-draft-col + scrollToIndex + rAF focus); commitDraft captures its tab before awaiting (slow insert + tab switch used to clear the wrong tab's draft and could pop a staged-edits discard); session-fail now writes globalError (was: permanent fake "Loading table…"); Histogram cancels its GROUP BY on unmount (epoch-guarded — was burning the shared session); popovers re-clamp via ResizeObserver (used to grow off-screen from their loading size); clipboard writes route through new `lib/copyCue.ts` + CopyToast (copies were silent app-wide); light-theme shadows/fg-faint/backdrop tokenized; button-hover epidemic fixed; reduced-motion CSS guard; ⌘⇧F = Format everywhere (schema filter → ⌥⌘F, matched by e.code — ⌥F yields "ƒ"); saved queries keyboard-reachable + in palette.
**Audit method (repeatable)**: no Tauri WebDriver on macOS → three layers: adversarial code-lens audits (cheap, catches most) + standalone WKWebView repro harnesses for geometry (provable) + user screenshots. **Post-wave adversarial review** (combined diff, seam-focused): 1 S2 fixed — insertRow read the ACTIVE tab's table after the tab-capture refactor (correct today via a zero-await chain, but one future await away from wrong-table INSERT; table now threaded from the captured state) + skip-rerun left the committed row invisible (pageStale + refresh-on-return, parked while edits/draft live). 7 S3s fixed: pendingDraftFocus ref (Tab across >overscan disabled cells), copy-cancel vs in-flight write contradiction (copyCue now reports success; truncFlash success-gated; cancel target cleared pre-dispatch), reduced-motion static cell-flash highlight (the cue is commit-success signal), Histogram StrictMode double-mount cancel (microtask + epoch re-check), TabBar drag scroll-delta compensation, sidebar handle 4px, band Esc two-step arm (stray Esc can't eat a typed row). Reviewer confirmed clean: scrollPaddingEnd math, z-ladder, ⌘⇧F/⌥⌘F seams, zoom calc, clamp-loop guard.
Ledgered residuals: grid.css/editor.css backdrop+easing dialects, .rowpeek chrome outliers, status-bar fg-faint (grid.css), export-results palette action, empty-state copy case discipline, flashReason kind-by-string-identity (honest shape = {msg,kind} store field).

### 2026-07-20 — v0.9.1-firstlight: user release-review fixes (session 12)
First user-review feedback round on 0.9.0, three issues, all root-caused + fixed. **(1) Cold-start pop-in**: window was visible while the 1.47MB monolith bundle eval'd → glass pane first, elements later. Fix = window created hidden (`visible: false`), revealed after startup data (profiles+tabs) settles AND the shell painted (double-rAF); window-state plugin excludes `StateFlags::VISIBLE` (would re-show early); Rust 5s fallback shows the window if the webview dies pre-paint (blank beats invisible hang). Plus real code-splitting: entry 1.47MB → 590KB (190KB gz) — QueryBox/SqlEditor, ResultsPane, TableBrowser, Inspector, ExplainView, HistoryPanel, ShortcutsModal all React.lazy, preloaded right after reveal; CM core (312KB) + sql-formatter (293KB, dynamic import in format.ts — formatWithPreset is async now) split out. GOTCHA: stores with module-level `subscribe()` wiring (edits, browser) MUST stay in the entry chunk — side-effect imports in main.tsx; new `editor/editorBus.ts` holds editorRunText/Format/TimeTraveling so the shell never statically pulls CM. **(2) Draft-row multiline paste = data loss**: onPaste replaced the whole cell + newlines desync a one-line input (WebKit strips them from .value). Fix = insert at caret, merged-valid-JSON compacts, anything else opens a ValuePop pre-filled (new `startDirty` prop — a paste-opened pop must stage untouched content, the no-change close would eat the paste); multiline draft state renders ⏎ preview read-only, mousedown/paste route to the pop. Also fixes duplicating a row whose text value had newlines (pre-existing silent desync). **(3) Inspector raw-JSON layout**: Stage/Discard moved to the tools row top-right (visible when dirty); `.insp-body.raw-fill` makes the jsonfield grow to content, cap at panel, scroll inside — 60vh dead-space cap gone (scoped; ValuePop untouched).
**Audit round** (1 reviewer, 6 findings, all fixed): S1 — both new draft-JSON paths compacted via JSON.parse/stringify, rounding >2^53 ints through float64 (the exact LOSSY_NUMS hazard the inspector guards); compaction now json/jsonb-only + LOSSY_NUMS-gated, lossy values stage verbatim. S2 — inline compaction ignored column type (flattened JSON-shaped text pasted into text columns); type-gated now. S2 — the reveal effect undid the refused-appdb path's deliberate hide (empty window beside the fatal dialog); new `startup_ok` IPC + fatal stub state, reveal checks it first. S3s — watchdog `is_visible()` error now fails toward showing; lone trailing-newline pastes stay native single-line; insp-tools wraps at narrow widths. Gates re-run green: tsc ✓ build ✓ clippy 0 ✓ unit 50/50 ✓ · entry chunk 590KB (was 1,468KB).
Final wave of the v0.7→v0.9 push. **Behavior**: focus discipline (opener-restore via escStack + focus trap + role=dialog across all 13 modals; modals now blur the background — killed a live typing-leak bug), Mac destructive chords (DangerModal Enter=Cancel/⌘⌫=confirm; CloseGuard Enter=Keep — the Postico-convention inversion finally fixed), empty-state CTAs (zero-row pane w/ timing, first-connection hero, sidebar clear-filter, history hint), UI zoom ⌘=/⌘−/⌘0 (70–150% chrome-level, editor/grid own settings excluded; ⌘0 reassigned from tab-10; muda has no "Plus" key — ⌘= is the accelerator), live window title, springs reduced-motion stragglers (rail avatars, zen canvas). **Design/docs**: 14 raw values → tokens (+--danger-fg, AA-checked both modes), 12 dead rulesets removed (full class-usage scan = zero), README (leapfrogs-first, credential-clean, screenshot refs commented until captured), first-run welcome tab (guarded against the profiles-load race), internal staging db name purged from test defaults.
**Final audit**: 1 S2 fixed in-branch (RvInlineEdit layer suspended the modal Tab trap — bool picker escaped into the background; now traps at its own edge); cascade-focus fallback + zoom double-fire device test + --zoom dead hook ledgered. Auditor's arc verdict: ~106 audit findings fixed across 5 waves, "the correctness spine looks earned, not claimed… nothing in the diff threatens either invariant."
Gates at 0.9.0: tsc ✓ vite ✓ clippy 0 ✓ unit 50/50 ✓ staging 22/22 + 5/5 ✓ keyset harness 28/185 ✓ · qwry_0.9.0_aarch64.dmg built (7.8MB). Perf: keystroke worst-case 4.2ms (measured); cold start + 60fps on the user punch list (window probe needs Accessibility). USER REVIEW NEXT — release punch list in the PR + hand-off.

### 2026-07-19 — v0.8.1-timeline: never lose work, applied to data (session 11) — SHIPPED
Three parallel agents (one round lost to a session-limit kill mid-flight — clean tree, relaunched). **T1 inverse-SQL undo** (leapfrog — no shipping client has it): OLD values captured INSIDE the existing batch statements (FOR UPDATE subselect + RETURNING old/new/locator — zero added RTTs, matched-count byte-equivalence proven live incl. the cross-join artifact probe), revert PLANS persisted (appdb v4, TTL 15min, keep-20) and regenerated by the same SQL generator at undo time, undo re-enters run_verified_batch (stale = honest rollback), redo emerges free (an undo mints its inverse), session/DDL/TTL/supersede invalidation, prod read-only holds (tested). **T2 time-machine + files**: every run snapshots the buffer (appdb v5, deduped, 50/tab); ⌃⌘←/→ browse executed versions read-only w/ ghost banner (Enter = one undoable restore); .sql open ⌘O/save ⌘⇧S/drag-drop (Tauri window events); ⌥↑/⌥↓ psql-style history walk at doc edges. **T3 import + history**: 3-step CSV wizard — sniffed delimiter/header (headerless never loses row 1), name/position auto-map, validate = full always-rollback rehearsal w/ savepoint-bisected exact bad-row naming, commit = ONE all-or-nothing tx, no row silently dropped/padded/coerced; history panel grouping ×N + day dividers + connection/status chips.
**Audit round** (4 reviewers, ~30 findings fixed): the capture grammar survived independent EPQ analysis (no wrong-write constructible through it) — the S1s lived AROUND it: insert-reverts had no existence/identity pin (now WHERE NOT EXISTS + attname + relname probes — re-inserted rows, renames, and user-tx rollbacks all roll the undo back honestly), savepoint-mode batches logged non-durable "commits" as undoable (never logged now, both directions), ⌥-walk/⏎-restore lost never-run drafts across quit (drafts now park into the snapshot trail), import's COMMIT-failure path claimed certain rollback on connection-class errors (outcome UNKNOWN now), and the import wizard's session fallback could silently target the rail-active profile (bound to the browse session only, connection pill visible). Own-mode batches pin SET LOCAL datestyle=ISO (GUC-drift wrong-write closed; wrapper accounting extended with exact-count proofs). Offer races sequenced (resetTab can't wipe fresh offers). History grouping on raw SQL.
Gates at integration: tsc ✓ vite ✓ clippy 0 ✓ unit 50/50 ✓ staging 26/26 ✓ (22 smoke incl. staging_inverse_undo + staging_undo_prod_locked, 4 csv_import). Gotchas: UPDATE…FROM subselect + repeated outer WHERE keeps matched-count semantics exact where a join-on-__old would be equivalent but unindexable; RETURNING may reference the FROM alias (the whole capture rides one statement); csv crate flexible(true) silently pads short rows — import path must be flexible(false); WKWebView drops HTML5 file-drop events when Tauri dragDropEnabled — use onDragDropEvent + devicePixelRatio-scaled hit tests; appdb "datetime('now')" is UTC — parse with the ISO+Z form.

### 2026-07-19 — v0.8.0-spelunk: data navigation power (session 11) — SHIPPED
Three parallel in-repo agents, contract-coordinated (`setSortChain`/`compiledWhere` defined by the browse agent, consumed by the grid agent — composed clean on first integration). **S1 introspect v2 + structure**: one-round-trip batch → 9 result sets (sequences, extensions, enum labels, table+column comments, partition parents; +5ms real on 145 tables); sidebar Functions/Sequences/Enums/Extensions sections + relkind glyphs + nested partitions + comment tooltips + pinned-tables strip (localStorage per profile); new `table_stats` command (constraints/indexes/triggers via pg_get_*def, sizes, pg_stat_all_tables activity — matview-safe, never-used-index badges) rendered in a deep Structure tab w/ copy-DDL; Refresh routed to structure. **S2 browse power**: multi-column sort chains w/ per-key NULLS as ladder variants (default placement byte-identical), type-classed filter registry (BETWEEN/jsonb @>/quoted-IN), raw-WHERE owning mode + compiled-WHERE preview chip, cancellable exact-count footer, ⌘L jump w/ keyset re-anchor (gapless, proven); harness 10→20 cases/220 assertions. **S3 grid nav**: RecordView ⇧Space (transposed, ⌘↑/⌘↓ walk, stages through the real pipeline, honesty primitives + truncation routing), 2-row diff (effective-value comparison, tinted), FK picker (⌘↓, live search on tab session, sqlIdent quoting), value-distribution histograms (server GROUP BY w/ window totals on browse respecting compiledWhere; client-side labeled on editor tabs; json type-gated), multi-sort header UI (⇧-click chain badges, ⌥-click NULLS cycle); 59/59 logic assertions.
**Audit round** (4 reviewers, ~25 findings fixed; one agent round lost to a session-limit kill — tree was clean, relaunched identically): ladder math independently re-derived (8/8 combos match, live gap-probes green) — keyset ships clean; fixed: PK-anchor truncation (silent O(n²) fallback when chains end on NULL pages), NULLS-override planner cliff gated on both surfaces, RecordView/RowPeek hidden-column leak + dup keys, enforcement-only unique indexes losing the drop-advice badge, live Columns cell in table_stats, histogram/FK-picker moved to primary session (out of ⌘. scope), newline-safe WHERE composition vs trailing -- comments, ValuePop pretty-bytes staging no-op'd. Harness 28 cases w/ explicit keyset-vs-fallback assertions.
Gates: tsc ✓ vite ✓ clippy 0 ✓ unit 34/34 ✓ staging 20/20 ✓ keyset-proof 28 cases/185 ✓. Gotchas: pg_stat_user_tables loses matviews — always pg_stat_all_tables; window functions after GROUP BY ride the same histogram query for totals; pg_sequence join = PG10+ floor for introspection; per-key NULLS overrides CAN be keyset-laddered (don't fall back to offset — variants are provable).

### 2026-07-18/19 — v0.7.1-tempo: perf register burn (session 11) — SHIPPED
Three parallel in-repo agents (disjoint file ownership), all GAPS §2 registers burned. **Keyset pagination** (new pure `stores/browseSql.ts`, proof-harness-drivable): PK/ctid tiebreaker inherits sort dir; row-value fast path gated on uniform-dir+NOT NULL; OR-ladder with explicit NULL partitions otherwise; typed seek casts; honest offset fallback for non-keysettable relations; loadMore = one page on the tab session (no history flood, in-flight+identity guards). Measured on 8.1M-row staging table: page-5000 >300s OFFSET timeout → ~2ms; page-30 13.1→0.57ms. Correctness proof 8/8 via the app's REAL SQL generator against squad fixtures (dup sort values, double NULL partitions, mixed ASC/DESC, ctid tables; order-sensitive sequence equality). **Editor keystroke** ~24→4.2ms worst-case on 2.95MB: stmtScopePlugin incremental spans (20.2→0.42ms; differential-tested 6,600 randomized edits — caught its own unsound `;`-boundary assumption: final span must always re-lex), 200KB-capped completion context (2,664-position byte-equivalence proof), rope chunk compares in subscribers. **Inspector/trees/grid**: parse LRU + per-cell subscriptions, JsonTree 200-child expanders + capped search w/ visible note, SchemaTree virtualization, rAF-sliced 50k copy w/ progress, noAutocorrect tag-gating, live prefers-reduced-motion collapse. **FUNCS cache**: pg_catalog fns in appdb by server_version, drift probe in-batch (25.8→2.0ms + 338KB/connect); history_search bounded to newest-5k. Two stale GAPS entries found (setSelectionValue already batched; DDL-sniff already fixed in bedrock) — corrected.
**Audit round** (4 reviewers, 20 findings fixed): S1 inheritance-parent ctid collision (relhassubclass gate + live colliding-ctid proof); pinned page-1 keys (DDL-mid-scroll splice killed); committed-key-edit stale re-run; ⌘F reveal windowed (was O(hitIndex) mounts); copy-abandon honesty + progress/error slot discipline; page-1 ORDER-BY-ctid O(n)/page → 1M reltuples gate; span growth loop resumes (5MB keystroke 21.3→11.4ms = theoretical floor, re-fuzzed 32,652 tx w/ mutation-tested harness). Keyset proof harness PERMANENT: scripts/keyset-proof.ts. Final gates: staging 18/18.
Gates pre-audit: tsc ✓ vite ✓ clippy 0 ✓ unit 34/34 ✓ staging 17/17 ✓ (new: catalog-cache roundtrip+drift). Gotchas: tid btree opclass is PG14+ (ctid keyset gated on server_version_num); row-value `(a,b)>($1,$2)` ignores per-column direction — mixed dirs need the expanded ladder; JSC ropes make `toString()` after edits ~free (0.016ms/2.95MB) — the real editor costs were re-lexing and re-tokenizing; WKWebView GCs WeakRef aggressively — plain Map LRU beats WeakRef caches.

### 2026-07-18 — v0.7.0-bedrock: correctness ledger burn + driver spine (session 11) — SHIPPED
Launch push opened (plan: `docs/ROADMAP_v0.9.md`, waves v0.7.0→v0.9.0). Three parallel worktree agents built the bundles; orchestrator merged (zero conflicts), integrated the one cross-bundle residual (ctid delete guards in Grid), ticked 25 GAPS items. **A — driver+edit spine**: tx-state tracked in the driver (splitter statement-heads + error outcomes; tokio-postgres hides ReadyForQuery — checked source), `tx-state` event feeds the TX chip (frontend sniff deleted); edits inside a user tx = SAVEPOINT/RELEASE (outer tx never touched); ctid edits+deletes carry old-value identity guards; casts pre-qualified+quoted (`"char"` gotcha); table identity = {schema,name} end-to-end (dotted split_once dead); GENERATED/identity-ALWAYS read-only with reasons; per-relkind honest read-only reasons (matview ctid lie gone); `fetch_cell` command (aliased-ident-proof full-value fetch, honest errors); out-of-band cancel escalation (token → busy-poll → pg_cancel_backend on a fresh conn → user-confirmed terminate tier); TLS-downgrade surfaced in ServerInfo; keychain failure ≠ empty password; session-closed carries reason → ConnToast. **B — persistence spine**: appdb `user_version` migrations (legacy DBs stamped in place, verified on a copy of the real appdb), corrupt-row skip-and-log everywhere, history status ok/error/cancelled + 20k-char SQL cap + 20k-row prune + status dots in panel/palette, single-instance plugin. **C — frontend sweep**: connect() in-flight dedupe, markDisconnected disconnects dead sessions, open-tx confirms on disconnect/reconnect/recent-click/delete (recent-click no longer nukes sessions when already connected), Save&Connect failure keeps the card visible with a red error state, danger scanner rewritten on the real lexer (CTE-wrapped DML, comment-skip, top-level-WHERE only; 24-case proof), Grid keyed per tab:stmt (stale-selection class killed) + right-click retargets, overlay z from escStack depth + native-menu gate + IME Esc guard, theme/settings sanitized on rehydrate, draft-row touched-state grammar (DEFAULT/NULL/''), read-only reason flash, profiles-load-failure banner.
Gates at this point: tsc clean · vite build ok · clippy 0 warnings · unit 26/26 · **staging 15/15** (~73s; 6 new: savepoint-in-user-tx, ctid guard, generated/identity, dotted names on squad `"qwry.dotted"."ta.ble"`, oob cancel+terminate, tx-state event sequence). New test infra: dual staging DBs — crawler_data_production (read-only/big) + `squad` (DDL fixtures in `qwry_test` schema only, dropped at teardown).
**Audit round** (5 adversarial reviewers over the wave diff): 31 findings fixed by two parallel fix agents (Rust 12 / frontend 19). Worst catches: `ROLLBACK WORK TO SAVEPOINT` filler tokens mis-folded the tx tracker to Idle → an edit batch could COMMIT the user's open transaction (S2, the exact invariant SAVEPOINT mode protects — fixed + staging-tested); two pre-existing S1 corruption bugs in the frontend-fed identity layer (plan_edits group-signature collision folding two rows' edits into one UPDATE; stale snapshot names writing the WRONG COLUMN after external rename+add — both now roll back honestly via length-prefixed sigs + pg_attribute attname guards ANDed into hinted WHEREs); duplicate-row silently inserting a truncated 8KB prefix; the dynamic overlay z-stack leaving every context-menu submenu mouse-dead; corrupt-tab skip + replace-all tabs_save = permanent SQL loss (tabs errs again, by design). Deliberate deferrals ledgered in GAPS. IPC mirror audit: zero drift across the whole surface. Final gates: tsc ✓ vite ✓ clippy 0 ✓ unit 32/32 ✓ **staging 16/16** ✓. Version bumped 0.7.0.
Gotchas burned: tokio-postgres 0.7.17 exposes neither ReadyForQuery status nor CancelToken.process_id (pub(crate)) — lexing is the only zero-cost tx tracker, and the backend pid must be fetched via `SELECT pg_backend_pid()` at connect; `"char"` cast must stay quoted (bare `::char` = bpchar(1)); adding a Vec field to a serde struct breaks every Rust test literal (18 sites) — `#[serde(default)]` doesn't help struct literals; `git add -A` from the repo root sweeps agent worktrees under `.claude/worktrees/` into the index as embedded repos — ignore the dir (done) and prefer explicit paths; own-mode ⌘S emits in_tx→idle within ms (TX chip can flicker one frame — honest, cosmetic); ConnToast can miss a tab-session death toast if markDisconnected wins the listener race (ledgered).

### 2026-07-06 — v0.6.0 FINALIZED (session 9)
Cut from everything since 0.5.0: **per-connection workspaces** (tabs/bookmarks scoped per connection, cross-connection PINS with origin-colored badges + info popover, zero-tab zen screen with the wave sheet), **the performance batch** (⌘S ~18s→~1s on a bastion: 2-RTT batched verify-then-commit + cached-mapping planning with class-42 auto-repair; snapshot-fed 1-RTT editability; persisted stale-while-revalidate schema snapshot = instant sidebar at connect; tunnel dedupe by spec; instant danger modal w/ streamed estimates; claimable in-flight spare; async appdb/fs commands; debounced+cached quick-filter), **three design loops** (token adoption: 5-tier type ramp, 5-step radius, one button spec/badge/dot family, global focus ring, modal skin+width tiers, bevel), **QOL loops** (draggable editor/results split + sidebar width, grid density/font, statement-scope highlight, re-run, pinned tabs, conn-color ambient, drag-reorder tabs, TX chip+ROLLBACK, ⌘? cheatsheet, schema-tree columns, Liquid Glass gutters w/ transparency slider), formatter presets+minify, table DDL tab, quick-filter, selection stats, export-to-file, column hide/show, CTE runner, smart paste, DML impact estimates, NOTICE surfacing, RowPeek/ValuePop syntax highlight — plus 6 audit rounds (~50 interaction bugs fixed, worst: shortcuts-through-modals, selection-surviving-sort wrong-row writes, commit/run race, profile-wide session wipe). Gates at cut: tsc clean, cargo clean, staging 9/9. dmg: qwry_0.6.0_aarch64.dmg.



### 2026-07-06 — PERF BATCH: 2-RTT commits · cached mappings · schema hydrate · tunnel dedupe (session 10) — DONE
Nine-area latency batch (A–I), driven by a fresh-eyes audit. All 9 staging tests green (incl. 1 new); tsc + cargo check clean.
- **A — ⌘S commit path collapsed from ~3N+6 sequential RTTs to 2.** `apply_edits`/`delete_rows` now send ONE `BEGIN; stmt₁; …; stmtₙ` simple-query message (`run_verified_batch` in edit.rs — the simple protocol executes in order on the session), verify each statement's RETURNING row count from the SINGLE result stream (`matched==1` per row, message accounting asserted: `n+1` result sets or refuse+rollback), then COMMIT or ROLLBACK as the second trip. Any SQL/protocol error mid-batch → ROLLBACK ALL → Err. Verify-then-commit semantics byte-identical to before, just batched.
- **B — cached-mapping feed.** New `EditMapHint` (the EditabilityMap the frontend already holds + attnum→name from the schema snapshot) accepted by `edits_preview`/`edits_apply`/`delete_rows` → planning does ZERO catalog trips; preview with a warm mapping touches the server zero times, commit = exactly 2 RTTs regardless of N. New `TableIdentityHint[]` (oid→dotted+pk_attnums for the whole snapshot, WeakMap-memoized in `lib/editHints.ts`) accepted by `editability` → the map after a query costs ONE trip (the prepare). Trust model: hints are trusted-but-verified — incomplete hint → silent server-side fallback; identity check in `buildEditMapHint` (snapshot dotted name must equal the map's server-derived name, guards cross-DB oid collisions and renames); stale hint → SQL error or matched≠1 → full rollback. **Auto-repair**: SQLSTATE class-42 on preview → refetch map (hint-less) + retry once silently; class-42 on APPLY → batch already rolled back server-side → refetch, REGENERATE preview, show "schema changed — review the updated SQL" (a write is NEVER auto-retried). Executed CREATE/ALTER/DROP statement heads → `refreshMapsAfterDdl` background-refreshes every tab's cached maps (replaced the whole-buffer `looksLikeDdl` regex with real statement heads). **Byte-for-byte preview→commit**: `previewPayload` stashes the exact entries+hints the preview was generated from (keyed by executedSql + a pending-edits signature); commit replays them through the same backend generator. Fallback derive path also improved: attnames now ONE bulk pg_attribute query (was one per row group — GAPS §2.10).
- **C — persisted stale-while-revalidate schema snapshot.** appdb `schema_cache(profile_id PK, sig, data)`; `introspect` command takes `cache_key`/`cache_sig` and persists the fresh snapshot; new `schema_cache_get` returns the raw JSON. `useSchema.hydrate(profileId)` fires at connect START — sidebar + completion live before the handshake finishes. Ordering: hydrate NEVER overwrites an existing snapshot (re-checked at apply time), fetch (server) always wins — no timestamp needed, server data is by definition fresher than any persisted cache. `sig` = `connSig` (now exported), so a repointed profile can't hydrate the old server's schema; cache row deleted with the profile. Bonus (raw-JSON-passthrough of introspect cells) SKIPPED: embedding JSON-in-JSON bloats the IPC payload with escaping and loses Rust-side shape validation — judged a wash or worse.
- **D — tunnels deduped by spec.** `AppState.tunnels` re-keyed profile→SPEC (forward target + ssh params); DB-switcher clones now SHARE one ssh process. `profile_specs` records each profile's binding; `invalidate_profile` (and now profile_delete) unbinds and drops the tunnel only when NO other profile rides that spec — dropping a shared tunnel would kill sibling profiles' live sessions (refcount by enumeration). Repointed profile = new spec = own tunnel; the old one survives for its remaining users.
- **E — danger confirm opens instantly, estimates stream in.** `confirmDangerLive` (danger store: prompt id + `update(detail)` that no-ops once resolved/displaced). The no-WHERE modal opens immediately with "≈ estimating…" per statement; plain-EXPLAIN estimates race a 2s deadline → value or "estimate unavailable". Estimates moved to the PRIMARY session: a lock-stuck EXPLAIN previously queued IN FRONT of the user's own run on the tab session (tokio-postgres serializes per connection) AND held the modal shut.
- **F — in-flight spare claimable.** `spareInflight` map holds the replenish promise; `ensureTabSession` awaits + claims it (double-claim guarded by re-checking both the tab key and that the resolved sid is still the pooled spare). Kills the second-full-handshake on connect→immediate-⌘↵.
- **G — appdb/fs commands async.** tabs_*/history_*/saved_*/profile_*/clone_connection/connection_uri/invalidate_profile/write_text_file → `async fn` (Tauri runs them off the main thread); write_text_file uses tokio::fs — CSV export no longer freezes the event loop.
- **H — quick-filter cost shape fixed.** Input debounced ~80ms (local state, Enter flushes, close unmounts). Grid builds a lowercase per-row haystack ONCE per rows identity (cells joined with `\u0000` so matches can't span cells; freed when the filter closes), and an extending query narrows the PREVIOUS match set instead of rescanning.
- **I — small wins.** Tunnel wait_ready polls 25ms→×2 backoff→200ms cap (was fixed 200ms quanta — warm forwards are up in <50ms); completion engine caches the demoted `allTablesDemoted` list (was `.map(spread)` over the whole catalog per keystroke); DbSwitcher lists databases on the PRIMARY session (was minting/consuming a tab session; executedSessionId fallback now profile-checked).
- **New live test** `staging_batched_and_hinted_paths`: hinted editability == derived; hinted preview byte-identical to derived; multi-row batched commit w/ per-row verification; deliberately stale mapping (wrong attname) → Err + ZERO partial writes + session usable after; stale PK under hint → matched≠1 → full rollback; batched deletes (mixed stale → rollback, valid pair → commit).
- Gotchas burned: `execute_simple` segments the batch by CommandComplete — BEGIN yields an empty statement result, so expect exactly n+1 result sets and REFUSE (rollback) on any other count rather than guess. WKWebView/TS: a literal NUL typed into source survives — use `"\u0000"` escapes. Danger-estimate EXPLAIN must never share the tab session: plain EXPLAIN takes AccessShare locks and *queues* behind/ahead of user statements on the same connection.

### 2026-07-06 — Liquid Glass gutters (macOS 26) + glass slider (session 9) — DONE
- **Glass opacity slider** (Settings ▸ Appearance ▸ Glass, 10–90%, live preview, persisted): theme engine's `--glass-tint` alpha became `setGlassAlpha`-driven (was hardcoded 0.55/0.5 at three sites); settings subscriber re-applies on change.
- **Liquid Glass**: `apply_liquid_glass()` mounts an `NSGlassEffectView` beneath the transparent webview via raw objc2 msg_send — **class resolved at RUNTIME** (`AnyClass::get`), so pre-26 macOS falls back to the existing NSVisualEffectView vibrancy untouched. Hand-rolled CGRect/CGPoint/CGSize with `unsafe impl Encode` (objc2 marshals struct returns only with encodings). Autoresizing 18 (W|H sizable), added NSWindowBelow so the webview stays on top. The tint slider works over BOTH materials (it's a CSS wash on body).

### 2026-07-06 — DESIGN LOOP 3: rhythm + residuals (session 9) — DONE
- **List-row rhythm**: two tiers tokenized (`--row-y: 6px` standard, `--row-y-dense: 3px` trees/cheatsheet); seven ad-hoc row paddings collapsed onto them.
- **Color pickers unified**: connection swatches + theme dots both 20px.
- **Residual sweep**: last off-token shadows (the .55 danger/cg/palette recipes) onto tokens; JsonField inline 12px onto the ramp. Census after: ONE raw font-size left (16px glyph preview, documented one-off), two 1-2px micro-nub radii, component-specific shadows (avatar depth, theme-card lip) — all deliberate.
Design-system state: 5-tier type ramp · 5-step radius · 3 shadows · 3 modal widths · 2 row rhythms · 1 badge · 1 button spec · 1 dot family · global focus ring + disabled states.

### 2026-07-06 — DESIGN LOOP 2: structural consolidation (session 9) — DONE
- **Modal width tiers**: `--modal-w-sm/md/lg` (420/530/640) — every modal picks one (settings/srvinfo/cg/tp/generic→sm, danger→md, keys/history/valuepop/ep→lg); the three bg-panel stragglers (danger/cg/generic modal) joined the raised surface.
- **One action-button spec**: shared `.btn` skin in tokens.css + all 10 hand-rolled action-button bodies normalized to it in place (padding 5px 12px · radius-sm · border-strong · text-xs · 120ms transitions) — the nine-way "which padding/border did this dev pick" survey is over; valuepop's weak-border bug died in loop 1.
- **Icon/ghost buttons**: sizes floored (sb-info/insp-icon 24 · find/jt-nav 22 · status-strip 20), radius unified, hover = bg + fg shift everywhere (several only recolored).
- **Status dots**: inline dots standardized at 8px (conn-dot, tab-tx; pin-info/pl/dash already there); rail keeps its notch-ring treatment as the documented rail-only variant.
Also: editability reason strings now carry the RECIPE ("SELECT ctid, * FROM …") not just the diagnosis.

### 2026-07-06 — DESIGN LOOP 1: token adoption sweep (session 9) — DONE
Design audit agent (CSS/JSX lens) verdict: "the token system is good — 60% of component CSS ignores it." Loop 1 executed:
- **Type ramp locked to 5 tiers**: new `--text-2xs: 10px`; 53 raw font-sizes (9/9.5/10/10.5/11.5/13.5/14…) swept onto the ramp — 12 rendered sizes → 5.
- **Radius scale locked**: new `--radius-xs: 4` + `--radius-pill`; `--radius-sm` 5→6 (the majority literal); 47 raw radii swept (the 1–14px zoo + 99/999).
- **Shadows tokenized**: `--shadow-sm/md/lg` replace 13 hand-typed rgba recipes.
- **One `.badge`** (danger/accent) replaces pl-badge/pal-prod/dash-prod/st-pk clones (byte-identical ×3).
- **Global `:focus-visible`** accent ring (there was NONE app-wide) + **global `button:disabled`** (several surfaces rendered disabled identical to enabled).
- **Modal skin**: surface unified on `--bg-raised` (was split ~50/50 with panel by author), shared padding token, `.settings-title` onto the ramp (15/650).
- **Hit areas**: tab-close 15→18, pin glyph 13→20, tree-twist 14→18.
- **Tab dirty indicator** (user screenshot): amber dot moved INTO the close slot — visible on inactive tabs, morphs to ✕ on hover (VS Code grammar); no more bolted-on dot after the label. Tracking unified (.06em labels / .05em state chips); prod-chip weight matches status-tx.
Loop 2 queued (structural): one `.btn` set (~15 files), modal width tiers, status-dot family (8px, one treatment), icon-button unification onto 24px ghost.

### 2026-07-06 — bookmarks per connection + inspector toggle to titlebar (session 9) — DONE
- **Saved queries scoped per connection** (were global): `saved_queries.profile_id` additive migration; upsert stamps the active connection (⌘S under conn A files it under A); rename preserves the home connection; sidebar lists own + legacy-unscoped (adopt-on-touch mirror of the tabs design). Dirty-dot by-id lookups unaffected.
- **Inspector toggle moved to the titlebar** (PanelRight beside the theme swatch, accent when open); floating reopen arrow + both in-panel close buttons removed — one control, one place. Titlebar layout: breadcrumb owns the auto-push (two tools would each have grabbed margin-left:auto and spread apart).

### 2026-07-06 — zen waves · peek/ValuePop syntax highlight · inspector stale-target (session 9) — DONE
- **Zen screen v2**: canvas flow-sheet (42 whisper-thin monochrome lines, three incommensurate sines, center-envelope amplitude, breathing) + centered quote + borderless key grid (⌘T/⌘K/⌘Y/⌘?). **Gotcha: resizing a canvas backing store WIPES it, and ResizeObserver fires AFTER the frame's rAF draw** — during the inspector slide every frame was draw→wipe→blank; fit() now repaints synchronously. (Also: HMR did not apply this canvas fix — full relaunch did; rAF-heavy components + HMR unreliable.)
- **RowPeek/ValuePop syntax highlight**: structured (json/jsonb/array) peek values render through the read-only JsonField CM (colorized); ValuePop editor becomes a real CM editor for structured types with a **Format** button (parse→stringify(,2), parse errors surfaced, never mangles). JsonField gained autoFocus(caret-end).
- **Inspector stale target**: results mirror now follows activeId→NULL (zen) instead of keeping the closed tab's dataset alive; inspector drops its target the moment its coordinates stop resolving (tab close, re-run, shrink) — same coordinates-outliving-data class as the audit finds.
- ⌘? binding fixed (WebKit reports the UNSHIFTED key under ⌘ — match "/"+shift too); titlebar swatch back to theme picker.

### 2026-07-06 — ⌘T focus bug: StrictMode ate the one-shot signal (session 9) — DONE
⌘T must land the caret in the editor. Three "fixes" failed because the diagnosis was wrong; file-probe instrumentation (write_text_file to /tmp, user repro) showed **TWO editor mounts**: React StrictMode's dev double-effect. The throwaway first mount consumed the one-shot `editorFocusSignal`, started the focus-retry loop, then unmounted — the loop spun 20 frames against a DESTROYED CodeMirror view (focus() no-ops, hasFocus never true, activeElement stayed BODY); the surviving second mount found the signal already spent. Fix: **consume the signal only on CONFIRMED focus** (view.hasFocus), and the retry loop halts with its own view's lifecycle (`editorGone` in cleanup) — whichever mount survives claims the still-set signal. **Ledger rule: one-shot signals consumed in mount effects are eaten by StrictMode's throwaway first mount — consume on SUCCESS, never on attempt. And when a bug survives two fixes, stop reasoning and instrument.**

### 2026-07-06 — pin info popover + zero-tab state (session 9) — DONE
- **Pin UI redesign** (user: button-look begged a destructive click): chip background gone → plain origin-colored glyph; hover brightens/scales (affordance) but click opens an ANIMATED INFO CARD (AnchoredOverlay + popIn): origin dot+name, tab kind/name, first SQL line, contract text ("visible everywhere · runs against the ACTIVE connection · unpin via right-click; closing unpins"). Unpin only via context menu / close.
- **Zero tabs is legal**: killed the phoenix scratch tab. Closing the last visible tab (single or close-all) leaves an EMPTY strip + "no open tabs" pane with a New-tab button; ensureActiveVisible and load() accept empty. All shortcut paths null-safe (run/⌘W/⌘1-9/cycle bail cleanly).

### 2026-07-06 — PER-CONNECTION WORKSPACES + cross-connection pins (session 9) — DONE
Each connection now owns its tabs (and therefore its results/edits/browser/inspector state — all tabId-keyed downstream). Pinned tabs are the deliberate exception: visible under EVERY connection.
**Model**: `Tab.profile_id` (appdb additive migration `ALTER TABLE tabs ADD COLUMN profile_id TEXT`; TabRow serde-default). Visibility = own profile ∪ pinned ∪ legacy-null. **Legacy tabs (pre-migration, profile_id NULL) stay visible everywhere until first EDITED under a connection — adopt-on-touch, zero silent disappearance.** Per-connection last-active tab in localStorage map; profile switch restores it (else first visible, else fresh scratch stamped with the profile).
**Every tab operation re-scoped to the visible strip**: close-neighbor selection, ⌘1-9, ⌃Tab cycle, close-others/right/all (other connections' tabs untouchable), drag-reorder (anchor-splice into the global array — same technique as column reorder over hidden columns), palette Open-tabs group.
**Pin semantics**: pin = follow me across connections. Pin badge = origin connection's COLOR + tooltip "Pinned — <name>", click-to-unpin. X-closing a pinned tab unpins it (no dangling id). Unpinning a foreign/orphaned pin adopts the CURRENT connection (a tab visible nowhere would be lost alive) + refocuses if it just left the strip. Deleting a profile purges its unpinned tabs; its pins become orphans badged "origin deleted".
**Invariant**: the active connection never has zero visible tabs; activeId is always a member of the visible strip (ensureActiveVisible runs on profile switch, unpin, close paths).

### 2026-07-06 — autoscroll arm gate + titlebar swatch fix (session 9) — DONE
- Titlebar swatch button opens the THEME picker again (icon promised theme, opened Settings).
- **Click-and-hold near a grid edge auto-scrolled** — the edge-autoscroll loop armed on mousedown. Fix: seed the pointer from the mousedown event (stale until first mousemove — would fake instant movement) and keep the loop dormant until >5px of real travel; once armed it stays armed (pausing mid-drag at an edge is intentional autoscroll). **Lesson for the ledger: any mousedown-initiated loop with SIDE EFFECTS needs a movement threshold even when the click path looks compatible — tab/column reorder got thresholds day one because click had an obvious meaning; here the conflict was the side effect (scrolling), not the selection semantics, so it was missed.**

### 2026-07-05 — QOL closing fix pass: audit round 4, 12 fixes (session 9) — DONE
Sonnet audit over the whole QOL loop → fixes:
- **[P1] persisted split never re-clamped** — tall-display height restored on a small window pushed the divider + results off the clipped card (unrecoverable) → clamp on restore + live window-resize listener.
- **[P1] statement-scope plugin parsed the doc TWICE per keystroke** (splitStatementSpans + spanAtCursor's internal re-split) → one parse, inline span lookup; plus a 300-line cap (a pasted 5k-line VALUES statement must not mint 5k decorations per keystroke).
- **[P1-suspect] tab-strip dbl-click-new-tab removed** — the strip is a tauri drag region; macOS double-click-to-zoom on drag regions fires alongside (⌘T + the + button cover it).
- P2s: sidebar handle moved into the gutter (was shadowing the tree scrollbar); sidebar drag recomputes left per move (mount spring shifts the edge); conn-color var clears on disconnect (stale underline read as connected); density change now same-frame (useLayoutEffect for --grid-rh + measure); --grid-fs cells-only marked deliberate; pinned ids pruned in load() (localStorage grew forever — table pins guaranteed stale); rerun keeps executedOffset (squiggle honesty); RowEstimate refreshes with the header ↻ + falls back to a tab session; .cm-stmt-scope uses background-IMAGE (composes with .cm-activeLine's background-color instead of losing the cascade).
Agent-verified fine: single-Grid --grid-rh assumption, autoscroll ROW_H freshness, middle-click vs drag guards, dirty-dot cost (TabBar already re-rendered per keystroke), RowEstimate error-swallow + session isolation, pinned X-close intentional.

### 2026-07-04 — QOL loop round 3 (secondary tier): pins · dirty dot · conn color (session 9) — DONE
- **Pinned tabs**: context menu Pin/Unpin, angled pin glyph, localStorage set; middle-click + Close-others/right/all all skip pinned (close-all keeps pinned tabs alive, reindexes, only mints a scratch tab when none survive). Explicit X still closes.
- **Saved-query drift dot**: bookmarked tab whose buffer ≠ saved copy shows an amber dot ("⌘S updates").
- **Connection-color ambient**: profile avatar color → `--conn-color` → breadcrumb dot + 2px active-tab underline (theme accent fallback). Complements the prod strip.
- Checked-already-exists: inspector JSON-path copy (alt-click + context row). Sticky/pinned data columns deferred to tertiary (virtualizer surgery).

### 2026-07-04 — QOL loop round 2: run-scope highlight · re-run · placeholder · row estimate (session 9) — DONE
- **Statement-scope highlight**: ViewPlugin bands the statement under the caret (accent 4.5% bg + left rule) — the ⌘↵ scope is visible at a glance; drawn only in multi-statement buffers (no noise on one statement).
- **Re-run button** (↻ in the status bar): re-runs the EXACT executed SQL through run() (staged-edit + danger guards ride along); hidden while running/connecting.
- **Editor placeholder**: empty buffer shows the core grammar hints (⌘↵ / ⌘⇧↵ / ⌘K).
- **Browser row estimate**: table header shows `~123,456 rows` from reltuples (planner stats, no COUNT scan; tooltip says estimate). Skipped a browser limit picker — infinite scroll already pages.

### 2026-07-04 — QOL loop round 1: layout drag + density + tab ergonomics (session 9) — DONE
User clarified: "draggable query tab" meant the EDITOR/RESULTS SPLIT (not tab reorder — that shipped anyway). New mandate: tiered QOL loop (UI/UX foundations → primary daily → secondary), implement first, fix at the end.
- **Draggable editor/results split**: 5px divider (fat hit-area, thin line, accent on hover/drag), clamps both panes usable, CSS-var during drag (zero re-renders), localStorage persist, double-click resets to default 38%.
- **Draggable sidebar width**: right-edge handle, 180–420px clamp, persisted (--sidebar-w).
- **Results grid font size + density**: Settings ▸ Results grid — font stepper (--grid-fs) + Compact/Normal/Roomy (row height 22/26/32; memoized Cell reads height from --grid-rh var matching virtualizer estimateSize; rowVirt.measure() on change).
- **Tab ergonomics**: middle-click closes a tab; double-click empty tab-strip = new tab.
- **Header menu: Copy column name** (top of the right-click header menu).

### 2026-07-04 — release loop round 4: audit-3 fixes (session 9) — DONE
Round-3 Sonnet audit → 9 findings, all handled:
- **[P1] two context menus at once** (cell + header independent states, stacked full-viewport backdrops, Esc needed twice) → each closes the other on open.
- **[P1] .dbsw had no flex** → ⓘ rendered as its own row under the DB header pushing the tree down → dbsw is the flex row now (border moved up from sb-dbhead).
- **[P1] commit()/run() race** — commit's reconnect await window let a confirmed re-run replace the result set; PK locators then built from NEW rows at OLD column positions (wrong-row write risk behind the matched≠1 net) → run() bails while committing (+ re-check after its own await) AND commit aborts if `executedSql` changed after session resolution.
- **[P2] hidden first column killed draft autofocus** (firstDraftCol was data-order) → first VISIBLE non-ctid.
- **[P2] find hits in hidden columns unreachable** ("frozen" find) → Grid publishes hiddenCols via gridFilter store; FindBar skips them.
- **[P2] sort by a hidden column = invisible undiscoverable sort** → hiding the sorted column clears the client sort.
- **[P2] ServerInfo dead-primary false "not connected"** → falls back to any live tab session on the profile.
- **[P2] schema-tree insert on table tab silently no-oped** → seeds a new query tab instead.
- ctrl+click hardening on header drag (macOS context-click convention). Click-before-dblclick copy noted as acceptable.
Agent-verified clean: hide-while-editing invalidation, width persistence under hide/reorder, reorder full-permutation splice, spread-paste/commitDraft name-keyed safety, markDisconnected races.

### 2026-07-04 — release loop round 3: column hide/show · header menu · server info (session 9) — DONE
- **Column hide/show**: `hiddenCols` (data indexes) joins the view-map stack — `viewCols` = reorder permutation minus hidden; `viewColLen` replaced every cols.length-as-view-count consumer (colVirt, selection bounds, offsets/hit-test, keyboard jumps, paste/export/draft-spread loops, find-scroll skips hidden-column hits). Reorder commit now maps VIEW gesture → FULL permutation so hidden columns keep their slot. Hide disabled at 1 visible column. Reset per statement like sort/reorder; remap effect clears editor+selection on hide/show too.
- **Header context menu** (right-click header): sort asc/desc/clear + Hide column + Show hidden (N) submenu (per-column restore + show all).
- **Server info ⓘ** (next to DB switcher): one-shot read-only facts — version, encoding, TimeZone, search_path, pg_size_pretty(db), database, user, listen addr.

### 2026-07-04 — release loop round 2: audit fixes + schema-tree columns (session 9) — DONE
Round-2 Sonnet audit (new-feature paths) → 8 findings, all fixed:
- **[P0] selection survived sort/column-reorder** — rect held VIEW coords; batch actions (Set NULL, fill down, ⌘V, delete, export) re-resolved through the NEW maps onto DIFFERENT rows/cols. `sel.reset()` now fires on every view remap (filter+sort+reorder unified effect).
- **[P1] markDisconnected wiped the whole profile on ONE tab-session death** (10-min idle-in-tx timeout kills sessions individually!) — sibling tabs lost their session mapping, TX chips vanished, live backend sessions leaked unreferenced. Now: dead spare → replace; dead TAB session → forget just that skey; dead PRIMARY → flip the dot only (siblings reap themselves via their own events).
- **[P1] palette "Disconnect current" had no confirm** despite killing every tab session + tunnel — now confirmDanger with staged-edit count.
- **[P2] TX ROLLBACK while a query runs silently queues** (tokio-postgres serializes per connection) — button disabled while running, tooltip points at ⌘..
- **[P2] ⌘? dead inside modals** while the menu path worked — exempted from the overlayOpen guard (non-destructive).
- **[P2] RowPeek ignored column reorder** — now renders via colAt (data-keyed internals untouched).
- **[P2] draft Esc-refocus no-op when ALL columns NULL-toggled** (every input disabled) — falls back to a ∅ button.
- **[P2] commit-time NOTICEs dropped after session rebuild** — liveSessionId stamps the rebuilt sid into executedSessionId so pg-notice routing matches.
Feature: **schema tree column expansion** — chevron per table → column list (PK key badge, type right-aligned, null/default tooltip); click copies name, double-click inserts at editor caret (`editorInsert`).
Agent-verified clean: tab drag vs click/window-drag/rename/persist round-trip, filter+sort composition across ALL consumers, notice strip layout, TX regex vs failed BEGIN, Rust edit txns always paired.

### 2026-07-04 — release loop round 1: draggable tabs · TX chip · cheatsheet (session 9) — DONE
User handed over feature/bug direction ("iterate until best db client ever, releasable"). Round 1:
- **Drag-reorder tabs**: mousedown + 5px threshold (plain click stays select), insertion boundary from tab midpoints, accent drop-line (inset box-shadow before/after), `moveTab` reindexes `position` + persists (query tabs only, as ever). Close button + rename input excluded from drag start.
- **TX chip**: status bar shows `TX OPEN · ROLLBACK` for the active tab's open transaction (txTabs already tracked by results store BEGIN/COMMIT detection; tab strip dot already existed). ROLLBACK runs directly on the tab session via ipc.execute — through run() it would wipe the result grid being inspected mid-transaction.
- **Keyboard cheatsheet**: ⌘? / ⌘⇧/ + Help ▸ Keyboard Shortcuts (new Help menu) → static two-column modal of the full grammar (query/tabs/grid/app).
- Checked-already-exists: tab context menu (rename/duplicate/copy SQL/close-others/right/all), ⌘/ comment toggle (defaultKeymap + lang-sql commentTokens), tab TX dot.
Round 2 audit agent dispatched (new-feature interaction paths).

### 2026-07-04 — interaction-coherence sweep: user reports + Sonnet audit (session 9) — DONE
User-caught (draft band + palette class): container onPaste swallowed draft-input pastes AND staged clipboard over the grid selection (target-tag bail added); multiline JSON paste kept only "[" (WebKit one-line inputs) → JSON compacts to one line, other multiline stored verbatim in draft state; TSV row paste spreads across draft cells; single-cell ⌘C now copies RAW (TSV quoting garbled quotes in inputs); wheel over an overflowing draft input pans the input, not the grid (native non-passive listener); grid-focused Esc closes an EMPTY band / refocuses a filled one (stray Esc never eats typed data); focus entering the band clears the grid selection (stale highlight lied); cmdk duplicate values (three "new qwry" tabs) misrouted Enter → index-disambiguated + active tab shown by accent+bold name (hover flips to --accent-fg — accent-on-accent vanish class); ∅ buttons left tab order.
Sonnet audit round: **[P1] global ⌘-shortcuts fired THROUGH open modals** — escStack hard-claims only Escape, everything else fell through to App's window handler (⌘W closed tabs behind the palette) → `overlayOpen()` guard in App onKey; **[P1] connection profiles with equal names collided in cmdk** (Enter could connect to the WRONG DB — the first "prod" in DOM order) → value includes p.id; **[P2] Tab in the cell editor** blurred into the ∅ button instead of commit+move-right → explicit Tab = onSave("right") (saveEdit gained a direction); **[P2] draft-band Esc double-fired** through band + grid layers → stopPropagation. Own pass: **export under quick-filter walked past rowOrder** (identity fallback → duplicate rows in the file) → export extent = viewLen.

### 2026-07-03 — batch 3: table DDL · palette upgrades · quick-filter · referenced-by estimates (session 9) — DONE
- **Table DDL view**: third tab in the table browser (Data · DDL · Structure). New `table_ddl` command reconstructs CREATE TABLE from pg_catalog with the SERVER's own deparse (`format_type`, `pg_get_expr` defaults, `pg_get_constraintdef`, `pg_get_indexdef`) — columns incl. identity/GENERATED STORED, constraints ordered PK→unique→check→FK, secondary indexes, table comment. Copy button. Staging test `staging_table_ddl` (9th) asserts real output.
- **Palette upgrades**: "Open tabs" group (switch to any query/table tab, active checked, close current), plus actions: Format SQL, Settings…, Query history panel (custom window event → App state), Disconnect current.
- **Quick-filter loaded rows**: funnel in the status bar → inline input; view-level map composed WITH client sort (filter first, then sort), browser excluded (one filtered server page would lie). Status shows "n of m rows match". **Structural change: virtualizers + selection now created AFTER the view-mapping block and sized by `viewLen`** (filter shrinks the view; every rows.length-as-view-count consumer swapped: virtualizer, selection bounds, ⌘-arrow jumps, paste bounds, autoscroll env, peek step, find-scroll — find hits in filtered-out rows skip scroll). Filter change closes the open editor + clears selection (same wrong-cell class as sort-under-editor). rowViewOf inverse now sized by DATA rows (filtered-out → undefined slot).
- **Referenced-by estimates**: opening the grid context menu fires `EXPLAIN (FORMAT JSON) SELECT 1 FROM src WHERE col = <cell value>` per inbound FK (cap 8) — submenu shows "orders.user_id  ≈ 1,240". Per-VALUE planner estimate (MCV/histogram), not table rowcount; async fills as answers land.

### 2026-07-03 — batch 2 (surpass features): impact estimate · CTE runner · smart paste · NOTICEs (session 9) — DONE
Four GAPS §4 leapfrogs:
- **Pre-execution impact estimate**: the no-WHERE UPDATE/DELETE confirm now shows the planner's blast radius. **Gotcha (user-caught, staging-verified): DML plans root at a `ModifyTable` node whose OWN Plan Rows is 0** (nothing returned without RETURNING) — the real estimate is the child scan node's Plan Rows; parser descends `Plans[0]` past ModifyTable — each dangerous statement gets `EXPLAIN (FORMAT JSON)` on the tab session first and the prompt reads "≈ 4,213,900 rows (planner estimate)". Plain EXPLAIN plans without executing → prod read-only-safe; estimate failure never blocks the prompt. `dangerousStatements` also upgraded to the real splitter (a `;` in a string no longer splits the danger scan).
- **Run CTE standalone**: caret inside a CTE definition → editor context menu "Run CTE <name> standalone" → executes `WITH <defs up to and incl. it> SELECT * FROM <name> LIMIT 100` (dependencies preserved, RECURSIVE kept, quoted names as-written). New tolerant CTE lexer in statements.ts (col lists, [NOT] MATERIALIZED, parens in strings/comments) — bun-tested.
- **Smart paste**: editor context menu "Paste as… ▸ IN list / VALUES rows" — clipboard lines + tab cells → `('a','b',…)` or `(a,b),(c,d)`; all-numeric stays unquoted, quotes escaped by doubling.
- **Server NOTICEs surfaced**: spawn_session now drains `poll_message` instead of awaiting the connection — `AsyncMessage::Notice` → `pg-notice` event {session_id, severity, message} → results store routes to the tab OWNING that session (exact: per-tab sessions) → notice strip above the status bar (last 4 + count, cap 50, cleared on next run). RAISE NOTICE / implicit-index notes no longer vanish. connect() signature grew `on_notice` (tests +no-op).
**Gotcha (self-inflicted)**: python heredoc patcher turned TS `\r?\n`/`\t` literals into real control chars — TS1161 unterminated regex. Repair via byte-level replace; lesson: emit TS regex literals only through raw strings.

### 2026-07-03 — batch 1: run-under-cursor · selection stats · export · width persist (session 9) — DONE
Four deferred P0s in one sweep:
- **Run statement under cursor**: ⌘↵ = selection if any, else the STATEMENT the caret sits in (new TS splitter `src/editor/statements.ts` mirroring the Rust lexer rules — `;` inside strings/quoted idents/dollar-quotes/comments is not a boundary; caret in a gap → next statement). **⌘⇧↵ = Run All** (whole buffer; also Query ▸ Run All). Toolbar Run + ⌘E explain follow the same scope. `editorRunText` now returns `{text, offset}`; results store stamps `executedOffset` so the **error squiggle rebases correctly for partial runs** (validity = executed text still sits at its offset in the buffer). Splitter bun-tested against comment/string/dollar `;` traps.
- **Selection stats in status bar**: Grid publishes per-selection aggregates through `useGridStats` (Grid owns the view→data maps; wire-text numeric detection per value) — `n/m cells · Σ · avg · min · max` when ≥2 numeric values; >200k cells shows count only (no sync stall). Cleared on unmount so a tab switch can't show stale stats.
- **Export to file**: grid context menu "Export (selection) to file ▸ CSV/TSV/JSON/Markdown/SQL INSERTs" — native save dialog (`tauri-plugin-dialog`) → new `write_text_file` command. Reuses the clipboard `formatCells` builders (RFC CSV quoting, dup-key JSON names, real table name + ctid-dropped INSERTs). Exports what the grid HOLDS — capped result exports loaded rows, never a silent refetch.
- **Column widths persist**: hand-resized widths saved to localStorage keyed by column-name signature (LRU 300); on the same shape reappearing (re-run, tomorrow's browse) stored widths apply IMMEDIATELY (before rows stream), else content estimation as before. Saved on resize mouseup via a widths ref (state updates async).

### 2026-07-03 — formatter presets + minify (session 9) — DONE
⌘⇧F upgraded from one hardcoded style to a preset system (`src/editor/format.ts`): **Standard** (2-space, ~50-col wrap) · **Compact** (wide wrap, dense operators) · **Expanded** (one expression/line, leading AND/OR) · **Tabular left/right** (river-aligned keywords) · **Minify** (hand-rolled lexer — strings/quoted idents/dollar-quotes/comments verbatim, line comment keeps its newline so it can't swallow SQL). Keyword case (UPPER/lower/as-is) is a separate setting layered onto every preset (types follow it, function names lowercase unless as-is). ⌘⇧F + menu Format = the default preset (Settings ▸ SQL Format: style dropdown + case segmented); editor context menu gains "Format as…" submenu with every preset (default badged) + Minify. All verified via bun run on sample SQL — 5 visibly distinct outputs, minify round-trips tricky literals.

### 2026-07-03 — v0.5 batch: settings · prod safe-mode · test-conn · history · persistence · editor (session 9) — DONE
Whole v0.5 core in one sweep, version bumped to **0.5.0**:
- **Settings ⌘,** (`SettingsModal`, menu Settings…): mode segmented control, theme picker link, editor font size 10–20px (flows through `--editor-fs` CSS var — no CM remount), wrap-lines toggle (extension → remount), fn-in-complete, statement timeout secs (0 = none).
- **Statement timeout wired end-to-end**: `useSettings.statementTimeoutSecs` → `ipc.connect` sends `statementTimeoutMs` → `commands::connect` → `pg_config`. Applies to NEW sessions (spares/tabs pick it up as they're built). 300s default preserved.
- **PROD SAFE-MODE (enforced)**: `is_prod` profiles get `-c default_transaction_read_only=on` in connection options — every session (primary/tab/spare) starts read-only AT THE SERVER; a stray UPDATE errors before touching data. Titlebar chip `PROD · READ-ONLY` (green lock) → confirmDanger → `SET default_transaction_read_only = off` on the ACTIVE TAB's session only (`setSessionWrites`, `writeTabs[skey]` state, cleared everywhere txTabs is) → chip flips red `PROD · WRITES ON`, click re-locks without ceremony. Staging test `staging_prod_read_only` (7th) proves block → unlock → relock. **Gotcha: PG read-only txns still allow writes to EXISTING temp tables** (CREATE TEMP is blocked though) — relock probe uses CREATE TEMP, not INSERT-into-temp.
- **Test connection** button in the connection editor: `test_connection` command does ephemeral connect (tunnel cached for the real connect) → `SELECT version()` → drop; shows `PostgreSQL 16.x · 42ms · TLS` or the real server error. Probes the CURRENT form values incl. unsaved password.
- **History panel ⌘Y** (View ▸ Query History): debounced search over appdb history across ALL connections (`history_search` profile_id now optional/NULL = all), ↑↓/↵ opens the SQL in a new tab, shows profile · rows · ms · relative time.
- **Window state**: `tauri-plugin-window-state` — size/position restored across launches. **Active tab** remembered via localStorage (`qwry.activeTab`), restored on load if the tab still exists (table tabs are session-only → falls back to first).
- **Editor**: ⌘⇧F **Format SQL** (sql-formatter, postgresql dialect, keyword UPPER; selection-aware; ONE dispatch so ⌘Z restores exactly; unparseable → no-op). Multi-cursor (`allowMultipleSelections`, ⌥-click adds caret), Tab indents (`indentWithTab`), wrap toggle. ⌘⇧F in-editor wins over the schema-filter-focus binding (CM preventDefaults first); outside the editor it still focuses the filter.
- Menu additions: Query ▸ Format SQL ⌘⇧F, View ▸ Query History ⌘Y; Settings… now opens the real settings pane. All checks green: tsc, cargo check, staging smoke 7/7.

### 2026-07-02 — v0.5 step 1: native macOS menu bar (session 9) — DONE
Owned the menu (was: Tauri default). `build_menu` in lib.rs: **app menu** (About w/ metadata · Settings… ⌘, · Services/Hide · custom Quit ⌘Q), **File** (New Tab ⌘T · New Connection · Close Tab ⌘W · Reopen Closed ⌘⇧T), **Edit** (predefined undo/redo/cut/copy/paste/select-all — keeps native text-field behavior), **Query** (Run ⌘↵ · Cancel ⌘. · Explain ⌘E · Commit/Save ⌘S), **View** (Palette ⌘K · Inspector ⌘I · Theme… · Refresh Schema ⌘R), **Window** (minimize/zoom/fullscreen — deliberately NO Close Window, ⌘W belongs to tabs). Custom items emit `menu` events with their id; App.tsx dispatches to the same store actions the keyboard uses. **⌘Q hole closed**: the custom Quit routes through a shared `requestQuit()` (flush persist → dirty-edit confirm → destroy) also used by onCloseRequested — the default menu's direct-exit Quit is gone. Settings… opens ThemePicker until the Settings pane lands (next). Shortcuts now discoverable in the menus; webview keydown handlers unchanged (empirically they win key equivalents on this setup; menu clicks drive the same actions via events — no double-fire either way).

### 2026-07-02 — self-audit round 2: consolidated agent report, 6 more fixed (session 9) — DONE
Main audit agent's consolidated sweep (live-verified several findings against staging) surfaced more: **[P0] sort/reorder under an open cell editor staged the edit on the WRONG cell** — the editor holds VIEW coords and save resolves through the NEW maps; editor now closes on clientSort/colOrder/statement change + saveEdit bounds-guards vanished rows. **[P0] ⌘S commit followed the RAIL, not the result** — `liveSessionId` resolved `activeProfileId`, so clicking another connected profile (staging→prod!) redirected the UPDATEs to a different database with matching PKs; results now stamp `executedProfileId` and commits resolve sessions on THAT profile only. **[P0] run() double-execute during the connect window** — covered by the round-1 runInflight guard (synchronous, spans the handshake). **[P0] ROW_CAP auto-cancel aborted DML** — a capped `UPDATE…RETURNING` was cancelled → rolled back ENTIRELY while the UI reported ~50k rows written; auto-cancel now fires only for read-only heads (select/table/values/show/explain), DML drains fully. **[P1] grid keyboard grammar hijacked the draft-band inputs** (Backspace staged NULLs over the selection while typing a new row!) — onKeyDown bails on INPUT/TEXTAREA/SELECT targets and while the row peek is open. **[P1] ⌘V belt-and-suspenders** — keydown fallback (tauri readText) restored alongside the paste event; preventDefault stops double-fire. **[P2] zero-label enum killed ALL introspection** (labels subquery lacked coalesce — live-verified) — fixed. **[P2] danger store orphaned a displaced caller** — a new prompt now resolves the old resolver false. Chip honesty: unmappable truncated cells say "full value unavailable" instead of loading forever. Deferred with GAPS entries: dotted schema/table split in edit SQL (needs EditabilityMap shape change); default-menu accelerator class (native menu bar resolves it).
All 6 staging tests green after; empirical note: agent's menu-accelerator theory over-generalized — ⌘C/⌘A/⌘Z/⌘W demonstrably reach the webview here, only ⌘V misbehaved.

### 2026-07-02 — self-audit round: 7 findings fixed (session 9) — DONE
Sonnet review agents swept every surface changed today (primed with the session's failure patterns); 7 confirmed findings, all fixed:
**[P0] ValuePop/RowPeek portal keystroke leak** — same class as the ContextMenu filter fix but missed: keys typed in the pop-out/peek bubbled through the REACT tree into Grid's type-to-edit (seeded a cell editor UNDER the modal; Enter couldn't newline). stopPropagation at both roots. **[P0] Commit/preview read the ACTIVE-tab mirror mid-flight** — switch tabs while a commit round-trips and buildRowEdits pulled PK WHERE values from the OTHER tab's rows (silent wrong-row write); patchStatement painted the wrong tab too. Both now read/write `byTab[the-edit's-tab]` explicitly (patchStatement gained a tabId param). **[P1] Submenu filter + keyboard**: parent nav state indexes the UNFILTERED list — Enter activated the stale index, not the visible match; while the ctx-search input is focused it now owns the keyboard. **[P1] Inspector Copy shipped the 8KB prefix silently** on unmapped results (edit path was gated, copy wasn't) — copy disabled with an honest tooltip until the full value is loaded. **[P1] deleteProfile leaked the spare + primary session** and an in-flight replenish could mint a session for a deleted profile — full teardown + connState flip before the delete. **[P2] "+ Filter" seeded `op:"="` on boolean first columns** (select desync) — seeds `opsForType(...)[0]`. **[P2] run() re-entrancy**: second ⌘↵ during a confirm modal overwrote the single danger resolver, orphaning the first run — synchronous per-tab runInflight guard spanning the prompts, released on every exit path.
Pattern reinforced for the ledger: EVERY portaled interactive element needs keydown stopPropagation; EVERY store helper that reads "current" state during an async flow must take the flow's own tabId, never the active-tab mirror.

### 2026-07-02 — FK nav → auto-run query tabs (session 9) — DONE
User call (agreed — more SQL-first): FK navigation (forward + Referenced-by) now opens a QUERY tab named after the target table with `SELECT * FROM t WHERE col = 'v' LIMIT 100` visible in the editor and auto-executed — the filter is never hidden GUI state, the WHERE is immediately tweakable, and the result stays editable via the edit-from-SQL pipeline. Trade-off accepted: loses browser filter-builder chrome for these jumps; tabs accumulate while spelunking (mitigated by LIMIT + named tabs; ⌘W is cheap). `openTable(initialFilters)` machinery kept for future use.

### 2026-07-02 — v0.45 feedback round 3 (session 9) — DONE
**Submenu detached from its parent item** (tall Referenced-by panel floated above the menu) → the generic clamp's edge-FLIP (`top = y - height`) is wrong for submenus; SubPanel now positions itself: attached to the parent item's right edge, SLIDING vertically only as far as needed to fit (left-of-menu fallback when no room right). Gotcha: `useClampedPosition`'s flip is for context menus at a POINT — attached panels need slide-clamping, not flipping. **Filter column switch string→bool left a stale "=" op** → column onChange normalizes op+value against the new type's op list (bool → IS + TRUE dropdown instantly).

### 2026-07-02 — v0.45 feedback round 2 (session 9) — DONE
**Bool picker** reverted to horizontal (user preferred), t/f wire semantics + NULL kept. **Submenu filter seeded grid cells** → the React-portal gotcha: portaled SubPanel events bubble through the REACT tree (not DOM) into the grid's type-to-edit — input now stopPropagation()s every key (gotcha ledger: ANY portaled input above the grid needs this). **RowPeek editing** → replaced in-place editor (reflowed the whole panel on tall JSON) with a **pop-out value editor** (`ValuePop`): fixed-size modal, col name + type chip, caret lands after the value; because it's its OWN overlay-stack entry above the peek, Esc closes the editor first and the peek never moves — the stack solved the esc-layering bug by construction. **Bool filter rows** → constant 3-control layout: op = `IS` / `IS NOT` / raw SQL, value = fixed TRUE/FALSE/NULL select (whitelisted in browseSql); no more controls appearing/vanishing as you click; op-switch normalizes the stored value.

### 2026-07-02 — v0.45 feedback round (session 9) — DONE
Five user-verify findings fixed. **⌘V paste dead** → root cause: Tauri's DEFAULT app menu owns the ⌘V accelerator, so the grid keydown NEVER fired (same class as the ⌘Q hole) — paste now handled via the DOM `paste` event on the grid container (menu-driven paste produces it too); keydown branch removed. **Gotcha for the ledger: any ⌘-shortcut also present in the default Edit menu (V, maybe Z/X/A on some paths) may never reach webview keydown handlers — prefer semantic DOM events (paste/copy/cut) or wait for the native-menu takeover in v0.5.** **Type-to-edit caret** landed before the seed char → textarea now focuses manually with setSelectionRange(end). **Bool editor** → vertical layout, includes NULL, and commits WIRE text 't'/'f' — PG returns bools as t/f, so re-picking the current value is a no-op instead of phantom-dirtying (label still true/false). **Referenced-by submenu unusable on hot columns** (user_id → dozens of tables) → ContextMenu grew max-height + scroll + active-row scrollIntoView, and SubPanel gets a sticky FILTER INPUT when >12 items (Enter picks first match; typing works because the esc-stack only intercepts keys it handles). **RowPeek v2** → structured values (json/jsonb/pg-arrays) render PRETTY-PRINTED; editable cells edit INLINE in the pretty form (dbl-click, ⌘↵ stage / esc) — json/jsonb validate-then-stage-verbatim (text fidelity + big-number guard reused), arrays convert back via jsToPgArray; dirty rows tint with ✎; walking rows abandons an open editor. Space peek is now a mini record editor.

### 2026-07-02 — v0.45 editing-power batch, ALL AT ONCE (session 9) — DONE
The whole milestone in one pass. **Undo/redo of staged edits** (`edits.ts`): per-tab undo/redo stacks of `pending` snapshots (cap 100); every mutator pushes an undo step; new `setEditsBatch` makes multi-cell operations ONE step AND one store write (also killed the old O(N²) per-cell Set-NULL writes). ⌘Z/⌘⇧Z grid-scoped. **Fill-down ⌘D** (top row of the selection fills below, per column) + **paste-into-selection ⌘V** (TSV block from Excel/Sheets anchored at the selection's top-left, or single value into the whole rect) — both STAGED, so ⌘S previews the SQL first. **Full keyboard grammar**: PageUp/Down, Home/End (row ends), ⌘Home/End (first/last row), Tab/⇧Tab, F2, type-to-edit (printable char opens the editor seeded), Delete/Backspace stages NULL over the selection, Enter-saves-then-advances-down, Space = row peek. **Type-aware editors**: pg_enum introspected (`SchemaSnapshot.enums`, 6th result set) → enum columns get a picker, bool columns get true/false buttons; text editor is now an auto-grow TEXTAREA (multiline values finally editable inline; ⌥/⇧-Enter = newline). **Set DEFAULT**: `RowEdit.use_default` end-to-end (Rust emits `SET col = DEFAULT`), accent DEFAULT chip in the cell, inspector honest about it — proven live (`new_value == 'dflt'`). **Per-cell revert**: "Revert edits in selection" menu item (batch of value:=original → one undo step). **FK navigation**: cell menu "Open referenced <table> →" (single-col FKs, snapshot-matched by result column name) and "Referenced by ▸" reverse lookup — both open a browse tab PRE-FILTERED via `openTable(t, initialFilters)`. **Row peek** (`grid/RowPeek.tsx`): Space transposes the focused row into a modal (column|value, type icons, NULL/∅ chips), ↑/↓ walks rows. **Duplicate row** (browser): prefills the draft band minus PK/ctid. **Filter power**: ops extended (NOT IN/NOT LIKE/NOT ILIKE/~/!~/contains/starts/ends/IS TRUE/IS FALSE) + type-aware op lists per column + **raw SQL** escape-hatch op (predicate verbatim); LIKE-family inputs auto-escape %/_. **EditPreview** gains Copy SQL.
Deferred from the v0.45 table (deliberate): column pin/freeze (fights the 2-axis virtualizer — revisit), date-picker editors, searchable filter-column picker (superseded by the v0.5 filter-bar-completion idea), JSON array element add/remove. Alias caveat recorded: FK nav matches by RESULT column name — an aliased FK column won't offer navigation (correct, never wrong).
All 6 staging tests green incl. new DEFAULT round-trip; RowEdit test literals updated for `use_default`.

### 2026-07-02 — column-reorder drop indicator (session 9) — DONE
User: no visual feedback for where a dragged column lands. Added a live insertion guide: full-grid-height 2px accent line (vertical gradient fade + glow, beacon dot at top, z above the sticky header) at the exact boundary the splice will use — `computeDrop()` is SHARED between the mousemove (line) and mouseup (commit), so the indicator can never disagree with the landing spot; left-edge when dragging left, right-edge when dragging right, hidden while hovering the column's own slot. rAF-throttled; the drop target is cached on the drag ref so the commit uses precisely what was shown.

### 2026-07-02 — design pass: header sort affordance (session 9) — DONE
User: sort should be a visible, beautifully-styled arrow, not whole-header click. New micro-interaction: a ghost ⇅ (ChevronsUpDown) materializes on header hover (scale 0.82→1 + fade, 160ms standard-decelerate), morphs to a single chevron that SPRING-FLIPS 180° between asc/desc (220ms overshoot bezier), accent-pill background when active; the sorted column earns a gradient accent BASELINE (edge-faded 2px bar + soft glow) so sort state reads at a glance from across the grid; long header names fade under a mask-image gradient instead of reflowing — nothing jumps on hover. Gesture map rebalanced: header click = SELECT COLUMN again (natural, was the pre-sort behavior), drag = reorder, arrow = sort — no modifier needed for any of the three. Arrow stops propagation on mousedown/dblclick so it can never start a reorder or trigger cell paths.

### 2026-07-02 — grid: header sort + column reorder + drag autoscroll (session 9) — DONE
Three user asks in one grid pass, built on VIEW-INDEX MAPS over untouched data (`rowOrder`/`colOrder` + inverses) — the load-bearing invariant: staged edits, truncated markers, find hits and editability stay keyed to UNDERLYING indexes, so sorting after staging an edit can never make ⌘S write through the wrong row (a naive rows-array sort would have been a corruption bug: buildRowEdits reads PKs by row index at commit time). **Sort**: header click tri-states asc→desc→clear with an ▲/▼ glyph — client-side over loaded rows in editor results (NULLs always last; numeric compare from the editability type), REAL server ORDER BY in the browser (client-sorting one page of paged data would lie). **Reorder**: drag a header horizontally (>4px) and drop — view-level `colOrder` splice; widths stay per-underlying-column; `estimateSize` reads through a `colAtRef` since the virtualizer is declared before the mapping block. **Header gestures**: click = sort · drag = reorder · ⌘/⇧-click = the old column-select (tooltip documents all three). **Autoscroll**: dragging a selection past the viewport edge scrolls (rAF loop, 24px threshold, ≤28px/frame) and keeps extending the selection to the pointer via a prefix-offset column hit-test; loop inputs live in refs so the callback is stable. Sort/reorder reset on statement change (session-only view state).
Every selection-consuming path (copy formats incl. INSERT ctid-exclusion, Set NULL/EMPTY, delete-rows locators, inline editor, inspector target, draft band, ⌘F scroll-to-hit) audited and mapped view→data.

### 2026-07-02 — v0.4 perf batch 1 (session 9) — DONE
Five of the register's top items. **Grid**: `Cell` extracted + `React.memo` on primitive props — an arrow key / drag step / stream flush now repaints only the cells whose state changed, not the whole visible window; ALL cell mouse handling delegated to the container via `data-r`/`data-c` (`closest("[data-r]")` hit-test) so cells carry zero per-render closures; `mouseover` (bubbles — mouseenter doesn't) feeds an rAF-coalesced dragOver (one store write per frame, was one per cell crossed); window-mouseup ends drags released outside the window (stuck-drag bug dead). **Shell**: App.tsx selects tab scalars (kind/name/table-ref) instead of the tab object — setSql replaces the active tab object every keystroke, which re-rendered TabBar+grid+sidebar per keypress; inspector resize writes a `--inspector-w` CSS var per mousemove with ONE store/localStorage write on mouseup (was: full shell re-render + synchronous localStorage per pixel). **Hot-path imports**: completion engine's 3 dynamic imports per invocation → static (source finally synchronous-in-practice as ARCHITECTURE claims); ⌘E/⌘I keydown + grid focus/edit-routing imports static; focus→inspector setTarget debounced 60ms. **Browser**: filter value applies on Enter/blur (typing "manish" = 1 query, was 6); setFilters diffs the compiled SQL and skips the server when nothing effective changed (checkbox/col/op churn on empty values = zero queries).
Measurement debt (gate says measured-not-vibed): changes are structurally strict wins (fewer renders/writes by construction); formal before/after numbers land with the perf-harness item later in v0.4. Delegation gotcha: `mouseenter` doesn't bubble — delegated drag uses `mouseover`; rownum/header keep their own handlers (no data-r → hit-test ignores them).

### 2026-07-02 — first-run latency v2: spare-session pool (session 9) — DONE
Upgraded the warm-on-switch approach (same session) to a **pre-warmed spare pool**: ONE hot standby session per connected profile (module-level maps in connections.ts). A fresh tab's first run CLAIMS the spare instantly — zero handshake, guaranteed even if the user runs the moment the tab opens — and `replenishSpare` builds the replacement in the background. Strictly better than warm-on-switch: connection footprint = tabs-that-ran + 1 (was: every VISITED tab), and no race between typing speed and the handshake. Lifecycle: built after connect() and after any unspared direct connect; dropped on reconnect (old epoch), invalidate, and profile disconnect; a spare that dies quietly (bastion idle-drop) is detected via the session-closed event's session_id — replaced silently WITHOUT flipping the profile dot (App.tsx now passes session_id to markDisconnected; full per-session handling for tab sessions still a ledger item). `connecting` honest-UI state from v1 retained as the fallback for the no-spare path.

### 2026-07-02 — first-run-in-tab latency: session pre-warm + connecting state (session 9) — DONE
User-found: 2-3s dead pause on the first ⌘↵ in every fresh tab over SSH (fine on re-run). Cause: per-tab sessions — the first run pays the full TCP+TLS+auth handshake through the bastion, and nothing in the UI said so (Cancel button only appeared once `running` flipped, after the handshake). Three-part fix: **(1) pre-warm** — the tab's dedicated session is established in the BACKGROUND on tab switch/creation (results.ts tabs-subscriber; only for already-connected profiles — a tab switch must never trigger a tunnel build) and for the active tab right after connect(); by the time the user types a query the session is ready. **(2) in-flight dedupe in ensureTabSession** — concurrent callers (warm-up + run + explain) share one promise; previously two parallel connects leaked the loser's backend session (audit item); catch now also sets errorProfileId so the toast keeps its context. **(3) honest UI** — `connecting` per-tab flag around ensureTabSession: Run button shows "Connecting…", status bar 🔌, empty pane says Connecting… (rarely visible now that warm-up wins the race).
Trade-off noted: warm-on-switch means every visited query tab holds a PG connection (was: every RUN-IN tab) — same steady state, earlier timing; the audit's session-count-visibility item stays open.

### 2026-07-02 — bugfix: duplicate-tab-id wedge from load() re-merge (session 9) — DONE
User-found: two identical tabs, neither closable, "⚠ not saving" stuck. Root cause: batch-4's load() scratch-merge was NON-idempotent — a second load() call (HMR remount re-runs App's effect; StrictMode would too) counted the already-restored tabs as "scratch" and merged them with the same disk rows → two tabs sharing one ID. Cascade: closeTab's filter removed both copies → fallback undefined → TypeError swallowed by onClick → close appeared dead; tabs_save hit the id PRIMARY KEY → permanent save failure. Three-layer fix: (1) `load()` runs exactly once (`if (loaded) return`) + scratch-merge excludes ids already on disk; (2) closeTab guards the empty-next case and null-coalesces the fallback — a bad state can never wedge closing; (3) doSave dedupes rows by id so a dupe can never permanently break persistence. **Lesson for the gotcha ledger: any store action called from a React effect must be idempotent — dev HMR remounts re-run effects.**

### 2026-07-02 — bugfix: DangerModal keyboard grammar (session 9) — DONE
User-found: "⌘⇧D doesn't reset edited cells" — the new discard confirm's auto-focused Cancel ate plain Enter (silent cancel that read as broken). Fix: explicit grammar on DangerModal — Esc = Cancel, ⌘↵ = confirm, plain Enter = NOTHING (preventDefault suppresses focused-button activation; a destructive action can be neither Enter-confirmed nor silently Enter-cancelled), key hints rendered on both buttons. Applies to every confirmDanger surface (delete rows, no-WHERE, quit guard, bulk close, re-run-with-edits). Store path audited clean — discardAll always worked; the modal UX was the bug.

### 2026-07-02 — v0.35 batch 6: honesty (§1c) + robustness (§1d) sweep (session 9) — DONE
Fourteen items across both remaining ledger sections. **Display truth**: NULL renders as a dim chip, `''` as "∅ empty", literal 'NULL' text plain; numerics right-align tabular-nums; browse status bar says "first N rows — scroll for more" / "all N rows" (page never reads as the whole table); late errors banner over live rows (batch 4) + statement error pane and editor squiggle now show **PG DETAIL/HINT** (plumbed Rust→QueryEvent→types.ts→UI). **Search truth**: palette fuzzy-ranks the WHOLE table catalog (was: first-400 slice made deep tables unfindable; forceMount defeats cmdk re-filtering); history LIKE properly escaped (`%`/`_`/`\` + ESCAPE); history_add moved into the `finished` event (ms=0 race dead); "Clear history (all)" relabeled to what it does; DbSwitcher failure shows the error, not "No databases"; introspection failure shows message+Retry and ⌘R keeps the old tree (stale-while-revalidate). **Copy fidelity**: TSV Excel-quoting (never mutates data — old tab→spaces gone), CSV `\r`, markdown `\n`→`<br>`, JSON dedupes duplicate column keys. **Driver robustness**: ROW_CAP auto-cancel (last statement / dead sink → cancel own query, swallow 57014 as capped completion — proven live incl. the cancel-races-and-loses path); TCP keepalives 30s/10s×3; `statement_timeout=300s` + `idle_in_transaction_session_timeout=600s` at connect (deviation from locked 30s recorded in DECISIONS — 30s would bite real analytics queries before Settings exists); cancel gets a 3s deadline; disconnect cancels in-flight queries; splitter lexes `E'…'` backslash escapes (statement-index desync fixed, unit-tested).
All 6 staging tests green (auto-cancel test uses per-row pg_sleep so the cancel genuinely lands; fast queries legitimately win the race and complete as a normal capped drain).

### 2026-07-02 — v0.35 batch 5: per-tab EditorState — cross-tab undo fix (session 9) — DONE
`SqlEditor.tsx`: one `EditorState` per tab, swapped via `view.setState()` on activeId change (subscribe to useTabs INSIDE the mount effect; `select()` sets activeId before setSql, so the old doc-replace subscriber now no-ops on switches and only serves genuine same-tab external changes). Kills the worst editor bug: the shared undo history meant a tab switch was a doc-replace ON the undo stack — ⌘Z in tab B restored tab A's SQL into B and persisted it. Ride-alongs for free: per-tab undo/redo depth, selection, and cursor position survive tab switches in-session. Stale-cache guard compares cached doc vs tab.sql (kept in sync while active). Cache lives in the effect closure → theme remount (isDark) restarts it clean — undo-across-theme-change arrives with the Compartment fix (v0.35 table, still open).
Gotcha: per-tab states share ONE extensions array — history/search/diagnostic state are StateFields so each EditorState carries its own values; safe to reuse the array.

### 2026-07-02 — v0.35 correctness batch 4: never-lose-work sweep (session 9) — DONE
Ten §1b ledger items. **Persistence**: `persist()` gated on `loaded` (a failed startup load can no longer be followed by a replace-all save that WIPES every stored tab); `load()` catches + retries 1.5s, hands out a scratch tab meanwhile and merges any typing into the restored set; `tabs_save` failures now auto-retry (3s) + show a persistent "⚠ not saving" badge in the tab strip; `flushTabs()` fires the debounced save on window blur / visibility-hidden; `PRAGMA busy_timeout=2000`. **Window close**: `onCloseRequested` holds the close, AWAITS the flush, and prompts (DangerModal) when any tab holds uncommitted edits or a half-typed draft row; `win.destroy()` completes an approved close. Known hole: ⌘Q via the default app menu skips onCloseRequested — fully closes when the native menu bar owns Quit (v0.5 P0); blur-flush narrows text loss to ~0 meanwhile. **Edit-loss guards**: explicit re-runs (⌘↵ / filter / sort / refresh) confirm "Discard N staged edits?"; scroll-triggered loadMore PARKS while edits are staged (no modal on scroll); bulk tab closes (others/right/all) show one aggregate confirm; close guard counts the draft row; ⌘⇧D discard-all confirms. **Misc**: late global errors render as a banner over still-browsable rows (was: wiped 30k streamed rows); connection editor Esc/Cancel confirms when dirty; ⌘⇧T closed-stack entries carry kind/table/saved_id — table tabs reopen, saved-query links survive.
All 6 staging tests green. Gotcha: `win.destroy()` bypasses onCloseRequested — that's the approved-close path; never call it outside the guard.

### 2026-07-02 — v0.35 correctness batch 3: corruption sweep (session 9) — DONE
Nine ledger items in one pass. **JSON staging class**: `structuredValue` now STRICTLY type-gated (json/jsonb/array only — a text cell that looks like JSON renders/edits as text; kills the '[draft]' corruption); raw JSON mode stages typed text VERBATIM for json+jsonb (parse = validation only — no minify/key-reorder, >2^53 numbers survive); `json` (non-jsonb) tree editing disabled (raw only); docs with bare ≥16-digit numbers get tree-edit + pretty-print disabled w/ warning chip (raw text displayed/copied, never a rounded re-serialization); array raw edits require a JSON array root; `parsePgArray` returns undefined on malformed input (was: fabricated `[]`); JSON key-rename onto an existing key is a no-op (was: silent value loss). **SQL-gen class**: new `lib/sqlIdent.ts` (`qi` quote-when-needed + PG reserved list, `qualify`, `qualifyDotted`) — SchemaTree SELECT/copy refs quoted; copy-as-INSERT emits the real qualified table name + quoted columns and excludes ctid locator cols (was: literal `your_table`, unquoted, ctid included); `standard_conforming_strings=on` forced at connect (edit literal escaping assumed it). **Misc**: browse WHERE folds left-associatively (mixed AND/OR now means what the linear filter UI reads); draft-row ⌘↵ double-commit guarded (was: duplicate INSERT); tab rename no longer pushes the buffer's unsaved draft over the linked saved query's SQL.
All 6 staging + 9 unit tests green. Gotcha: structuredValue's strict gate means unmapped results (editability 'unavailable') show raw JSON text, not a tree — honest-over-pretty, revisit when type_oid lands on the streaming path (v0.5).

### 2026-07-02 — v0.35 correctness batch 2: verify-then-commit + edit guards (session 9) — DONE
**matched≠1 → ROLLBACK** (`edit.rs`): apply_edits/delete_rows rewritten to verify-then-commit — explicit BEGIN, each UPDATE/DELETE runs as its own statement, RETURNING count checked per row; ANY mismatch (stale ctid, 0-row locator) → ROLLBACK everything, `committed:false`, per-row messages ("N rows matched (expected 1)" / "rolled back — another row did not match exactly 1"). Previously a 0/2-row match COMMITTED and reported success. Also killed the `[0]`/`unwrap` panic sites (edit.rs:130,330,574 → `.first()`/`ok_or`).
**Frontend commit honesty** (`edits.ts`): buildRowEdits now returns `{rowEdits, used}` aligned pairs — kills the latent result/index misalignment (results were mapped positionally onto the UNFILTERED edit list); commit clears ONLY keys that actually committed — rolled-back and skipped-map edits stay staged with a status-bar message ("N edits skipped (no editability info) — still staged"). Grid delete now checks `outcome.committed` and reports rollback instead of blindly re-running.
**NULL→'' corruption** (`Grid.tsx`): cell editor tracks `startedNull`; opening a NULL cell and closing with an empty draft stages NOTHING (before: blur staged `''`). Editor shows a NULL placeholder. Empty string stays reachable via explicit Set EMPTY.
**Truncated-cell edit guard** (`Grid.tsx` + `Inspector.tsx`): all edit entry points refuse a truncated cell until its full value is fetched — grid inline editor routes truncated cells to the inspector; inspector gates pencil/dblclick/tree/raw on `fullLoaded` and auto-enters edit when the fetch lands (wantEdit ref); chip reads "editing disabled until loaded". Also fixed: staged edit now always wins over the fetched DB value in the inspector display.
**Proven live**: new `staging_matched_rollback` test (mixed valid+stale edit batch rolls back fully; single valid commits; same for deletes) + all 5 prior smoke tests green.

### 2026-07-02 — v0.35 keystone: statement-at-a-time execution (session 9) — DONE
`execute_stream` rewritten (`driver/postgres/execute.rs`): each split statement now runs as its OWN `simple_query` — psql autocommit semantics. Fixes the worst 'never lies' bug: whole-buffer runs were ONE implicit transaction, so the UI reported early UPDATEs committed while a later error silently rolled them back. Now: committed means committed; a mid-script error stops the run AT the failing statement; explicit BEGIN/COMMIT still span statements (same session); VACUUM/CREATE INDEX CONCURRENTLY/CREATE DATABASE work in multi-statement buffers. Rode along: `StmtSpan` in splitter (statement + char offset) → PG error positions rebased onto the whole buffer (squiggle now correct for statement index > 0); StatementStart fires at TRUE start (progress + real ms for DDL/UPDATE, was ~0); `affected` always passed through (UPDATE…RETURNING keeps its count). Zero frontend changes needed — results.ts already handled per-statement `error` events + the Err return.
**Proven live**: new `staging_statement_at_a_time` test (autocommit-before-error, position rebase, VACUUM-mid-buffer, BEGIN/ROLLBACK spanning, DO-block real ms) + all 4 existing smoke tests green against staging (cap streaming + cancel regression-clean).
Semantics decision recorded in DECISIONS.md: psql parity (autocommit + stop-on-first-error) over Postico's atomic-implicit-tx; a "run atomically" toggle is a candidate for beyond-v0.5.

### 2026-07-02 — v0.3 Step 6: Copy URI + paste-DSN (session 9) — DONE
Both halves of the DSN round-trip. **Copy**: Rust `connection_uri(profile_id, include_password)` command (percent-encodes userinfo/dbname, brackets IPv6 hosts, appends sslmode; password read from Keychain ONLY on the with-password variant per locked decision #2) surfaced as a "Copy URI ▸" submenu in the shared connection menu (rail + dashboard): no-password default / with password / Copy psql command. **Paste**: `home/dsn.ts` regex parser (WHATWG URL unreliable for non-special schemes in WKWebView) — handles postgresql:// spelling, %-encoded creds, IPv6 brackets, sslmode mapping (allow→prefer, verify-*→require); ConnectionEditor form-level onPaste fills fields + password, autonames from dbname, flashes "filled from pasted URI". User-verified round-trip on staging. **v0.3's explicit asks are now all shipped** (menus / ⌘F ×2 / copy-URI+paste); remaining v0.3 table items (copy-as-INSERT table name, CellEditor clamp, gutter/header menus) fold into the v0.35 correctness batch.

### 2026-07-02 — v0.3 Step 5: ⌘F find system (session 9) — DONE
One ⌘F mental model, scoped to the focused surface. **Editor**: `@codemirror/search` (`search({top:true})` + `searchKeymap` appended after defaultKeymap so existing binds win) + `highlightSelectionMatches`; panel restyled with theme tokens in editor.css; context-menu "Find / replace…" item via `openSearchPanel(view)`. **Results grid**: `stores/find.ts` (open/query/hits/hitSet/idx) + `grid/FindBar.tsx` (owns the 120ms-debounced match compute over loaded rows; NULL cells never match text; 5k hit cap; honest "loaded X of Y rows" chip when capped) + Grid subscribes hitSet/current for `.find-hit`/`.find-cur` classes and scroll-to-current. **Routing** in App.tsx window handler: CM and JsonTree preventDefault their own ⌘F before it; `.inspector-fixed` containment guard covers the raw-JSON CM field; ⌘G/⇧⌘G step while open. **Fixed en route**: JsonTree's document-level ⌘F hijack (audit P1) — now only fires when focus is already inside the inspector. User-verified on staging.
Gotcha: CM search panel close button is `[name="close"]` (plain button) — style via attribute selector; panel checkboxes take `accent-color`.

### 2026-07-02 — v0.3 Step 4: connection + saved-query menus (session 9) — DONE
Shared `sidebar/connectionMenu.ts` builder (connected: Open/Disconnect/Reconnect · else Connect · Edit…/Duplicate via `clone_connection`/danger Delete) wired into ConnectionRail (replacing right-click→edit) and Dashboard cards; `savedMenu` on SavedQueries rows (Open/Rename/Duplicate/Copy SQL/Delete w/ DangerModal). User-verified in-app 2026-07-02. Copy-URI deliberately left for its own step (redacted default + with-password submenu).

### 2026-07-02 — Mission upgrade + 13-agent density audit (session 9)
User pivot: goal is no longer "releasable v0.5" but **the best Postgres client that exists — surpass TablePlus/Postico/Beekeeper/DataGrip/pgAdmin**, no finish line. Two invariants: never slow at any scale; never lies/corrupts. Ran a 13-agent audit (9 code-surface auditors reading every src/ + src-tauri/ file, 4 web-researched competitor/workflow lenses) → **`docs/GAPS.md`** (ranked census: correctness ledger §1, perf register §2, per-surface gaps §3, surpass list §4, sequencing §5) + raw reports in `docs/audit-2026-07/*.json`. ROADMAP_v0.5.md updated: audit banner, v0.35 re-scoped as the **Correctness Milestone** (keystone = statement-at-a-time execution in the driver), v0.4 gains promoted P0s (run-under-cursor, selection stats, App-shell re-render fix, ROW_CAP auto-cancel), new "Beyond v0.5 — surpass backlog" section.
Scariest audit finds (all in GAPS §1): editing a truncated >8KB cell commits the 8KB prefix (corruption); multi-statement runs are one implicit tx — UI reports early UPDATEs committed then a later error rolls back everything; scrolling a browse you're editing wipes staged edits (loadMore → resetTab); ⌘Q loses ≤600ms typing + skips close guard; cross-tab undo bleeds SQL between tabs; failed tabs_list at startup → next persist wipes all saved tabs; AND/OR filter precedence compiles wrong WHERE; NULL→'' staged by editor blur.
Also this session: **Step 4 (connection + saved-query context menus) shipped but still awaiting user in-app verification** — verify before ticking. Harness gotcha: pausing a running Workflow kills its in-flight synthesis agent (restart-from-zero on resume); audit results were salvaged from the workflow journal and synthesized inline.

### 2026-06-26 — v0.3 Step 3: context menus on sidebar tree + tabs (session 8) — DONE
Right-click menus on the two highest-traffic surfaces via the Step-2 primitive. **Sidebar table tree** (`SchemaTree.tsx`, had no menu): Browse / Open SELECT in editor / Copy name / Copy qualified name / Copy SELECT / Refresh schema. **Editor tabs** (`TabBar.tsx`): Rename / Duplicate + Copy SQL (query tabs) / Close / Close others / Close to the right / Close all. Added `closeOthers`/`closeToRight`/`closeAll` to the tabs store (loop `closeTab`, then reselect the kept tab). User-verified on staging.
Known limitation (followup): bulk close (others/right/all) bypasses the unsaved-edits close-guard — single Close still routes through it. Acceptable for v0.3; revisit if it bites.

### 2026-06-26 — v0.3 Step 2: nested ContextMenu primitive (session 7) — DONE
`app/overlay/ContextMenu.tsx` — declarative menu (`MenuNode[]`: item / submenu / sep) on the overlay primitive. One submenu level, full keyboard nav (↑/↓ move · →/Enter open-or-activate · ← back · Esc close) driven through the stack's topmost-only `onKey`; mouse hover/click too. Submenu = a portaled `SubPanel` clamped via the extracted `useClampedPosition` (z 45, above the menu catcher), no backdrop of its own (parent owns close + keys). Refactored `Overlay.tsx`: extracted `useClampedPosition`, added `onKey` to `AnchoredOverlay`. Migrated grid menu (Copy + Copy-as▸ submenu + Set NULL/EMPTY + Delete) and editor menu (Run / Search fns / toggle) onto it; dropped their motion/menuIn usage. User-verified on staging.
Notes: submenu anchor rect is read off the (motion-scaled) parent item so it can be ~3% off — invisible in practice; deeper-than-one nesting intentionally unsupported. Old `.vgrid-menu`/`.ed-menu` button CSS now dead (left in place, low-priority cleanup).

### 2026-06-26 — v0.3 Step 1: shared overlay primitive (session 6) — DONE
Built `src/app/overlay/` — the substrate the whole v0.3 context-menu/find work sits on. `escStack.ts` = one capture-phase window listener routing keys to the **topmost overlay only** (Esc→onClose; other keys→optional `onKey`), installed on import so it outranks component listeners. `Overlay.tsx` = `Modal` (portal to `<body>`, dim backdrop, outside-click) + `AnchoredOverlay` (point-anchored, viewport-clamp + edge-flip, measured via `offsetWidth/Height` so a motion scale doesn't undersize it) + `useOverlayLayer`. Migrated 7 popups onto it (each keeps its own CSS class + spring): grid menu, editor menu → `AnchoredOverlay` (clamp fix); ThemePicker (Esc fix), DangerModal, CloseGuardModal, EditPreview, Palette → `Modal`. All gates user-verified on staging.
Gotchas/decisions burned:
- **Portal to `<body>` is mandatory**, not cosmetic: `position:fixed` popups inside the floating-card shell can be positioned relative to a transformed card ancestor (motion leaves a transform), breaking fixed coords. Portaling fixes it for good.
- **Topmost-only key routing** (not per-component window listeners): two stacked modals (EditPreview under CloseGuard via ⌘S then ⌘W) both had capture-phase `Enter` listeners with `stopPropagation` → one Enter fired BOTH (commit + discard). Route every key through the stack to the topmost entry only. `stopPropagation` ≠ `stopImmediatePropagation` on a shared target — use immediate.
- **Clamp must measure `offsetWidth/offsetHeight`**, never `getBoundingClientRect`, when the content has a motion entrance scale — getBoundingClientRect returns the transformed (scaled) box → undersized clamp → edge clip.
- Layer-aware `onClose` for modals with a sub-form (ThemePicker draft): Esc backs out one layer at a time.
- escStack is a module singleton with a global listener → **relaunch `tauri dev` after editing it** (stale listener on HMR), same class as the store-subscribe gotcha.
Deferred to Step 2 (not bugs): fold FnSearch + the 3 CSS-anchored dropdowns (DbSwitcher / inspector-copy / sort) + ExplainView into the stack for full consistency; no re-clamp on window resize; optionally close transient menus when a modal opens.

### 2026-06-26 — v0.3 → v0.5 roadmap drafted (session 5)
Kicked off the **releasable v0.5 push**. Ran a 16-agent audit+research workflow: 8 subsystem code-audits (grid/results · editing/inspector · editor/completion · browser · connections/schema · Rust core · tabs/palette/history · shell/keyboard/perf) + 5 competitor inventories (TablePlus · Postico 2 · Beekeeper · DataGrip+DBeaver · community wishlist) → gap matrix → completeness critic → roadmap. Output = **`docs/ROADMAP_v0.5.md`**: 5 milestones (v0.3 → v0.35 → v0.4 → v0.45 → v0.5) + 4 cross-cutting tracks (perf/anti-sluggish · bug-polish · context-menu system · find system) + deferred list + 6 open questions. Every bug/perf item is file:line-cited. **No code yet — plan only.** v0.3 (next) builds the shared overlay/nested-menu primitive, ⌘F (editor + results), copy connection URI, and context menus on every surface. Open-question defaults to lock with the user before/while building. Same step-by-step QnA→build→verify rhythm as v0.2.5.

### 2026-06-17 — v0.2.5 QOL batch (session 4) — SHIPPED
Tables-as-tabs, type-icon headers, inline add-row, toolbar reorg, close-with-edits guard, connect-error surfacing, tab auto-scroll. All gates user-verified against live staging. Bumped to **v0.2.5**.
Gotchas burned this session:
- **HMR cross-store desync** (the nasty one): stores wire via module-level `subscribe()` (`tabs.activeId → results.active → edits.active → browser`). After heavy store-file HMR, the wiring can bind to a stale/duplicate store instance → `edits.active` stops tracking `tabs.activeId`, so a staged edit lands under one key while a reader (close guard) checks another → silent miss. **Relaunch `tauri dev` after store-file churn before trusting cross-store behavior.** Production single-load is unaffected.
- `height:100%` on a flex child breaks once it gains a sibling: `.tbrowser` under the new `TabBar` overflowed by the tab-bar height (pushed content up, clipped the bar). Use `flex:1; min-height:0`, never `height:100%`, for a flex column's growing child.
- Simple-protocol `ColumnMeta` has no type — for header type icons reuse the editability map's `type_name` (already fetched eagerly), don't touch the Rust streaming path.
- Inline draft row = a `position:sticky; top:HEADER_H` band rendered in normal flow (pushes the rownum column down automatically); data cells get an explicit `+draftH` Y offset since they're absolutely positioned.
- TLS→plain connect fallback was masking auth/pg_hba failures — only fall back on transport errors (`e.as_db_error().is_none()`), else report the server's message.

### 2026-06-14/15 — v0.2 visual overhaul (session 3) — SHIPPED
Major UI overhaul to a floating-card shell on a themed-glass gutter, plus a colour/theme engine and per-tab results. All gates user-verified against live staging. Bumped to **v0.2.0**.
- **P2.1 layout shell**: transparent window + vibrancy gutters; floating `.card` panels (rail / sidebar / main / inspector) with `--gutter`/`--rail-w` tokens; `panelIn`/`railItemIn` springs.
- **P2.2 theme engine** (`design/theme.ts`): palette = seeds → full token set as inline CSS vars (no flash); hue (8 Pokémon palettes) + anchors (custom) kinds; auto-contrast `--accent-fg`; dual-mode synthesis so custom themes flip with the light/dark toggle; ThemePicker.
- **P2.3 rail + customization + home**: shared `Avatar` (colour+glyph), ConnectionRail with drag-reorder (`Reorder` + `position` column), home Dashboard + full-panel ConnectionEditor, DB-switcher, recent-activity strip, connection liveness (`on_close` → `session-closed` → auto-reconnect).
- **P2.4 per-tab results**: `useResults`/`useEdits` keyed `byTab` with the active tab mirrored to top-level — zero consumer churn, background streams can't bleed into the visible tab.
- **P2.5 inspector redesign**: chips + icon toolbar, structured formatting for JSON *and* PG arrays (arrays editable via `jsToPgArray`), colorized raw view/edit via CodeMirror `JsonField`, split copy button.
- **P2.6 breadcrumb + anim**: connection/db/context breadcrumb (`swapIn`); inspector animates its *width* (CSS) so the main card reflows in lockstep; DB-switcher queries a live session + reuses an existing connection for the picked db.
- **P2.7 robustness + logo + scalar polish**: saved-connection edits now invalidate stale sessions + tunnel (`connSig` + tunnel `spec`); commit/preview re-resolve a live session; editor shortcuts via capture listener; new macOS-grid app logo (`scripts/make_icon.py`); inspector scalar value card + auto-growing editor + shortcut hints.

Gotchas burned this session:
- WKWebView `<button>` doesn't take focus on click → click-then-⌘S won't bubble; use a capture-phase `window` keydown listener for surface-level shortcuts.
- A cached SSH tunnel keeps forwarding to its *original* host — repointing a profile must drop/rebuild it (tunnel `spec` mismatch), else writes silently hit a read replica.
- Never colorize text via a transparent textarea over a `<pre>` — WKWebView's opaque native field background covers it; use CodeMirror.
- App icon: the raw logo PNG full-bleed reads as "pasted"; reshape to Apple's 824²-in-1024 rounded-squircle grid. Dev runs the bare binary (icon baked at compile time); macOS may cache — `tauri build` for the final.

### 2026-06-13/14 — v0.1.5 P1.1–P1.7 (session 2)
Shipped the entire 11-feature backlog as v0.1.5 (P1.1→P1.7), every gate user-verified against live staging.
- **P1.1 edit power**: `plan_edits` groups edits by (table,row) → one batched UPDATE/row; ctid-fallback editing (detect a `ctid` result column → row locator when no PK), `is_ctid`/`warn` on ColumnEditMeta.
- **P1.2 row lifecycle**: browser insert panel (`insert_row`, blank=DEFAULT/∅=NULL/typed=value) + grid context-menu delete (`delete_rows`, one txn, DangerModal preview). Browser auto-selects `ctid, *` for PK-less tables. Also: global `noAutocorrect` killer for the WKWebView macOS suggestion bubble.
- **P1.3 JSON power**: rewrote JsonTree with ⌘F search (filter+highlight+hit-nav) and in-place type-preserving leaf/key editing → stages to pending edits.
- **P1.4 per-tab sessions**: `ensureTabSession` (keyed profile::tab), `results.executedSessionId` so edits share the tab txn; isolated primary session for introspect; amber tab tx-dot (SQL-sniff).
- **P1.5 light theme**: `:root[data-theme=light]` token override, `--syn-*`/`--hl-*` tokens, parametric CM `dark` flag (editor remounts on theme change), settings.theme system|dark|light persisted, palette Appearance group.
- **P1.6 native skin**: window-vibrancy Sidebar material (transparent window + macOSPrivateApi feature must match conf), opaque content panes; custom app icon (qwry-icon.svg → tauri icon).
- **P1.7 ssh tunnel**: `tunnel.rs` spawns system `ssh -L` (BatchMode, ~/.ssh/config), one tunnel/profile shared across sessions, connect routes through the local port.

Gotchas burned this session:
- `tauri` cargo feature `macos-private-api` MUST match `app.macOSPrivateApi:true` in tauri.conf or build.rs aborts.
- editing Cargo.toml mid-session makes the `tauri dev` watcher rebuild — it can show transient errors from a half-applied edit; trust a clean standalone `cargo check`.
- WKWebView forces macOS autocorrect on every field; no global switch — stamp `autocorrect/autocapitalize/spellcheck=off` per field + MutationObserver (`app/noAutocorrect.ts`).
- per-tab sessions = one PG connection per tab (and one ssh tunnel/profile shared); tx indicator is a SQL-sniff heuristic.

Remaining deferred (not in v0.1.5): Glide grid reeval (only if grid feels slow), ERD, CSV/Parquet import-export, MySQL/SQLite drivers, LISTEN/NOTIFY, flame-graph EXPLAIN. Scratch tables on staging: `qwry_scratch_ctid`, `qwry_scratch_json` (drop when done testing).

### 2026-06-12 — P5–P10 (session 1, conclusion)
ENTIRE v1 ROADMAP SHIPPED IN ONE SESSION (P0→P10), every gate user-verified against live staging data. The three headline differentiators all work: (1) schema-aware intellisense with alias scoping + FK joins, (2) editable results from arbitrary SQL via prepare()-metadata, (3) free first-class jsonb. Plus: streaming 1M-row grid, table browser with AND/OR filters + searchable sort, tabs/saved-queries/history/palette, EXPLAIN viz, danger guards, native chrome with springs.

Key gotchas burned into this codebase (also see inline notes):
- vite HMR lies for CodeMirror-internal modules — restart `tauri dev` to verify editor changes
- `window.confirm()` is a silent stub → two-click arm pattern
- drag regions need `core:window:allow-start-dragging` (dblclick works without it — trap!)
- never rely on CSS transform where motion animates — flex-center backdrops instead
- `cargo build | tail` masks exit codes; `time` crate pinned 0.3.47

Deferred backlog (next sessions): light theme, vibrancy, app icon artwork, row insert/delete in browser, ctid-fallback editing, SSH tunnel UI (tunnel.rs never built — profiles work via existing local tunnels), per-tab sessions for transactions, JsonTree search, tree-mode JSON editing, Glide reevaluation if grids ever feel slow.

### 2026-06-12 — P5 (session 1, continued)
P5 complete, user-verified. THE differentiator feature works: edit any base-table cell from arbitrary SQL (incl. JOINs). Design notes: editability via `prepare()` of the same SQL (RowDescription table_oid/column_id — zero re-execution); `'value'::typename` casts give psql-typing semantics; PK WHERE values always from original row data; whole commit keyboard-driven (Enter in grid → ⌘S → Enter). Deferred: ctid fallback for PK-less tables (read-only reason is actionable instead), batched multi-cell row updates (one UPDATE per cell currently). Next: P6 inspector + jsonb.

### 2026-06-12 — P4 (session 1, continued)
P4 intellisense built + 3 user-feedback rounds. CRITICAL GOTCHA: the CodeMirror EditorView is created in a `useEffect([], )` — it captures completion-engine closures at mount. Vite HMR swaps modules but the live view keeps OLD references → engine edits silently don't apply. ALWAYS restart `tauri dev` (not just HMR) when testing editor-internal changes. Engine reads stores dynamically per keystroke (no Compartment reconfig — also HMR-fragile). Functions gated: typed flow excludes 3.5k pg functions unless settings.fnInComplete; ^Space always includes; ⌘⇧U / right-click → searchable function palette. context.ts parses FULL statement for tables (cursor-bounded only for clause) — `SELECT col| FROM t` needs tables after cursor.

### 2026-06-12 — P2 (session 1, continued)
P2 complete, all gates user-verified incl. 1M-row stream (50k cap), instant ⌘. cancel, statement chips, row/col/all selection, 5-format copy. Key learnings: (1) `simple_query_raw` is public in tokio-postgres — true streaming, no materialization. (2) `navigator.clipboard` is unavailable in dev (http://localhost = insecure context in WKWebView) — always use tauri-plugin-clipboard-manager. (3) Backdrop-close patterns must check `e.target === e.currentTarget` or inner button clicks die. (4) rAF-batched row flushing into zustand keeps stream renders at 60/s. Next: P3 (introspection + sidebar tree + lang-sql completion).

### 2026-06-12 — P1 (session 1, continued)
P1 complete. Rust core: driver/postgres (simple-protocol exec, TLS prefer/require/disable, cancel), secrets (keyring v3), appdb (rusqlite profiles), commands wired. Frontend: zustand connections store, profile list+form modal, query box (⌘Enter/⌘.), plain results table w/ per-statement blocks + error pane. Live staging smoke test in `src-tauri/tests/staging_smoke.rs` (run with --ignored + env creds). Gotchas: (1) Keychain items seeded via `security` CLI trigger an auth prompt asking for the *Mac login password* — app-saved passwords avoid it; dev rebuilds are ad-hoc-signed so prompts can recur until P10 signing. (2) keyring v4 is a breaking meta-crate — stay on v3. Next: P2 from top (splitter lexer first).

### 2026-06-12 — P0 (session 1)
Scaffolded. rustc 1.96.0, tauri 2.11.2 (React 19.2.7, Vite, bun). Docs written. Gotcha: `time` 0.3.48 breaks tauri-utils 2.9.2 (E0119) — pinned 0.3.47 in Cargo.lock, see DECISIONS.md. Beware: `cargo build | tail` masks exit code — check PIPESTATUS or drop the pipe. Next: P1 from top.
